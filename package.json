{
  "name": "stock-monitor",
  "version": "2.0.0",
  "description": "股票期货实时监控助手 - 实时监控、语音播报、价格提醒、涨停看板、走势图",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.3.0"
  },
  "scripts": {
    "dev": "vite",
    "server": "node server/index.js",
    "start": "node server/index.js",
    "build": "vite build",
    "preview": "npm run build && npm run server",
    "lint": "eslint \"src/**/*.js\" \"server/**/*.js\" \"tests/**/*.js\" \"e2e/**/*.js\" \"vite.config.js\" \"playwright.config.js\"",
    "lint:fix": "eslint src server tests e2e vite.config.js playwright.config.js --fix",
    "test": "qunit --require ./tests/_jsdom-setup.cjs \"tests/**/*.test.js\"",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "ci": "npm run lint && npm run test && npm run e2e && npm run build"
  },
  "dependencies": {
    "lightweight-charts": "^4.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "eslint": "^8.57.0",
    "jsdom": "^24.1.3",
    "qunit": "^2.20.0",
    "terser": "^5.36.0",
    "vite": "^5.4.0"
  }
}
