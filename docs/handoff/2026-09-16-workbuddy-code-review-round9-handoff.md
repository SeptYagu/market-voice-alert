# 国际品种接入方案 WorkBuddy 独立审查 round 9 复查报告

> **结论**：**未通过（1×P2 + 3×P3）**
> 被审 HEAD：`4beb59cb664bb521711a6ebd7138368324406464`
> 基准提交：`45593a5522343c1ed56725f841b16760f80217b7`
> 本轮实际审查增量：`b344926..4beb59c` = 4 文件 / +256 −58（纯文档；主方案 361 → 379 行）
> 审查日期：2026-09-16

---

## 一、审查基本信息与通过项简述（≤10 行）

- 版本核验：分支 `main` 已配置上游 `origin/main`、工作区干净且与远端一致；实际 HEAD `4beb59cb664bb521711a6ebd7138368324406464` 与任务给定待审 SHA **完全一致**，`45593a5` 为祖先；全量 diff 仅含 `STATUS.md`、`docs/handoff/INDEX.md` 与 9 份 `.md`，**未触碰产品代码与测试**。
- Round 8 五项缺陷主项**全部闭环**：`:182` 与 `:238` 的 `voiceSchedule.js:28` 判据已逐字一致（均保留 `session === 'after-close'`）；全文转义恢复（`\d`=8、`\|`=12、反斜杠合计 20，较上一轮少 2 处系表格内容变化所致），三张表列数统一为 9/4/6 且**无畸形行**；`sinaScale` 列已按真源回填（NQ/ES 登记值改为真新浪值）；`:59` 的 HSI 表述已限定为日盘并与 `:269` 复采表统一；§5 三条语音断言已补齐 `previous` 与固定时钟。
- 另核验：§3.1.2 清单 17 行的 `文件:行号` **逐条 `sed` 复原全部相符**（`utils.js:76-86`、`marketData.js:18-22/160/196`、`proxyRoutes.js:10/13`、`proxyService.js:10/31`、`klineService.js:40/41/95`、`api.js:30-31/36-39/252/260`、`marketSession.js:46-52`、`voiceSchedule.js:12/28`、`tts.js:217-218`、`alert.js:87-88`、`monitorTableView.js:95/339`、`kline.js:210/218/232/326/347/360-367/522`、`batchExportService.js:102`）；§3.1.1 登记除数与 `f107/f57` 反查键经真源复测成立。
- 独立设计并执行 5 组探针（10 品种同刻 `qt`/`trends2`/新浪三源比对、加解密与恒等式核验、仓库外副本按方案字面实现后回放 §5 四条语音断言 + 两处变异、港美股东财端点量纲、主机延迟/可达性），其中 **4 组命中缺陷**（见第二节）。

---

## 二、审查发现与缺陷清单

### P2-1 §5 新增的 CL/NG 跨源断言把「上游合约基差」固化为实现门禁 `[4.0%, 5.0%]` → 门禁随上游合约结构漂移、已被本项目自身实测与本轮登记值双双越界

- **严重级别**：P2
- **文件与行号**：
  - 断言：`docs/handoff/2026-09-16-global-market-feasibility-and-architecture-handoff.md:350-351`
    `const diff = Math.abs(qt.f43 / qtDivisor - sina.field0) / sina.field0;`
    `assert.ok(diff >= 0.040 && diff <= 0.050); // 断言价差严格处于 4.0%~5.0% 合约基准差区间`
  - 本轮新登记的区间：同文件 `:63`（CL「实测价差 4.3%~4.8%」）、`:67` / `:113`（NG「4.2%~4.5%」）、`:279`（「存在系统性 4.3%~4.8% 合约基差」）
  - 备源机制：同文件 `:266-279`（§3.4.2，本轮新增 CL/NG 处置）
- **触发条件**：对 Phase 1 的 `GL_CL0`/`GL_NG0` 执行 §5 该条跨源断言（按方案字面，这是 Phase 1 的必过门禁）。
- **实际行为与期望行为**：

  | 采样（北京） | `GL_CL0` 东财 `f43/qtDivisor` 对 新浪 `field0` | `GL_NG0` 东财 对 新浪 | 来源 |
  |---|---|---|---|
  | 2026-09-16 早段 | **4.97% ~ 5.1%**（五档一致偏离） | — | 本项目 Round 1 报告 `:81`（真源实测） |
  | 17:25 | 4.75% / 4.79% | 4.27% / 4.49% | Round 8 报告 `:64-65`（本机真源） |
  | **18:26（本轮独立复测）** | **4.605%**（103.72 对 99.154） | **4.1558%**（2.929 对 3.056） | 本轮探针（`push2delay` + `hq.sinajs.cn`，同刻同探针） |

  - 期望（按 `:351` 字面）：`diff ∈ [0.040, 0.050]` 恒成立。
  - 实际：该量的观测值在本项目自身的实测序列里为 **4.1558% ~ 5.1%**（跨度 0.94pp）——**上界 5.0% 已被 Round 1 的 4.97%~5.1% 越过**；**本轮新登记的 NG 区间「4.2%~4.5%」在 18:26 同刻实测即已被 4.1558% 证伪**（低于下沿 0.044pp）；CL 在 17:25→18:26 一小时内由 4.75%~4.79% 漂移到 4.605%（约 0.15pp/h），距下界 4.0% 仅剩 0.6pp。
- **根因**：`diff` 由**上游两源的合约结构**决定（新浪 `hf_CL`/`hf_NG` 跟踪当月合约，东财 `CL00Y`/`NG00Y` 为连续/换月指数），随交易日推进与换月持续漂移，**不由本方案任何一行代码控制**。`assert.ok(diff >= 0.040 && ...)` 中的**下界**尤其不是实现的正确性属性——它等价于要求「实现必须使两个上游源保持 ≥4% 的价差」，任何正确实现都会在上游收敛/换月时转红 → 与 Round 8 P2-2「方案自身 §5 门禁必然失败，实现者只能改断言或造数」属**同一失效模式**。
- **影响范围**：Phase 1 验收规格不可稳定满足；实现者按字面交付时会在不同交易日得到随机的门禁红/绿，最终只能删除或放宽该断言，导致本轮 P2-2 修复的验收要求（「`GL_CL0`/`GL_NG0` 的跨源偏差有显式登记与处置」）失去可测量性。同时 `:351` 与 §3.4.2 `:279`「严禁强行按 1:1 比对绝对价格」在措辞上互相拉扯（前者仍在硬性约束绝对价的跨源关系）。
- **复现方法/运行证据**：`node` 探针（仓库外，未改产品代码）直连 `https://push2delay.eastmoney.com/api/qt/stock/get?secid=102.CL00Y|102.NG00Y&fields=f43,...,f169` 与 `https://hq.sinajs.cn/list=hf_CL,hf_NG`（带 `Referer: https://finance.sina.com.cn`，GBK→latin1 解析），同刻读取；配套可判别性对照：同探针同刻其余 8 品种跨源偏差为 `GC 0.0319% / SI 0.0200% / HG 0.0357% / NQ 0.0000% / ES 0.0000% / YM 0.0136% / A50 0.0032% / HSI 0.0067%`（均 ≤0.04%，排除网络与时刻因素），东财内部 `qt.f60/qtDivisor == trends2.preClose` 对 10/10 品种误差恒 0。
- **修复建议**：把 CL/NG 的验收改为**实现可控性质**，删除对上游绝对价差的硬区间：
  1. 断言备源报价的昨结基准取自备源自身：`parseSinaGlobalFuture(hfPayload, { code:'GL_CL0' }).prevClose === Number(payload.split(',')[7])`；
  2. 断言涨跌幅由自身昨结计算且**主备切换后同一时刻的 `changePercent` 跳变 < 0.6pp**（实测 CL 切换前后为 `-1.99%`(东财) 对 `-1.58%`(新浪)，量级正确）；
  3. 绝对价差区间（4.0%~5.0%）**降级为资料性注释**或改为 `diff > 0 && diff < 0.10` 的健壮性守卫，并显式标注「该量由上游合约结构决定，随换月漂移，不作为实现门禁」。
- **修复后验收标准**：CL/NG 的断言不含对上游绝对价差的硬区间；把「备源昨结基准」错绑为东财 `f60`（变异）时用例**必须确定性转红**；连续两个交易日各采一次，门禁结果一致。

---

### P3-1 §5 新增断言引用未定义标识 `resolveFallbackQuote`（全仓与方案内均无定义、未纳入 §3.1.2 清单）→ 按字面无法落码

- **严重级别**：P3
- **文件与行号**：同文件 `:353`
  `assert.equal(resolveFallbackQuote('GL_CL0', sinaPayload).prevClose, Number(sinaPayload.split(',')[7]));`
- **触发条件**：按 §5 文字实现该条 Phase 1 门禁用例。
- **实际行为与期望行为**：
  - 期望：§5 作为「单一可测量门禁规范」，其中出现的标识应可在方案内定位到定义或落点。
  - 实际：`grep -rn "resolveFallbackQuote" --include=*.js --include=*.md .` **仅命中该文档自身**（1 处）；仓库 `src/`、`server/`、`tests/` 无此导出，§3.1.2 的 17 条改造点（`:170`-`:186`）与 §3.4.2 均未定义它（`parseSinaGlobalFuture` 在 `:178` 被提及但未给该语义与签名）。同时 `sinaPayload` 为未绑定的伪变量。
- **根因**：本轮为闭环 Round 8 P2-2 而在 §5 追加了一条新断言，直接引入了一个新的解析入口名，但未同步在 §3.1.2 登记其落点（与 Round 3 P3-1「引用仓库不存在的标识 `inferAssetType`、未纳入改造清单」同族，Round 3 已按 P3 处理，本轮同标准）。
- **影响范围**：Phase 1 该条验收断言不可执行；实现者须自行猜测「备源昨结重校」落在哪个函数，存在偏移到主源解析路径（`parseEastmoney`）而破坏 A 股路径的风险。
- **修复建议**：二选一 —— ① 在 §3.1.2 增加该函数的落点行（例如「`src/js/parser.js` 新增导出 `parseSinaGlobalFuture(raw, { code, scale, baseFromOwnField7 })`」，或将其定义为备源解析器内部入口），并在 §3.4.2 引用同一名称；② 改写断言为对既有/新列导出的直接调用（如 `parseSinaGlobalFuture(hfPayload, { code:'GL_CL0' }).prevClose`）。
- **修复后验收标准**：§5 中出现的每个标识均可在方案内定位到定义/落点；按文档字面落码后可执行且通过。

---

### P3-2 P3-1（HKFE 日夜盘）闭环证据表未覆盖 Round 8 明确要求的「日盘（16:00 前）与夜盘（17:15 后）各 ≥3 次」，而 `:59` 新增的正是「日盘常规交易时段」这一活体声明

- **严重级别**：P3
- **文件与行号**：
  - 复采表：同文件 `:271-276`（4 行：16:35 / 17:07 / 17:23 / 17:28）
  - 被支撑的声明：同文件 `:59`（「**日盘常规交易时段**新浪 `hf_HSI` 字段 7 昨结 24676.000 与东财 `f60` 1:1 对齐」）
  - Round 8 的要求：`docs/handoff/2026-09-16-workbuddy-code-review-round8-handoff.md:105-106`（「日盘（16:00 前）与夜盘（17:15 后）各复采 ≥3 次」「修复后验收标准：日/夜盘复采证据落档」）
- **触发条件**：核对 `:59` 的「日盘常规交易时段 1:1 对齐」是否有盘中样本支撑。
- **实际行为与期望行为**：
  - 期望：日盘盘中（16:00 前）≥3 次、夜盘（17:15 后）≥3 次采样落档。
  - 实际：表中 16:35 被自标为「**日盘收盘后**」（非盘中）、17:07 为「转场清算中」（夜盘 17:15 开盘**前**）、17:23 与 17:28 两行数值完全相同（24688 / 24681）——即**日盘盘中样本 0 个**、夜盘有效样本实为 1 组，且 4 行数值分别沿用 Round 7（16:35、17:07）与 Round 8（17:23、17:28）既有采样，非本轮新增复采。
- **根因**：本轮把既有单点测量重新编排为「表格 + 机制说明」，把 Round 8 要求的「跨时段多轮复采」理解为「补齐已有采样时刻的行」。
- **影响范围**：`:59` 关于**日盘常规交易时段** 1:1 的声明仍只有收盘后（16:35，结算价口径）样本支撑；主备源切换锚点（`trends2.preClose`）在日盘盘中是否恒等未被验证。**机制结论本身方向正确**：本轮 18:26 独立复测得东财 `134.HSI_M` `f60 = 24688 = trends2.preClose`，新浪 `hf_HSI` 字段 7 亦已收敛至 `24688`（较表中 17:23/17:28 的 24681 更新），跨源昨结差由 0.028% 归零，与「以东财分时 `trends2.preClose` 为权威基准」的处置一致。
- **修复建议**：① 按 Round 8 要求补日盘盘中（如 10:00、14:00、15:30）与夜盘（如 18:00、20:00、22:00）各 ≥3 次采样，保留采样时刻、两侧数值与偏差百分比；② 若无法在日盘盘中复采，则把 `:59` 的限定由「**日盘常规交易时段**」改为「**日盘结算后（16:30 后）**」，使声明与证据口径一致。
- **修复后验收标准**：`:59` 的限定与 `:271-276` 的样本时段一一对应；表中每行的采样时刻、两侧数值、偏差可被同日独立复采复现（容差 ≤1 点或 0.01%）。

---

### P3-3 Phase 2/3（港美股）未登记东财 `qt` 端点的报价量纲，而 §3.1.2 行 5 指定的兜底恰为 `div100` → 按字面实现将再现 10× 错位（本轮首次实测该量纲）

- **严重级别**：P3
- **文件与行号**：
  - 实施口径：同文件 `:175`（§3.1.2 行 5：「按品种特定的 `qtDivisor`…**未命中回退 `div100`**」；③「其余外盘与港美标的按注册表自身口径解析」）
  - 注册表范围：同文件 `:107-118`（§3.1.1 只登记 10 个外盘期货，无 `hk*`/`us*` 行）
  - 路线图：同文件 `:309-314`、`:321-325`（Phase 2 港股 / Phase 3 美股）
  - 兜底路径落点：`src/js/api.js:150`（`fetchQuotes`）→ `:167`（`missingStocks`）→ `:172`（`fetchEastmoney(missingStocks, opts)`）→ `src/js/api.js:113-119`（`fetchEastmoneyOne` → `parseEastmoney`）
- **触发条件**：Phase 2/3 落地后，任何腾讯未返回的港股/美股代码（`w`）走东财 `qt` 兜底并进入 `parseEastmoney`。
- **实际行为与期望行为**：**2026-09-16 18:26 真源实测**（`push2delay.eastmoney.com/api/qt/stock/get`）：

  | secid | 东财 `qt.f43` | 同源 `trends2` 末根 | 腾讯 `field3` | 实测除数 |
  |---|---|---|---|---|
  | `116.00700` | `433400` | `433.400` | `433.400` | **÷1000** |
  | `116.09988` | `106200` | `106.200` | `106.200` | **÷1000** |
  | `105.AAPL` | `331340` | `331.340` | `331.34` | **÷1000** |
  | `106.BABA` | `109340` | `109.340` | `109.34` | **÷1000** |
  | `105.TSLA` | `356580` | `356.580` | `356.58` | **÷1000** |

  - 期望：按 `:175` ③「按注册表自身口径解析」得出真实价格。
  - 实际：`hk*`/`us*` 的 `(f107, f57)` 键不在 `globalCatalog` → 命中 `:175` 的「未命中回退 `div100`」→ 得到 `4334.00`（港股）/ `3313.40`（美股），**较真值大 10 倍**；即本方案已连续 8 轮处置的「10~100 倍端点量纲错位」会在 Phase 2/3 原形再现，而 §5 的验收断言（`:334-376`）全部只覆盖 Phase 1 的 10 个外盘期货，无任何港美股量纲断言。
- **根因**：方案的注册表与归一化设计只面向外盘期货；港美股的量纲（东财 `qt` 对 `m:105/106/116` 统一为 ×1000，同源 `trends2`/腾讯为真实单位）从未取真源复核（历轮「Phase 2/3 未重采」的未验证项），而 `:175` 又把 `div100` 写成通用兜底。
- **影响范围**：Phase 2/3 的价格、涨跌幅、语音播报均可能错 10 倍（若同时按 `:175` ② 泛化了身份推导使报价能写入 `state.quotes`）；当前若不泛化身份推导，则表现为报价整段被 `monitorController` 丢弃（`code='sz00700'`），即港美股在腾讯失败时无报价可兜底。两种结果都不满足 §3.4「第一数据源」与 §2.2/§2.3 的可用性预期。
- **修复建议**：① 在 §3.1.1 注册表新增港股/美股段（实测 `hk*`/`us*` 的东财 `qt` 除数 = **1000**，`trends2` 与腾讯为 1:1），或另建 `hkUsCatalog`；② 把 `:175` 的「未命中回退 `div100`」改为「未命中回退到按 `f107` 分派的 A 股（`div100`）/港美股（`div1000`）默认除数」；③ §5 补港美股量纲断言：`parseEastmoney({ data:{ f107:116, f57:'00700', f43:433400, f60:438800 } }).price === 433.40 && prevClose === 438.80`；④ 明确港美股 `code` 身份推导（`hk*`/`us*`）是否同轮泛化。
- **修复后验收标准**：`hk*`/`us*` 的东财 `qt` 除数在方案中显式登记且有真源采样支撑；§5 存在对应的港美股量纲断言，把除数改错一档时**必须确定性转红**。

---

## 三、待确认风险与未验证项

### 待确认风险

1. **`hf_*` 与 `GL_*` 同时被分类为 `futures_global`，但方案未定义 `hf_*` 的准入策略（有合理怀疑依据，未定级）**：`:173` 契约 ① 与 §3.1.1 NOTE 的断言强制 `inferAssetType('hf_CL'/'hf_SI'/'hf_HSI') === 'futures_global'`，而 `:98` 的 `ASSET_TYPES.FUTURES_GLOBAL` 注释与 §1.2 的标的清单只列 `GL_*`；§3.4.2 又声明「新浪 `hf_` 仅作为**全量实时报价备用源**」。若 UI/搜索允许把 `hf_*` 加入自选池，同一品种会与 `GL_CL0` **并存且价格水平不同**（本轮同刻实测 `hf_CL 99.154` 对 `CL00Y/100 = 103.72`，差 4.6%；`hf_HG 650.082` 美分对东财 `6.4985` 美元，差 100×），而 `toEastmoneySecId` 按 `:174` 只支持 `GL_*`/`hk*`/`us*` → `hf_*` 无东财 secid，将出现「同品种两个报价、其中一个无图表」的用户可见现象。**建议**：在方案中明确「`hf_*` 仅作备用源解析入口、不作为可入池代码」，或说明二者互斥的准入规则。**验证方法**：检查搜索联想/`normalizeCode` 输出对象的准入过滤是否排除 `hf_`。

2. **`ChinaFuturesSessionStrategy` 未锚定既有 `src/js/futures/session.js`（`getFuturesSession`），内盘精密规则的复用路径未落文字**：`:205` 只写「保持原日夜盘、节前夜无夜盘、品种闭市时点不变」，而 `futures/session.js` 与 `getFuturesSession` 在方案中**零出现**、未纳入 §3.1.2 的 17 条清单；同时 §3.1.2 行 11（`:181`）要求把 `marketSession.js:46-52` 的二元分派整体改写为策略分派。既有回归网对该行为覆盖极薄（`tests/reviewFollowup.test.js:21-22` 仅 2 条 `getVoiceEligibleCodes` 用例），若策略被重新实现而非委托既有模块，「节前夜无夜盘」「02:30 凌晨续段收盘」等精密规则存在静默回归风险。**建议**：在 §3.1.2 与 §3.2.1 显式写明 `ChinaFuturesSessionStrategy` **必须委托既有 `getFuturesSession(code, now, tradingDates)`**，并在 §5 增设一条以既有模块输出为基准的等价性断言。

### 未验证项（含残余风险）

1. **受限网络「先失败 → 后轮转 `push2delay`」的转移时序仍无法在本出口复现**（承接 Round 6/8）：本机实测 `push2his.eastmoney.com` 的 `kline`/`trends2` 均 `200`（可达）、`90.push2his.eastmoney.com` 为 `socket hang up`、`push2.eastmoney.com` 恒 `502` —— 与方案 §2.1 `:51` 所述「受限网络（仅 `push2delay` 可达）」**不是同一出口画像**，故 §5 `:369-370` 的轮转兜底断言无法在此环境落地验证。残余风险：轮转实现后的失败切换时延与 `server/proxyService.js:35` 的 `timeoutMs: 15_000`、前端刷新预算叠加（该预算约束方案仍未给出）。
2. **§2.1 `:32`「实测响应延时 `< 100ms`」无法在本环境复现**（该文本本轮未改动，仅作记录）：新建连接实测 Sina `hf_CL` 670ms / 5 品种批 206ms、东财 `push2delay` `qt` 971ms / `trends2` 2055ms、腾讯 219~914ms；上述均含 TLS 建链与未复用连接的开销，不足以直接否证该阈值，但亦无任何落档采样支撑。
3. **HKFE 日盘盘中（16:00 前）两侧昨结一致性**本轮未采（采样时点为北京 18:26 夜盘，已并入 P3-2）。
4. **Phase 2/3 根数与字段口径**：港股 `trends2` 331 / `klt=1` 330、美股 `trends2` 391 / `klt=1` 390、腾讯 `r_hk` 78 字段与美股「71 + 偶发 73」本轮未重采（沿用 Round 2/3 结论；本轮仅旁证 `r_hk00700=78`、`usAAPL=71`、`usTSLA=73` 与之一致）。

---

## 四、推荐修复顺序与复审验收标准

**推荐修复顺序**：P2-1（直接决定 Phase 1 门禁可否稳定交付，且牵涉 §3.4.2 措辞一致性）→ P3-1（门禁可执行性，一改即净）→ P3-3（Phase 2/3 量纲，随注册表一并补）→ P3-2（补齐日盘盘中采样或收缩声明口径）→ 待确认风险 1/2。

**复审验收标准**：
1. CL/NG 的 §5 断言不含对上游绝对价差的硬区间；改为「备源昨结取自备源 field7 + 切换后涨跌幅跳变 < 0.6pp」，且把基准错绑为主源 `f60`（变异）时用例必须确定性转红；`:351` 与 §3.4.2 `:279` 措辞不再互斥。
2. §5 中出现的全部标识均可在方案内定位到定义/落点，按字面落码后可执行；`resolveFallbackQuote`（或替代名）已纳入 §3.1.2 清单。
3. `hk*`/`us*` 的东财 `qt` 除数在注册表中显式登记并有真源采样支撑；§5 存在港美股量纲断言，除数错一档必须转红。
4. `:59` 的时段限定与 `:271-276` 的样本时段一一对应（补日盘盘中 ≥3 次，或把声明收缩为「日盘结算后」）。
5. Round 8 的 3×P2 + 2×P3 主项**保持**闭环（本轮已独立复测成立，不得回退）；本文 1×P2 + 3×P3 与两项待确认风险 **100% 修复/定论闭环**；未验证项保持明确列明。

**审查通过条件**：上述全部达成，且不得引入新的结构性/契约性缺陷，方可判定通过。
