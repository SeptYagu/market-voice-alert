# WorkBuddy 独立代码审查 Round 2 — 国际期货/国际股票接入可行性与架构方案（复查）

> **文件名说明**：任务模板指定的 `docs/handoff/2026-09-16-workbuddy-code-review-round2-handoff.md` **已被另一条审查链占用**（图表日期格式与成交量红绿修复 Round 2，被审 `1d08f60`，本文件被 `STATUS.md:52` 与 `docs/handoff/INDEX.md:9` 引用）。为避免覆盖他链既有记录，本报告改用任务作用域命名 `2026-09-16-global-market-workbuddy-code-review-round2-handoff.md`（与被审方案 `2026-09-16-global-market-feasibility-and-architecture-handoff.md` 同链同前缀）。

## 一、审查基本信息与通过项简述

- 被审 HEAD SHA：`ea2bb5aeb2bc85920734ccdce904c2897e401666`（= 待审提交，已核对一致；`git pull --ff-only` 无变更，工作区干净，无 `docs/review-checklist.md`）
- 基准 SHA：`45593a5522343c1ed56725f841b16760f80217b7`；实际审查范围 `45593a5..ea2bb5a` = 4 文件 / +465 −2，全为文档；本轮**无产品代码与测试改动**，`npm test` 实跑 **843/843**（与 §5.1 声称一致）。
- 通过项（极简）：Round 1 的 P2-1 主项（`m:101/102/103` 上 `CL/GC/SI/HG/NG/NQ/ES/YM` **8 个 secid 双端点实测取数成立**）、P2-2（`GL_` 命名空间、`hf_CHA50CFD` 实测 15 字段有效）、P2-3（A 股窗口 `[[555,690],[780,900]]` 与现码 `api.js:36-39` **逐值一致**，不回归）、P2-4（根因描述与 `marketSession.js:46-52/86-103` 相符）、P2-5（9 行清单含服务端 2 行，5 个正则行号逐条相符，新正则实测可匹配 `v_r_hk00700`/`v_usAAPL`，旧正则对二者匹配数为 0）、P3-3（Gzip 基准 115.10 KB 与 `zlib.gzipSync` 实测 **115,098 B 精确一致**）、P3-4（持仓量 index 9 按品种实测 0 / 非 0 与纠偏一致）经独立复核实测闭环。
- 总体判定：**未通过**，**2×P2 + 5×P3**。核心问题：Round 1 P2-1 的**同类缺陷在新增的 `m:100` 行原形复现**（2/10 个 Phase 1 品种 secid 实测为空），且本轮新引入的 CME 结算窗口固化**删除了 Round 1 原文的冬季变体**，构成连带回归。

## 二、审查发现与缺陷清单

### P2-1 新增 `m:100` 行整行为伪：市场号归属错误 + 两个 secid 实测 `data:null`（GL_A50 / GL_HSI 无法按方案取数）

- **文件与行号**：`docs/handoff/2026-09-16-global-market-feasibility-and-architecture-handoff.md:56`（`m:100` 分类）、`:96`（`GL_A50` → `100.CHA50CFD`）、`:97`（`GL_HSI` → `100.HSI00Y`）、`:229`（Phase 1 标的范围 10 品种）、`:245`（`secid 有效性` 验收断言）；同步扩散至 `STATUS.md:9` 与 `docs/handoff/INDEX.md:8`
- **触发条件**：按 §2.1 的显式注册表为 `GL_A50` / `GL_HSI` 请求东财 `qt/stock/get` 或 `trends2`。
- **实际行为**：
  1. `100.CHA50CFD`：`{"rc":100,...,"data":null}`；`trends2` 同为 `data:null`（多主机 × 多轮重试一致，非 502 抖动）。
  2. `100.HSI00Y`：同上，`data:null`。
  3. `m:100` 的真实归属经东财自身 `clist` 枚举为**国际指数**市场（63 只：`KOSPI200/KS11/HEX25/CRB/NZ50/XIN9|富时中国A50/TWII/N225/BVSP/SENSEX/HSI|恒生指数/SPX/DJIA/NDX...`），并非方案所称「新加坡/港期」；且其中 `XIN9` 为**富时中国A50 现货指数**、`HSI` 为**恒生指数现货**，均非期货合约。
  4. 真实 A50 **期指** secid 为 `104.CN00Y`（`f58=A50期指当月连续`，`trends2` n=1088，non-null），归属 `m:104`（新加坡期货：`CN`=A50 期指、`TF`=20 号合成胶、`RT`=3 号烟片胶）。
- **期望行为**：注册表中每个 secid 均可实测取数；市场号归属与东财 `clist` 一致；恒指期货若东财确无合约，应显式登记为「仅新浪报价、无东财分时/K线」，而非填入无效 secid。
- **根因**：Round 1 P2-1 的根因（以单样本外推全局规则、未回真源逐项求证）在新引入的两个品种上原形复现——`m:100` 行未做 `clist` 归属核验，也未对 `100.CHA50CFD`/`100.HSI00Y` 做单品种取数探测（8 个 CME/NYMEX 品种做了，恰好这 2 个没做）。
- **影响范围**：Phase 1 的 10 个品种中 `GL_A50`、`GL_HSI` 无法取数 → 与本文档 **`:245` 自身的验收断言自相矛盾**（「Phase 1 全部 10 个品种的 Eastmoney secid 均能成功拉取 `trends2` 与 `kline`，无 502/空数据」按现方案必失败）；`:185-192` §3.4「外盘期货优先采用东方财富作为第一数据源」对这两个品种不可行且未给降级契约；`STATUS.md:9` / `INDEX.md:8` 的「缺陷全面闭环」结论随之失实。
- **复现方法/运行证据**（探针在仓库外 `D:/review_r2/`，直接请求真源，未触碰仓库）：
  - `probe3.mjs 102.CL00Y 101.GC00Y 101.SI00Y 101.HG00Y 102.NG00Y 103.NQ00Y 103.ES00Y 103.YM00Y 100.CHA50CFD 100.HSI00Y` → 前 8 个 `qt`/`trends2` 全部 OK；`100.CHA50CFD`、`100.HSI00Y` 在两端点均为 `data:null`（`raw={"rc":100,...,"data":null}`）。
  - `probe5.mjs`（m:100→m:124 全量 `clist` 扫描）：`m:100 n=63 HITS: XIN9|富时中国A50 ; HSI|恒生指数`；`m:104 n=33 HITS: CN00Y|A50期指当月连续 ...`；**未发现任何恒指期货合约**。
  - `probe3.mjs 104.CN00Y` → `qt OK f58=A50期指当月连续 last=143940 f60=142710`；`trends2 OK n=1088`。
- **修复建议**：`GL_A50` 的 secid 改为 `104.CN00Y`（并复核 `hf_CHA50CFD` 与 `104.CN00Y` 的合约/基准锚点，落实 §3.4 的昨结对齐）；`GL_HSI` 若东财无对应合约，则把该行标注为「无东财 secid，仅新浪 `hf_HSI` 报价」，并在 §3.4 / §5.2 中为「无东财源品种」单列降级契约与验收断言；`m:100` 行按东财 `clist` 实际归属重写或删除；`STATUS.md` / `INDEX.md` 的对应描述同步修正。
- **修复后验收标准**：对 Phase 1 每个品种（含 `GL_A50`、`GL_HSI`）执行 `qt/stock/get` + `trends2` 双端点探测，结果与注册表逐条一致（有 secid 者非空，无 secid 者以「仅新浪源」显式登记）；文档中不再出现把 `m:100` 描述为「新加坡/港期」或把 `100.CHA50CFD`/`100.HSI00Y` 列为有效 secid 的表述。

### P2-2 本轮把 CME 每日结算休市窗口固化为「北京时间 05:00-06:00」，删去了 Round 1 原文的冬季变体 → 冬季把真实交易时段当休市剔除（连带回归）

- **文件与行号**：`…architecture-handoff.md:145`（`GlobalFuturesSessionStrategy`：`除每日 05:00-06:00 结算休市`）、`:178`（§3.3「外盘期货：全天窗口，剔除每日 05:00-06:00 结算点」）；对照 Round 1 原文 `a2b8791:…:56`（`每日仅有 1 小时结算休市窗口（北京时间 05:00-06:00 或 06:00-07:00）`）
- **触发条件**：美国处于标准时（每年 11 月第一个周日至次年 3 月第二个周日），对外盘期货取分时并按 §3.3 的窗口过滤。
- **实际行为**：CME Globex 的每日结算休市为**芝加哥本地 16:00–17:00**，换算北京时间随 DST 平移：夏令时 → 北京 **05:00–06:00**（本机实测一致：`102.CL00Y`/`101.GC00Y`/`101.SI00Y`/`101.HG00Y`/`102.NG00Y` 的 `trends2` 首根均为 `06:00`）；标准时 → 北京 **06:00–07:00**。方案把窗口固化为「05:00-06:00」，于是在标准时期间：① 北京 05:00–06:00（= CT 15:00–16:00）**真实在交易**却被当休市剔除 → 外盘分时出现 1 小时断裂；② 真正的休市时段 06:00–07:00 未被声明剔除（该时段无 bar，尚不致错）。
- **期望行为**：结算休市窗口由美东/芝加哥 DST 纯函数推导（方案 `:141` 已为美股定义 `isUsDaylightSavingTime`），夏令时 05:00–06:00 / 标准时 06:00–07:00 两档显式给出。
- **根因**：本轮重写 §3.2.1 第 5 条时把 Round 1 原文已经写明的双变体（「或 06:00-07:00」）删除，收窄为单一常量；同一文档对美股做了 DST 分派（`:141-142`）却对 CME 未做，属同一时间模型内部不自洽。
- **影响范围**：Phase 1 核心交付（外盘分时/K线）在标准时期间（约 5 个月/年）每日丢失约 60 根真实 bar，与 `:248` §5.2「外盘分时无日内断裂」验收断言冲突；亦与 `:216` gantt 的「多时区会话策略与语音调度解耦」目标相悖。
- **复现方法/运行证据**：窗口算术（CT 16:00–17:00，CDT=UTC-5 → 北京 05:00–06:00；CST=UTC-6 → 北京 06:00–07:00，北京=UTC+8）+ 本机 DST 期真源实测首根 06:00（`probe3.mjs` 输出 `first=2026-09-16 06:00`）；`a2b8791` 原文含双变体（`git show a2b8791:docs/handoff/…-handoff.md` 可见），本轮删除。
- **修复建议**：`:145` / `:178` 恢复双变体并注明由 DST 纯函数判定；§3.3 的外盘期货窗口改为「按 CME 结算窗口（DST 分派）剔除」而非固定常量。
- **修复后验收标准**：以冬令时/夏令时两个固定时钟分别驱动 `GlobalFuturesSessionStrategy`，断言结算窗口为 `06:00-07:00` / `05:00-06:00`；文档不再出现未带季节条件的单一「05:00-06:00」结算窗口表述。

### P3-1 §3.1.1 的「启动期冲突断言」恒真且引用不存在的标识 → 「绝对正交」的保证为零

- **文件与行号**：`:99-105`（`for (const key of Object.keys(GLOBAL_PRODUCT_MAP)) assert(!DOMESTIC_PRODUCT_MAP[key], …)`）
- **触发条件**：执行该断言；或出现真实的标识冲突（用户输入 `SI0` 需判定归属）。
- **实际行为**：① 该循环用 **global 的键**去查 **domestic 的表**，而 `GL_` 前缀命名空间与国内品种键（`RB0`/`SI0`/`IF2603`…）按构造不可能相交 → **任何情况下都不会失败**，不构成防护；② `DOMESTIC_PRODUCT_MAP` 在仓库中不存在——国内品种表实为 `src/js/futures/contractCatalog.js:16` 的 `PRODUCT_MAP`，`GLOBAL_PRODUCT_MAP`/`DOMESTIC_PRODUCT_MAP` 在 `src/`、`server/` 全域 `grep` 均无命中，片段照抄将直接 `ReferenceError`。
- **期望行为**：断言应落在真正的冲突面——**输入解析的分派结果**（如 `parseFutureInput('SI0').exchange === 'gfex'`、`inferAssetType('GL_SI0') === FUTURES_GLOBAL`），而非两个必然不相交的键集合。
- **根因**：把「命名空间不冲突」当作可由「键集合互斥」证明的性质（二者互为因果），并沿用手写示意变量名而未与既有导出对齐。
- **影响范围**：文档声称的「单元测试与系统启动时双重校验，保证国内与国际期货命名空间绝对正交」不成立；Round 1 P2-2 要求的「外盘/内盘标识冲突有自动化断言」实际未被闭合。
- **复现方法/运行证据**：`grep -rn "DOMESTIC_PRODUCT_MAP\|GLOBAL_PRODUCT_MAP" src/ server/` → 0 命中；`grep -n "export const PRODUCT_MAP" src/js/futures/contractCatalog.js` → `16:export const PRODUCT_MAP = Object.freeze({`；键集合互斥的构造性论证见 `ASSET_TYPES` 注释中的 `GL_` 前缀约定（`:77`）。
- **修复建议**：改为对解析器/分派器结果的断言，并修正被引用标识名称（`PRODUCT_MAP`）。
- **修复后验收标准**：存在能**在冲突发生时失败**的断言（如把 `GLOBAL` 目录中某条目的代码改为与国内键同名时，该断言必须失败）；引用标识与仓库实际导出一致。

### P3-2 §3.1.2 表将 `src/js/kline.js:331` 标注为 `STOCK_CODE_RE`，该行实为 `getPriceLimit` 内部行

- **文件与行号**：`:119`（表格第 8 行「`src/js/kline.js:210, 331` (`STOCK_CODE_RE`)」）；对照同表第 9 行 `:120`「`src/js/kline.js:326-345` (`getPriceLimit`)」
- **触发条件**：按改造清单定位并修改 `STOCK_CODE_RE` 的形态假设点。
- **实际行为**：`STOCK_CODE_RE` 在 `kline.js` 的出现位置为 **210**（声明）、**218**、**232**（两处守卫，分别对应 `buildTencentKlineUrl` 与 `buildTencentYearKlineUrl`）；**331** 落在 `getPriceLimit` 函数体内（`const numeric = code.replace(/^(sh|sz|bj)/i, '')`），与 `STOCK_CODE_RE` 无关。
- **期望行为**：行号引用指向真实的形态假设点（210/218/232）；`getPriceLimit` 已由本表第 9 行单列，不应在同一行重复指向。
- **根因**：清单填写时未逐行 `sed` 回代码求证（Round 1 原文为 `kline.js:210`，本轮新增的 `, 331` 未核）。
- **影响范围**：按清单实施会改错位置，且漏改 218/232 两个真实守卫点 → `buildTencentKlineUrl`/`buildTencentYearKlineUrl` 对港/美/外盘仍返回 `null`，直接导致 §2.2/§2.3 的 K 线链路对 `hk00700`/`usAAPL` 失效。
- **复现方法/运行证据**：`grep -n "STOCK_CODE_RE" src/js/kline.js` → `210/218/232`；`sed -n '331p' src/js/kline.js` → `const numeric = code.replace(/^(sh|sz|bj)/i, '');`。
- **修复建议**：改为 `src/js/kline.js:210, 218, 232`。
- **修复后验收标准**：清单中每一条 `文件:行号` 均可由 `sed -n '<行号>p'` 还原出该单元格声称的逻辑。

### P3-3 §3.1.2 表第 9 行的改造目标不可达：`getPriceLimit` 返回 `null` 会被消费方静默回落为 10

- **文件与行号**：`:120`（改造目标「非 A 股标的返回 `null`，彻底消除港美股/外盘 K 线上无意义的 ±10% 限价线」）；消费方 `src/js/kline.js:345`（`const lim = Number(limit) || 10;`）、`src/js/kline.js:360-367`（`formatCandleColors` → `classifyKlineBar(it, prevClose, limit)`）
- **触发条件**：按方案让 `getPriceLimit('hk00700', …)` 等非 A 股返回 `null`，随后渲染 K 线（走 `formatCandleColors`）。
- **实际行为**：`Number(null) === 0`，`0 || 10` → `10`，即 `classifyKlineBar` 仍以 **10%** 阈值判定涨停/跌停与颜色。以真实模块实测：`classifyKlineBar({open:100,high:110,low:99,close:109.9}, 100, null)` 与 `classifyKlineBar(…, 100, 10)` **返回完全相同**（均为 `'limit-up-failed'`）。因此「返回 `null`」这一步**不改变任何可观测行为**，方案所称「彻底消除」不成立。
- **期望行为**：非 A 股标的的涨跌停判定整体失效（消费方需显式判空/按资产类型短路，或改为传入资产类型而非依赖数值回退）。
- **根因**：只改了数值产生方（`getPriceLimit`），未同步消费方的 `Number(limit) || 10` 回退守卫；与 Round 1 P3-2 修复建议中的「渲染层跳过」半句未落实一致。
- **影响范围**：港/美/外盘 K 线仍会对涨幅 >9.8% 的 bar 打出涨跌停分类/配色（无涨跌停制度市场产生误导性标记）；Round 1 P3-2 的验收标准「港/美/外盘 K 线不出现涨跌停线/标记」未达成。
- **复现方法/运行证据**（仓库外探针，直接 import 真实模块）：
  ```
  getPriceLimit('hk00700','腾讯控股') = 10
  getPriceLimit('usAAPL','苹果')       = 10
  Number(null)||10                     = 10
  classifyKlineBar(it,100,10)   -> limit-up-failed
  classifyKlineBar(it,100,null) -> limit-up-failed   // 完全等价
  ```
- **修复建议**：改造点补为「`getPriceLimit` 非 A 股返回 `null`」**且**「`classifyKlineBar`/`formatCandleColors` 对 `null` 显式短路（不回落 10）」，或改为传入资产类型由函数内部判定。
- **修复后验收标准**：存在断言证明改造后非 A 股标的的涨跌停分类不再生效（如 `classifyKlineBar(涨幅 9.9% 的 hk 样本, 昨收, null) === 'normal'`），同时 A 股 5/10/20/30% 既有用例全绿。

### P3-4 §2.3 腾讯美股「73 字段」与实测不符（71），未标注取值条件

- **文件与行号**：`:63`（「腾讯 `https://qt.gtimg.cn/q=usAAPL`（73 字段）」）；同类定值断言另见 `:59`（港股 78 字段）
- **触发条件**：按该字段数编写/校验美股解析器（如断言 `fields.length >= 73`）。
- **实际行为**：实测 `usAAPL`、`usBABA` 的 payload 均为 **71** 字段（`fields[3]=331.34` 收盘价、`fields[30]='2026-09-15 16:00:01'` 时间格式均与方案一致）；同一探针下 `r_hk00700` 为 **78** 字段（与方案一致）。美股 73 与实测 71 不符，且未注明取值条件（会话状态）。
- **期望行为**：字段数以实测为准，并标注取值条件（本次为美股非交易时段实测）。
- **根因**：该数值直接沿用了 Round 1 审查报告中的表述（报告 `:139` 亦写 73），**未回真源复测**——与本链 Round 15 P3 的教训同类（不得以上一轮报告的结论为事实来源）。
- **影响范围**：验收标准 1（数据源「字段完整性」评估）的支撑数字失真；若实现按 73 做长度校验，美股解析可能被错误拒绝/误判为不完整。
- **复现方法/运行证据**：`probe6.mjs` 输出 `[TENCENT usAAPL] fields=71 … [TENCENT usBABA] fields=71 … [TENCENT r_hk00700] fields=78`（GBK 解码后按 payload `split('~')` 计数）。
- **修复建议**：改为 71 并标注「美股非交易时段实测；盘中字段数可能不同」；港股 78 处同样标注条件。
- **修复后验收标准**：文档内所有「N 字段」断言均可由一次真源探测复现，且带有取值条件。

### P3-5 §2.2 港股「330 根 = 09:30-12:00 共 150 根 + 13:00-16:00 共 180 根」的（区间, 根数）自相矛盾，且与美股采用了不同端点口径

- **文件与行号**：`:60`（§2.2 港股全日理论 330 根）；`:222`（gantt「港股时段 (09:30-16:00) 与分时 330 根」）；对照 `:64`（美股 391 根）
- **触发条件**：按该根数对港股分时做长度校验/渲染完整性判断。
- **实际行为**：
  1. **午后无 13:00 bar**：「13:00-16:00 共 180 根」若首根为 13:00 则应为 181 根；实测 `trends2` 与 `kline(klt=1)` 两条链路均为 `12:00 → 13:01`（`has 13:00 bar? false`），故 180 根对应区间是 **13:01-16:00**，表格标注区间与根数互斥。
  2. **端点口径在港股与美股间不一致**：走**分时（`trends2`）**端点时，港股上午含 09:30 与 12:00 两端（09:30–12:00 = **151** 根），全日 = 151 + 180 = **331** 根，与方案 330 不符；走**1 分钟 K 线（`klt=1`）**端点时首根为 09:31（09:31–12:00 = 150 根），全日 = **330** 根。而方案对美股给出的 391 根**恰是 `trends2` 口径**（实测 `105.AAPL` `trends2` total=391；同标的 `klt=1` 为 390）→ 同一份文档对两个市场使用了不同端点口径而未标注。
- **期望行为**：给出根数时明确端点（分时 `trends2` / 1 分钟 K 线 `klt=1`）与端点包含性（首根是否含开盘快照、末根是否含收盘快照），并保证区间与根数自洽。
- **根因**：Round 1 已将该「口径不一致」列为提示项（建议统一），本轮虽统一了数字，但未回真源复算端点（保留了 `13:00` 起始的错误标注，且对港股取了 K 线口径、对美股取了分时口径）。
- **影响范围**：后续实现若按 330 对分时管线做长度/完整性断言，会与真实 331 根差 1（或反之），Round 1 提示的「按错误根数做长度校验」风险仍然存在。
- **复现方法/运行证据**：`p8.mjs` → `around noon: 11:57 11:58 11:59 12:00 13:01 13:02 …`；`has 13:00 bar? false`；`count 09:30..12:00 = 151`；`[US 105.AAPL] total=391`。`p9.mjs`（`klt=1`）→ `[116.00700] 2026-09-16 n=269 am=150 pm=119 first=09:31 … NO13:00 HAS12:00`；`[105.AAPL] total=390`。
- **修复建议**：写为「分时（`trends2`）端点：09:30–12:00 = 151 根、13:01–16:00 = 180 根，全日 331 根；1 分钟 K 线（`klt=1`）端点：09:31–12:00 = 150 根，全日 330 根」，并同步 gantt 与美股 391 的口径说明。
- **修复后验收标准**：文档中每个根数均标注端点与取值条件，且区间与根数自洽（可由一次真源探测复现）；港股/美股使用同一口径或显式说明差异。

## 三、待确认风险与未验证项

1. **待确认风险：恒指期货在东财是否根本不存在可用 secid。** 本次 `m:100–m:124` 全量 `clist` 扫描仅命中现货 `100.HSI|恒生指数` 与 `m:124` 的恒生系列指数族，**未发现恒指期货合约**；但 `m:105/106/108/114/115/122` 等市场在本次扫描中出现分页失败（返回 0 条），故不能断言「全仓不存在」。**怀疑依据**：`100.HSI00Y` 双端点稳定 `data:null`，且 `m:100` 经枚举为指数市场。**建议验证方法**：对上述分页失败市场重跑 `clist` 并以 `f12/f14` 全量落盘后检索 `HSI`，或用东财 `suggest` 接口按「恒指期货」检索。**残余风险**：若确无合约，则 §3.4「东财单源优先」对 `GL_HSI` 结构性不可行，Phase 1 该品种只能依赖新浪报价单源。
2. **待确认风险：`resolveSessionStrategy` 缺 `default` 分支。** `:152-161` 的 `switch (asset)` 无兜底，若 `inferAssetType(code)` 对某些输入返回 `undefined`/`null`，函数隐式返回 `undefined`，调用方（`getVoiceEligibleCodes`、语音调度）解引用即抛错——而该路径会同时影响 A 股既有调度。**怀疑依据**：接口契约 `:131` 的 `getSession` 值域为封闭联合且无 `unknown` 态。**建议验证方法**：明确 `inferAssetType` 的值域并补 `default` 行为（抛错或回落 A 股策略）后，用未知代码做负向用例。**残余风险**：若实现者按文档片段直译，A 股调度可能被未知代码拖垮。
3. **初步验证项（通过）**：Round 1 待确认风险 1（美股盘前/盘后字段语义）在文档层面已消除——本轮 `:63` 已删除 Round 1 的「支持常规时段报价及盘前盘后价格提取」表述，不再构成无据承诺（能力本身仍未实测，Phase 3 排期时需补测）。
4. **未验证项**：本次未跑端到端分时渲染（东财主机高频连接重置，仅能做真源探测 + 窗口算术 + 真实模块直调）；P2-2 的「冬季丢 60 根 bar」为窗口算术 + DST 换算推导，未在标准时期真源复测（当前为夏令时）。

## 四、推荐修复顺序与复审验收标准

**修复顺序**（先修取数契约，再修时序模型，最后收敛引用精度）：

1. **P2-1**：`GL_A50` → `104.CN00Y`（并核合约锚点）；`GL_HSI` 按东财实际能力显式登记（有 secid 则填实测值，无则标注「仅新浪源」并补降级契约）；`m:100` 行重写；`STATUS.md:9` / `INDEX.md:8` 同步。
2. **P2-2**：`:145`/`:178` 恢复 CME 结算窗口的冬夏令时双档并注明由 DST 纯函数判定。
3. **P3-3**：`getPriceLimit` 改造连带 `classifyKlineBar`/`formatCandleColors` 的 `null` 守卫。
4. **P3-2**：`kline.js` 行号修正为 `210, 218, 232`。
5. **P3-1**：冲突断言改到解析层并修正 `PRODUCT_MAP` 名称。
6. **P3-4 / P3-5**：字段数与根数口径回真源复算并标注取值条件/端点（两者同族，可一并处理）。

**复审验收标准**（逐条可判定，复审将独立重跑）：

- 对 Phase 1 **全部品种逐个**执行 `qt/stock/get` + `trends2` 双端点探测，结果与 §3.1.1 注册表逐行一致；文档中不再出现 `m:100` = 新加坡/港期 的表述或 `100.CHA50CFD` / `100.HSI00Y` 作为有效 secid。
- CME 每日结算窗口在冬令时/夏令时两个固定时钟下分别输出 `06:00-07:00` / `05:00-06:00`。
- `getPriceLimit` 与消费方改造后，「非 A 股标的涨跌停分类不生效」有可失败断言覆盖，且 A 股 5/10/20/30% 既有用例全绿。
- §3.1.2 表中每条 `文件:行号` 均可由 `sed -n '<行号>p'` 还原出该行声称的逻辑。
- §3.1.1 的命名空间冲突断言在人为制造冲突时必须失败；被引用标识名与仓库实际导出一致。
- §2.2/§2.3 的字段数与根数均可由一次真源探测复现，且带取值条件与端点标注。
- 既有门禁保持 `npm test` 843/843（本轮已核）。
