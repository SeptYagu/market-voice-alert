# 国际品种接入方案 WorkBuddy Round 5 复查报告（未通过）

## 一、审查基本信息与通过项简述

* 被审 HEAD：`cfd243cd655e6347ef6445c688b9609353176a77`（`main`，工作区干净，与 `origin/main` 一致）
* 基准 SHA：`45593a5522343c1ed56725f841b16760f80217b7`；本轮实际修复增量：`f637dd9..cfd243c`（3 文件 / +529 −23：方案文档 +531 −23、`STATUS.md`、`INDEX.md`）
* 通过项简述：Round 4 P2-2 的**事实层**（`push2his` 族不可达、`push2` 恒 502、仅 `push2delay` 可达）与 Round 4 P2-3 的 **`hf_`/`r_hk` 值域**均经真源/探针复测成立；`server/marketData.js:14-16/18-22/160/194`、`server/klineService.js:91-95/95`、`server/utils.js:76-86` 等引用逐条与源码相符；`m:134`/`m:104` 三端点在 `push2delay` 上全部 `rc=0`；本轮 diff 全为 `.md`，未触碰产品代码与测试（门禁无需重跑）。
* 结论：**未通过** —— **3×P2 + 2×P3**（本轮修复引入 1 项结构性损坏、1 项错层修复、1 项闭环不完整）。

---

## 二、审查发现与缺陷清单

### P2-1 方案文档正文 §1–§3.1.2 被整体重复 4 份，§3.1.2 契约表行被切成 4 段并与重复正文交错

* **文件与行号**：`docs/handoff/2026-09-16-global-market-feasibility-and-architecture-handoff.md`
  * 重复副本：`:1–156`、`:158–312`、`:314–468`、`:470–624`（第 4 份接 `:625` 续行）
  * 同一表行的 4 个片段：`:157`、`:313`、`:469`、`:625`
  * 重复的文档抬头块：`:159–162`、`:315–318`、`:471–474`
* **触发条件**：任何读者/工具按 §3.1.2 读取 `inferAssetType` 契约；或后续轮次按"方案 `:行号`"引用本文件。
* **实际行为**（实测）：
  * `# 国际期货与国际股票接入可行性调研与架构设计方案 (Handoff v3 - 闭环审查修订单)` 出现 **4 次**（`:1`，以及 `:157`/`:313`/`:469` 三行的**行尾拼接**）；
  * `## 1. 背景与目标`、`## 2. 外部数据源可行性与实测核验`、`### 3.1 资产领域模型`、`#### 3.1.1 外盘期货元数据…`、`#### 3.1.2 归一化全链路改造点清单` 各出现 **4 次**（`awk '/^#/{print NR": "$0}'`）；
  * `src/js/parser.js:11-25` 那一行的契约文本被切成 4 段：①→`:157`、②③→`:313`、④→`:469`、⑤⑥→`:625`，中间夹着 3 整份重复正文（约 465 行，占全文 769 行的 **60%**）；
  * `:157`/`:313`/`:469` 三行的表格行**均未闭合**：行尾直接拼接下一份副本的 H1 标题，`|` 分隔的单元格被截断，§3.1.2 表在 Markdown 渲染下无法成表。
* **期望行为**：单份正文；§3.1.2 每行表格行完整闭合；同一命题在全文只出现一处权威表述。
* **根因**：本轮把新增契约文本与 §2.1/§3.1.1/§3.4.2/§5 的改写内容以整段插入方式写入时，把原有正文整体复粘了 3 次，并在 `:157`/`:313`/`:469` 处把同一表行从中间截断（截断点恰落在 ①③④ 正则的 `$` 锚点之前，直接导致 P3-1）。
* **影响范围**：① 交付物（方案）的核心契约表无法就位阅读，§3.1.2 自称"覆盖全仓 9 处"的清单实际散落在 4 处；② 全文并存 4 份 §2 实测事实，后续任一单点修订都会留下 3 份旧值（本仓库历史上 `STATUS.md`/正文数值漂移类缺陷即由这种残留引发）；③ 4 个同名 H1 与 4 套 `§1/§2/§3.1/§3.1.1/§3.1.2` 使锚点引用与本报告之外的"按行号复核"全部歧义；④ 被复制的抬头仍写"Handoff v3 … Round 2 审查缺陷已修正，提交 Round 3 复审"，与本轮 v5 自述互斥。
* **复现方法**：
  ```bash
  awk '/^#/{print NR": "$0}' docs/handoff/2026-09-16-global-market-feasibility-and-architecture-handoff.md   # 4 组
  git show cfd243c:docs/handoff/2026-09-16-global-market-feasibility-and-architecture-handoff.md | sed -n '157p;313p;469p;625p'
  diff <(git show cfd243c:<doc> | sed -n '1,156p') <(git show cfd243c:<doc> | sed -n '158,312p')   # 除首行外完全一致
  ```
* **修复建议**：删除 `:158–:477` 三份重复副本（保留单份正文，`:478` 起的第 4 份为当前有效版本），将 §3.1.2 表中 `src/js/parser.js:11-25` 行恢复为**单行完整文本**；同时校正唯一保留的抬头为 v5/Round 5。
* **修复后验收标准**：`grep -c "^# 国际期货" <doc>` == 1；`grep -c "^#### 3.1.2" <doc>` == 1；`awk '/^#/{print NR}'` 严格单调且无重复；§3.1.2 表中 `parser.js:11-25` 行的 ①~⑥ 在同一行内闭合，且该行以 `|` 结尾。

---

### P2-2 A50 单位约定登记在错误的边界：10× 分歧实测发生在**东财内部端点之间**，方案却登记为"源间(东财:新浪)"并只在"备源切换"时应用

* **文件与行号**：
  * `:102`（§3.1.1 注册表新增列「源间缩放 (东财:新浪)」= `0.1`）
  * `:709`（§3.4.2「东财 `104.CN00Y`（SGX 市场 104）按 0.1 点单位报价…备源切换时必须应用 `×0.1` 缩放归一化（`142710 × 0.1 = 14271.000`）」）
  * `:704`（§3.4.1「实时行情 `qt/stock/get` 与 `trends2` 分时图同源…保证最新价、涨跌幅与分时图末端价格 **100% 吻合**」）
  * `:762`（§5「备源切换锚点缩放断言」）
* **触发条件**：Phase 1 按 §3.4.1 以东方财富 `qt/stock/get` 作为 `GL_A50` **默认（第一源）实时报价**路径，图表取同源东财 `trends2`/`kline`；或按 `:709` 字面**仅在切至新浪备源时**对东财值乘 `0.1`。
* **实际行为**（本轮真源实测，`push2delay.eastmoney.com`，2026-09-16 16:18，`TZ=Asia/Shanghai`）：

  | 来源/端点 | 最新价 | 昨结 |
  |---|---|---|
  | 东财 `104.CN00Y` `qt/stock/get`（`f43`/`f60`） | `143900` | `142710` |
  | 东财 `104.CN00Y` `trends2`（末根 close / `preClose`） | `14390.0` | `14271` |
  | 东财 `104.CN00Y` `kline(klt=1)`（末根 close） | `14390.0` | — |
  | 新浪 `hf_CHA50CFD`（字段 0 / 字段 7） | `14392.800` | `14271.000` |

  比值实测：`qt.f43 / trends2.last = 143900 / 14390.0 = **10.000**`；`qt.f60 / trends2.preClose = 142710 / 14271 = **10.000**`；`trends2.last / sina.f0 = 14390.0 / 14392.8 = **0.99991**`。
  → **10× 分歧发生在东财自己的两个端点之间（`qt` ↔ `trends2`/`kline`），而 `trends2`/`kline` 与新浪完全同级（1:1）**；方案把该系数登记为"东财:新浪"的**源间**属性（且以"东财按 0.1 点为单位报价"表述为**品种/市场级**属性），并把应用点绑定到"备源切换"这一事件。

* **期望行为**：单位按**端点/字段**登记（`qt/stock/get` 的 `f43/f44/f45/f46/f60/f169` 为 0.1 点；`trends2`/`kline` 为 1 点，与新浪同点），在 EM 数据的**消费入口**统一归一到"点"，而不是在换源事件上做一次性重校。
* **影响范围**：
  1. §3.4.1 的"同源共享 `preClose` → 最新价/涨跌幅/分时末端 **100% 吻合**"对 `GL_A50` 被证伪（`143900` vs `14390.0`；`142710` vs `14271`）；
  2. **默认（主源）路径未归一化**：`src/js/parser.js:97-98` 对 EM 报价字段统一 `div100`（A 股口径，实测 `1.600519` 的 `f43=125800 → 1258.00` 正确），同一函数作用于 `104.CN00Y` 会输出 `price=1439.00`、`prevClose=1427.10` —— 即真值的 1/10，且与同源 `trends2` 图表（`14390.0`）**正好相差 10 倍**，语音播报同样 10 倍偏小；
  3. **反向风险**：若实现者按 `:102` 的"东财 104 市场 0.1 点"把 `×0.1` 施于 `trends2`/`kline`（同属"市场 104"），分时/K 线会反向缩小 10 倍；
  4. `:762` 的验收断言只校验"备源切换时的昨结"，上述任一种错位都不会被拦住。
* **根因**：把"报价单位"当作**品种/市场级**常量登记，而实测它是**端点级**（`qt` 与 `trends2`/`kline` 不同）属性；同时把归一化挂到"备源切换"这一事件上，而消费 10× 值的恰恰是不经过该事件的**默认主源路径**。
* **复现方法**（仓库外探针，同时刻四端点取数）：
  ```
  GET https://push2delay.eastmoney.com/api/qt/stock/get?secid=104.CN00Y&fields=f43,f44,f45,f46,f57,f58,f60
  GET https://push2delay.eastmoney.com/api/qt/stock/trends2/get?secid=104.CN00Y&fields1=f1,…,f13&fields2=f51,…,f58&ndays=1
  GET https://push2delay.eastmoney.com/api/qt/stock/kline/get?secid=104.CN00Y&klt=1&lmt=1000&beg=0&end=20500000
  GET https://hq.sinajs.cn/list=hf_CHA50CFD         （必须带 Referer: https://finance.sina.com.cn）
  → f43/trends2末根=10.000、f60/preClose=10.000、trends2末根/sina.f0=0.99991
  ```
  对照（同探针，排除"随便取数"）：`134.HSI_M` 为 1:1（`qt.f43=24701`、`trends2` 末根 `24701`、`preClose=f60=24676`、新浪 `hf_HSI` 昨结 `24676.000`）。
* **修复建议**：① §3.1.1 注册表把 `0.1` 从"源间(东财:新浪)"列改为**按端点的单位列**（`unit: { qt: 0.1, trends2: 1, kline: 1 }`，`GL_A50` 之外全部为 1）；② 归一化落点写到具体代码位置（`src/js/parser.js:93-127 parseEastmoney` 的 `div100` 分流、EM `trends2`/`kline` 解析入口），而非"备源切换"分支；③ §3.4.1 改写为"在点单位归一化后保证吻合"。
* **修复后验收标准**：同一时刻断言 `f43×0.1 == trends2 末根 close == 新浪 field0`（容差 ≤0.2%）且 `f60×0.1 == trends2.preClose == 新浪 field7`（误差 0）；并断言按方案实现后 `GL_A50` 的 `parseEastmoney` 输出 `price/prevClose` 与 `trends2` 末根/昨结一致，而 `134.HSI_M` 走同一路径仍为 1:1。

---

### P2-3 Round 4 P2-2 闭环不完整：`push2delay` 只进了服务端轮转数组，**未落到浏览器可见的东财三端点出网路径**

* **文件与行号**：
  * `:206`（§2.1 要求"**对 `qt/stock/get` 增加 `push2delay` 兜底**"）
  * `:312` / `:624`（§3.1.2 行只列 `server/marketData.js:18-22, 160, 194`）
  * **未纳入清单的真实出网点**：`server/proxyRoutes.js:10`（`/api/eastmoney-kline` → `https://push2his.eastmoney.com`）、`:13`（`/api/eastmoney` → `https://push2.eastmoney.com`）、`server/klineService.js:40`（`buildEastmoneyKlineUrl` 默认 host = `push2his.eastmoney.com`）
* **触发条件**：浏览器侧获取东财行情/分时/1 分 K 的全部路径：
  `src/js/api.js:52`（`/api/eastmoney/qt/stock/get`）、`src/js/api.js:66`（`/api/eastmoney-kline/qt/stock/trends2/get`）、`src/js/kline.js:73`（`/api/eastmoney-kline/qt/stock/kline/get`）→ `server/index.js:376`（`/api/*` 兜底）→ `server/proxyService.js:10 → resolveProxyTarget` → **`proxyRoutes.js` 静态 target**。
* **实际行为**：本轮逐主机实测（2026-09-16，探测三端点 × 五主机）：

  | 主机 | `trends2`(m:134) | `kline`(m:134) | `trends2`(m:104) | `qt`(m:104) | `qt`(1.600519) |
  |---|---|---|---|---|---|
  | `push2his.eastmoney.com` | `fetch failed` | `fetch failed` | `fetch failed` | `fetch failed` | `fetch failed` |
  | `90.push2his.eastmoney.com` | `fetch failed` | `fetch failed` | `fetch failed` | `fetch failed` | `fetch failed` |
  | `1.push2.eastmoney.com` | `fetch failed` | `fetch failed` | `fetch failed` | `fetch failed` | `fetch failed` |
  | `push2.eastmoney.com` | 502 | 502 | 502 | 502 | 502 |
  | `push2delay.eastmoney.com` | **rc=0 n=964** | **rc=0 n=963** | **rc=0 n=1189** | **rc=0** | **rc=0** |

  即 `proxyRoutes.js:10/13` 指向的两个主机在本机**全部不可达**，而方案只要求把 `push2delay` 追加进 `EASTMONEY_TRENDS_HOSTS`（`server/marketData.js:18-22`）与 `klineService.js` 的 hosts（`server/klineService.js:95`）——这两处只服务于服务端 `/api/cache/*` 路径，**不改变浏览器侧 `/api/eastmoney*` 的静态 target**。→ §5:760「Phase 1 全部 10 个品种均能成功通过 `qt` + `trends2` + `kline` 三端点拉取行情与图表」在方案自己登记的受限网络下仍不可满足；§3.4.1「东财第一源」会静默退化为新浪单源，而 `hf_` 只被登记为"全量备源"。
* **期望行为**：方案应显式把"主机可达性兜底"落到**浏览器可见的东财出网路径**上，并在 §3.1.2 清单中给出对应文件与行号。
* **根因**：把"多主机轮转"当成已经覆盖全部东财出网路径，但仓库实际存在两套独立路径（服务端服务函数用 hosts 数组轮转；浏览器请求经 `proxyRoutes` 静态 target 直连），本轮只改了前者——与 Round 4 P2-2 同一验收断言的闭环不完整。
* **复现方法**：`node` 探针逐主机请求上表 5 个端点（脚本见 §P2-2 的复现命令，替换 `secid` 与主机名）；并 `grep -rn "push2his\|push2\.eastmoney" src server | grep -v node_modules` 核对出网点清单（结果只列出 `klineService.js:40/95`、`marketData.js:18-22/199`、`proxyRoutes.js:10/11/13/14`）。
* **修复建议**：① §3.1.2 清单补齐 `server/proxyRoutes.js:10,13` 一行（多目标轮转或改为调用带轮转的服务端函数），并把 `klineService.js:40` 默认 host 纳入改造面；② `qt/stock/get` 的兜底落点写到具体文件（当前 `qt` 全仓无服务端实现，仅 `api.js:52` 走代理，`grep -rn "stock/get" server/` 命中 0）；③ §5 的三端点验收断言补"在 `push2his`/`push2` 不可达时可判别地经可达主机取数"，并附失败主机对照。
* **修复后验收标准**：在 `push2his` 族与 `push2` 均不可达的环境下（可用 `hosts` 屏蔽复现），浏览器侧 `qt`/`trends2`/`kline` 三端点仍能取到 Phase 1 品种数据；§3.1.2 清单中可见 `proxyRoutes.js:10,13` 行号。

---

### P3-1 §3.1.2 契约 ①③④ 丢失 `$` 锚点 → 与方案自身新增断言互斥，且与同 commit 的 `STATUS.md` 记载不一致

* **文件与行号**：`:157`（① 截断于 `^hf_[A-Za-z0-9_]+`）、`:313`（③ 截断于 `^(?:hk|r_hk)\d{5}`）、`:469`（④ 截断于 `^(?:us)?[A-Za-z]+`）；断言块 `:612`；对照 `STATUS.md:10`（同一 commit 记录为 `^hf_[A-Za-z0-9_]+$` / `^(?:hk|r_hk)\d{5}$` / `^(?:us)?[A-Za-z]+$`）
* **触发条件**：按**方案文档** §3.1.2 逐字实现 `inferAssetType`，并运行 §3.1.1 NOTE 内本轮新增的 Phase 1 单测。
* **实际行为**：仓库外探针按文档文本逐字实现（①②③④ 均无 `$` 锚点、⑤ `isFutureCode`、⑥ 兜底）后，方案新增的 10 条断言 **9 通过 1 失败**：`assert.equal(inferAssetType('unknown_foo'), 'stock_cn')` 实得 `'stock_us'`（`unknown_foo` 命中未锚定的 `^(?:us)?[A-Za-z]+`，且 `isFutureCode('unknown_foo') === false` 使"非国内期货"条件成立）。其余 9 例（`SI0`/`GL_SI0`/`hf_SI`/`hf_HSI`/`hf_CL`/`sh600519`/`hk00700`/`r_hk00700`/`usAAPL`）与方案断言一致 —— 即 Round 4 P2-3 的 `hf_`/`r_hk` 值域**内容层已闭环**，失败仅由锚点丢失引起。
* **期望行为**：契约文本与该断言自洽；方案文档与 `STATUS.md` 对同一契约的记载逐字一致。
* **影响范围**：Phase 1 落地时按文档实现则新增单测必红（回退到 `'stock_cn'` 兜底或改锚点二者只能选一）；两文件记载互斥会让实现者无法判定哪一版是契约。
* **复现方法**：`node` 探针按文档文本实现 6 条分支后跑方案 §3.1.1 NOTE 的 10 条断言 → 输出 `failed: ["unknown_foo"]`；对照 `git show cfd243c:STATUS.md | sed -n '10p'` 可见带 `$` 的版本。
* **修复建议**：随 P2-1 一并把该表行恢复为单行完整文本并补回 `$` 锚点（③ 亦建议补 `$`：当前 `hk00700extra` 会被判 `stock_hk`）；`STATUS.md:10` 与正文保持逐字一致。
* **修复后验收标准**：方案文档的 6 条值域分支与 `STATUS.md:10` 逐字相同；按文档逐字实现的探针 10/10 通过。

---

### P3-2 §2.1 新增的主机/根数断言作用域越界且不可复现，并与同文另一处定值互斥

* **文件与行号**：`:206`（"`push2delay.eastmoney.com` 稳定可达且能完整拉取 `m:134`（`trends2` 与 `kline` **938 根**）"）、`:214`（"实测在**各类网络环境下** `push2delay.eastmoney.com` 均能稳定可达"）、`:213`（同文"实测 `qt/stock/get` … `trends2` n=914，`kline(klt=1)` n=913"）
* **触发条件**：按 `:206` 复现根数；或按 `:214` 推断"任意网络环境均可达"。
* **实际行为**：本轮同时刻（16:17–16:18）实测 `134.HSI_M`：`trends2` n=**964**（首根 `2026-09-15 17:00`）、`kline(klt=1)` n=**963**（首根 `17:01`）——既非 938，也与同文 `:213` 的 914/913 互斥（Round 3 记 914/913、Round 4 记 935/938，本轮 964/963，同一端点已有 3 组互斥定值）。且两条端点口径天然不同（`trends2` 含段首点、`klt=1` 自次分钟起，实测恒差 1~3 根），写成"`trends2` 与 `kline` 938 根"在结构上不成立。另 `:214` 的"各类网络环境下均能稳定可达"超出本轮可测范围（仅本机、单一网络出口、单主机实测）。
* **期望行为**：区间/下限 + 采样时刻表述；主机可达性限定实测范围。
* **影响范围**：§2 作为"实测核验"章节的可复现性；后续轮次按 938 校验会得到第三种结论；"各类网络环境"的全称结论无证据支撑（Round 4 P2-2 同类模式，作用对象由主机可达性换为网络环境普遍性）。
* **复现方法**：`node` 探针对 `134.HSI_M` 同时请求 `trends2` 与 `kline(klt=1)`，打印数组长度与首末根（本轮：964 / 963，首根 `2026-09-15 17:00` / `17:01`）。
* **修复建议**：改为"≥900 根（随交易时段累积；本轮 `push2delay` 16:18 实测 `trends2` 964 / `kline` 963）"并删除 `:213` 的第二处定值（保留一处权威表述）；`:214` 限定为"本次受限网络实测（2026-09-16，仅 `push2delay` 可达）"。
* **修复后验收标准**：同文档内同一端点只保留一处带采样时刻/下限的表述；按该表述复探测不产生互斥值；主机可达性表述带有"实测范围/时间"限定语。

---

## 三、待确认风险与未验证项

1. **未验证（环境约束）**：`m:100`–`m:103` 上 8 个 secid（`GL_CL0`/`GC0`/`SI0`/`HG0`/`NG0`/`NQ0`/`ES0`/`YM0`）本轮未逐个重跑三端点，沿用 Round 3/4 结论；本机对 `push2his` 族与 `push2` 的不可达结论**仅代表本机网络**，作者侧网络的可达性需作者侧复测（若作者侧 `push2his`/`push2` 可达，P2-3 的紧迫性下降，但"清单缺出网点落位"仍需修复）。
2. **残余风险（无法证伪）**：东财侧 `push2`/`push2his` 的 502/连接重置是否为临时策略，无法从本机判定；若后续恢复，方案中"仅 `push2delay` 可达"的前提需重新采样，而不是固化常量。
3. **残余风险**：`m:104` 的 0.1 点单位是否为东财长期约定（本轮 2 次采样一致、且与新浪 1:1 交叉验证），但未做跨交易日复测；建议实现阶段对该端点做启动期一致性断言（见 P2-2 验收标准），而非依赖文档常量。

---

## 四、推荐修复顺序与复审验收标准

**修复顺序（先结构、后契约，再可落地性）**

1. **P2-1**（最高优先，结构）：删除 3 份重复副本，恢复单份正文与单行 §3.1.2 契约表 → 否则后续所有 `文件:行号` 引用与文本修订都无稳定标的。
2. **P2-2**（单位契约）：按端点登记 `GL_A50` 单位并在 EM 消费入口归一化，修正 §3.4.1/§3.4.2/§5 的表述与断言。
3. **P2-3**（可落地性）：把 `push2delay` 兜底落到 `server/proxyRoutes.js:10,13`（及 `klineService.js:40`），补 §5 三端点可判别断言。
4. **P3-1**（随 P2-1 一并做）：恢复 `$` 锚点，正文与 `STATUS.md` 逐字一致。
5. **P3-2**（随 P2 改动一并做）：根数改为"下限 + 采样时刻"，删除互斥定值，主机结论加实测范围限定。

**复审验收标准（Round 6 将逐条核验）**

* 结构：`grep -c "^# 国际期货"` == 1、`grep -c "^#### 3.1.2"` == 1、`awk '/^#/{print NR}'` 无重复且单调；§3.1.2 每行以 `|` 闭合且 `parser.js:11-25` 行含完整 ①~⑥。
* A50 单位：同刻四端点断言 `qt.f43×0.1 == trends2 末根 == 新浪 field0`、`qt.f60×0.1 == trends2.preClose == 新浪 field7`；`GL_A50` 经 `parseEastmoney` 输出与 `trends2` 一致，`134.HSI_M` 保持 1:1。
* 主机兜底：在 `push2his`/`push2` 被屏蔽的环境下，浏览器侧 `qt`/`trends2`/`kline` 三端点仍可取数；§3.1.2 清单含 `proxyRoutes.js:10,13`。
* 契约自洽：按方案文档逐字实现的 `inferAssetType` 探针 10/10 通过，且与 `STATUS.md` 逐字一致。
* 数值表述：同一端点全文仅一处带采样时刻/下限的根数表述；无"各类网络环境"类无范围全称断言。
* 附带要求：`STATUS.md`、`docs/handoff/INDEX.md` 的"v5 全量闭环"措辞需与正文实际状态一致（不得在 P2/P3 未闭环时声称"彻底闭环"）。
