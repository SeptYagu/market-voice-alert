# WorkBuddy 独立代码审查 Round 9 Handoff — 集合竞价日K下影线消除 / 分时图纳入 / 语音时段去重（修复轮复查 8）

> **结论**：**未通过（1×P3）**
> 被审 HEAD：`9da75a52e6d1157a25ee1403d18512256e7c42c9`
> 基准提交：`7c955b0cbcdba12adff9b42b5b159fe7c9663823`
> 本轮实际增量（修复提交自身）：`795e984..9da75a5` = 2 文件 / +157 −45（`src/js/kline.js` +16 −19、`tests/callAuction.test.js` +141 −26）
> 任务书范围：`7c955b0..9da75a5` = 18 文件 / +2993 −27
> 审查日期：2026-09-19

---

## 一、审查基本信息与通过项简述（≤10 行）

- 版本核验：分支 `main` 已配置上游 `origin/main`，`git pull --ff-only` 返回 `Already up to date`，工作区干净；实际 HEAD `9da75a5` 与任务给定待审 SHA **完全一致**，`7c955b0` 为其祖先；仓库内无 `docs/review-checklist.md`。
- 门禁复核（仓库外 pristine 导出 `git archive 9da75a5` + `node_modules` 目录联接，关键文件经 `git hash-object` 与 HEAD blob **逐字节一致**）：`node scripts/run-tests.mjs` **914/914 全绿**、`npx eslint`（6 个本轮涉改文件）0 问题。
- R8 三项缺陷**全部真闭环**（10 点定点变异在 914 项门禁下**全部确定性转红**）：P2-1 漂移形态——M1（重加内容代理析取项）908/6、M2（删除时效要求）906/8、M6（漂移拍保留官方 `open`/`min` 锁低）909/5，均命中 `5.1(1i)/(1s)/(1u)/(1v)/(1w)` 等新负例；P3-1 谓词契约——M4（回退手写前缀白名单）913/1 命中 `5.1(4g)`；P3-2 漂移负例——即上述 M1/M2/M6 的判别力来源，且 R8 风险 1 的跨日时间戳校验已落地（M3 删日期校验 913/1 命中新增 `5.1(1x)`）。
- 独立探针（真实 `parseEastmoney`/`parseTencent` 输出 + 真实模块，全部为**仓库外**临时文件 `_audit_r9/probes/`）：18 类代码形态分时资产隔离逐项对照 `ee4c38a`——R8 缺失的 8 类 A 股形态（ETF×3、可转债×2、B 股×2、裸 6 位码）全部恢复追加 09:18 竞价点，`hk00700`/`usAAPL`/`gl_HSI` 与上一轮行为逐项相同（**港美外盘零回归成立**）；语音单控制器可变时钟逐拍实测 `09:19:59 静默 → 09:20:00 播报 → 09:24:59 播报 → 09:25:00 起静默（基线由保证窗口种子写入）→ 09:26 价格变动恢复播报 → 09:26:30 恢复去重`（**需求 3 成立**）；非 A 股日K原地分支 `hk00700` 的 `high/low/open/volume` 吸收与上一轮逐字段一致（**验收 1「非 A 股不失真」成立**）。
- 本轮唯一缺陷为 R8 P2-1 修复包的**补偿半程缺失**（详见 P3-1）：不影响上述任一闭环结论。
- 证据基建说明：探针/变异脚本均不在仓库内（工作目录 `d:/AiPrograms/project1/_audit_r9`，基线由 `git archive 9da75a5 | tar -x` 导出 + `node_modules` 目录联接），不随提交入库；本轮未改动任何产品代码或官方测试。
- 结论：**未通过**，最高严重级别 **P3**（1 项）。

---

## 二、审查发现与缺陷清单

### P3-1 R8 P2-1 修复包只落地「收紧」半程，配套的「官方日K兜底重载」未实现：时效未知（东财兜底）通道上 A 股当日柱全天停留 `preview` 坍缩态，同一报价自带的权威 `open`/`high`/`low`/`volume` 被持续丢弃（相对 `ee4c38a` 的行为回归）

- **严重级别**：P3（范围窄：仅腾讯持续不覆盖、走 `api.js` 逐码东财回落的 A 股代码；且一旦任一带时间戳报价抵达即自动恢复，故较 R8 P2-1 的「虚拟价锁死全天」轻一级）
- **文件与行号**：
  - 判据：`src/js/kline.js:673`（`const hasOfficialTradeEvidence = isPostOpenTime && hasValidOpen;`）与 `:595`（追加分支同式）；`getQuoteBeijingTimeMinutes`（`:456-488`）对 `parseEastmoney`（`src/js/parser.js:299-320`，实测返回对象**无** `updateTime`/`time`/`quoteDate` 键）恒返 `null` ⇒ `isPostOpenTime` 恒假 ⇒ 走 `:688-700` 坍缩分支（四价=现价、`volume/amount=0`、保留 `preview: true`）
  - 可达性：`src/js/api.js:216-222` 腾讯漏码/整批失败时对 A 股逐码回落东财；tick 链路 `app.js:1350-1383` 只调 `applyLiveTickToChartForCode`（就地改末柱），日K 仅在 `openChart`（`app.js:1284`）/切周期（`chartRowController.js:543`）/手动强刷（`app.js:1306-1308` `handleForceReloadChart`）时重载——**无任何周期性官方日K兜底**
  - R8 修复包原文（`docs/handoff/2026-09-19-workbuddy-code-review-round8-handoff.md` §二 P2-1 修复建议 1 与 §四 推荐修复顺序 2）：「随后按 R7 P2-1 修复建议 1 补『`≥09:30` 后按固定周期重取官方日K，用带时间戳的权威柱替换 `preview` 柱』的兜底」「落地时须……登记保守侧副作用与『官方日K兜底重载』方案」——本轮只落地了收紧与注释登记（`kline.js:589-594`/`:668-672`），重载兜底**未实现也未登记方案**
- **触发条件**（全部为代码可见路径）：
  1. A 股 + `period === '1d'` + 目标代码腾讯持续不返回（如退市/停牌/非常规形态代码，或腾讯批次内该码缺失）⇒ `fetchQuotes` 逐码回落 `fetchEastmoney`；
  2. 图表在开盘前/无时间戳报价期间展开 ⇒ 追加分支（`:597-611`）建 `preview` 柱；
  3. 此后每一拍仍为东财报价（无时间戳）⇒ 原地分支 `hasOfficialTradeEvidence` 恒假 ⇒ 坍缩分支持续生效。
- **实际行为与期望行为**（真实 `parseEastmoney` 输出 + 真实 `applyLiveQuoteToKline`，`_audit_r9/probes/probeC.mjs`/`probeF.mjs` 实测；对照组为上一轮 `ee4c38a`）：

  | 时刻 | `9da75a5`（本轮） | `ee4c38a`（上一轮） |
  |---|---|---|
  | 09:26 | `o=20.5 h=20.5 l=20.5 c=20.5 v=0 preview=true` | 同左（一致） |
  | 10:30 | `o=19 h=19 l=19 c=19 v=0 preview=true` | `o=20.5 h=20.5 l=19 c=19 v=60000 preview=false` |
  | 14:55 | `o=21.3 h=21.3 l=21.3 c=21.3 v=0 preview=true` | `o=20.5 h=21.3 l=19 c=21.3 v=900000 preview=false` |

  即该通道上当日柱全天以「四价=最新价、`volume=0`」显示并随每一 tick 抖动，报价自始至终携带的权威 `open=20.50`/`low=19.00`/`volume=900000` 被丢弃（`low===19.0? false`、`open===20.5? false`、`volume` 恒 0）。期望：R8 修复包约定该通道的保守副作用应由「官方日K兜底重载」在盘后/周期内收敛为权威柱；当前无任何机制使其收敛（除非手动强刷/切周期，或上游偶发返回带时间戳报价）。
- **根因**：R8 首选修复 = 「删除内容代理判据（收紧）**＋** 官方日K兜底重载（补偿）」两半；本轮 commit `9da75a5` 只实现了前一半。收紧后 `preview` 语义成为该通道的**稳态**而非暂态，而 `preview` 在渲染层无任何消费方（`grep` 全 `src/js` 仅 `kline.js` 自用），用户看到的就是一根持续抖动的错误日K。
- **影响范围**：仅 A 股 `1d` 当日柱、且仅限腾讯持续不覆盖而走东财兜底的代码；一旦任一带 14 位 `updateTime` 的报价抵达（`_audit_r9/probes/probeF.mjs` 实测：东财 `preview` 坍缩柱 → 腾讯时间戳报价一拍即恢复 `o=20.5 h=20.5 l=19 c=19.5 v=70000 preview=false`），或用户手动强刷/切周期，即恢复正确。非 A 股、分钟/周/月周期、分时图、语音链路均不受影响（本轮探针逐项复核一致）。验收标准 1 的「非 A 股不失真」与「下影线彻底消除」两个字面目标**均未被本缺陷破坏**。
- **复现方法 / 运行证据**：
  1. `_audit_r9/probes/probeC.mjs`（当前 HEAD）与 `probeC_prev.mjs`（`ee4c38a` 对照）输出即上表；
  2. `_audit_r9/probes/probeF.mjs` 证明恢复路径存在（时间戳报价一拍恢复）与持久形态（纯东财序列全天 `preview`，真实 `low`/`volume` 全程未显示）；
  3. 该行为已被新增用例**有意固化**（`tests/callAuction.test.js` `5.1(1s)` 断言 `t.equal(t2_drift[1].low, 19.65, 'preview 模式下 low 坍缩至当前现价 19.65…')`、`5.1(1p)` 断言 `bEndNoTime volume === 0`），即属**登记过的设计取舍**，非无意引入——但 R8 明确要求该取舍须以「官方日K兜底重载」收敛，此半程缺失使保守态成为全天态；
  4. 新增 10 点变异矩阵（M1–M6、V1–V4）在 914 项门禁下全部确定性转红，证明收紧本身判别力充分——缺陷不在收紧，而在补偿缺失。
- **修复建议**：落地 R8 修复建议 1 的后半程——在 `≥09:30` 且当日末柱为 `preview` 且报价时效未知时，按固定周期（或每 N 拍）触发一次官方日K重载（`fetchKline` → 用权威今日柱替换 `preview` 柱；`handleForceReloadChart`（`app.js:1306`）已具备等价能力，可复用其 `force: true` 通道加节流），并在 STATUS/INDEX 登记该取舍；**不得**为此放宽 `:673` 的合取判据（放宽会使 M1/M2/M6 的负例失效、R8 P2-1 复发）。
- **修复后验收标准**：① `probeC.mjs` 同序列在兜底重载落地后，当日柱最终收敛为权威 `open=20.50`/`low=19.00`/`volume=900000` 且 `preview` 清除；② R8 验收全项不回归：`5.1(1a)~5.1(1x)`、`5.1(4a)~5.1(4g)`、`5.1(6a)~6c` 全绿，M1/M2/M3/M4/M5/M6/V1–V4 十点变异**保持确定性转红**；③ `npm test` 全绿、`npx eslint` 0 问题。

---

## 三、待确认风险与未验证项

1. **未验证项（P3-1 的修复前提）**：东财日K端点（`_fetchKlineFromNetwork` 主源，`api.js:614-640`）在交易时段内是否返回「今日半日柱」——本机对 `push2his.eastmoney.com` 的直连取样返回空，无法活体验证；若该端点盘中不含今日柱，则「官方日K兜底重载」需改用其他权威源或在盘后收敛，修复方案须据实调整。
2. **继承的待确认风险（R8 风险 2，本轮未改变其状态）**：上游 `≥09:30` 后是否仍返回盘前 payload、持续多久、其 `price` 是否在虚拟价区间漂移。当前为非交易时段且离线，无法取证；该前提只影响缺陷触发概率，不影响本轮任何结论的方向。
3. **残余边界（理论性，本轮探针证实当前不可达）**：`getQuoteBeijingTimeMinutes` 的 `quote.time` 回退路径（`kline.js` 时分秒分支）无日期可校验；若未来出现「仅带 `HH:mm` 无日期且实际为往日」的报价源，可绕过跨日校验。生产链路中 `chartRowController.js:396-398` 恒注入 `date`，且现役两源（腾讯 14 位/东财无）均不构成该形态。
4. **记簿事项（不计缺陷）**：R8 要求「登记保守侧副作用」——代码注释已如实登记（`kline.js:589-594`/`:668-672`），但 `STATUS.md`/`docs/handoff/INDEX.md` 未同步更新该取舍状态；属流程簿记，按本轮「不报文档措辞/流程」规则不计入缺陷清单。
5. **本轮未覆盖项**：未执行 `npm run e2e`（Playwright）、`npm run build`；未连接活体行情；需求 2 的 253 网格渲染侧仅以 QUnit（`5.1(4a)~4g`）+ V3 变异复核，未做浏览器视觉核对（继承 R8）。
6. **门禁稳定性（继承）**：本轮仓库外共运行 20+ 次门禁（含 10 次变异），0 次出现与集合竞价无关的失败签名，未复现 R5/R6 记录的冷启动偶发失败。

---

## 四、推荐修复顺序与复审验收标准

**推荐修复顺序**：

1. **P3-1**（官方日K兜底重载 + STATUS/INDEX 登记）：独立于日K判据逻辑，不改 `:673` 合取式，无回归风险面；落地前先按 §三 1 确认东财日K盘中是否含今日柱。
2. （无其他待修缺陷）R8 三项已闭环，无需再动 `kline.js` 判据与 `callAuction.test.js` 既有断言。

**复审验收标准**（下一轮需逐条提供可复现证据）：

- P3-1：`probeC.mjs` 同序列（东财兜底、无时间戳、末柱昨日 → 09:26/10:30/14:55 逐拍）在兜底机制生效后当日柱最终收敛为权威 `open=20.50`/`low=19.00`/`volume=900000`、`preview` 清除；给出重载触发点与节流参数的代码位置。
- 防回归红线：`kline.js:673`/`:595` 的 `isPostOpenTime && hasValidOpen` 合取式**不得放宽**；M1（重加内容代理）、M2（删时效要求）、M6（漂移拍保留官方 `open`/`min` 锁低）三点变异必须**保持确定性转红**（R8 P2-1 不得复发）。
- 全量门禁：`npm test` 全绿（当前基线 914 项）、`npx eslint` 0 问题；`5.1(1a)~5.1(1x)`、`5.1(4a)~5.1(4g)`、`5.1(6a)~6c` 全量不回归。
- 记簿：`STATUS.md` 与 `docs/handoff/INDEX.md` 同步登记「时效未知通道保守态 + 兜底重载」取舍的最终状态。

---

**报告文件命名说明**：本轮按任务书模板使用 `{DATE}-workbuddy-code-review-round9-handoff.md`（DATE 取审查执行日 2026-09-19）；另存在一份**不同任务链**的 `2026-09-16-workbuddy-code-review-round9-handoff.md`（global-futures 链），二者按日期前缀区分、互不覆盖。
