# 国际品种接入方案 WorkBuddy 独立审查 round 10 复查报告

> **结论**：**未通过（2×P2 + 2×P3）**
> 被审 HEAD：`be39cd92d9e9199287657902d09d60fe8ccc1e8d`
> 基准提交：`45593a5522343c1ed56725f841b16760f80217b7`
> 本轮实际审查增量：`cc760c4..be39cd9` = 3 文件 / +89 −34（纯文档；主方案 417 行）
> 审查日期：2026-09-16

---

## 一、审查基本信息与通过项简述（≤10 行）

- 版本核验：分支 `main` 已配置上游 `origin/main`、工作区干净且与远端一致；实际 HEAD `be39cd92d9e9199287657902d09d60fe8ccc1e8d` 与任务给定待审 SHA **完全一致**，`45593a5` 为其祖先；`45593a5..be39cd9` 全量 diff（12 文件 / +1982 −2）仅含 `STATUS.md`、`docs/handoff/INDEX.md` 与 10 份 `.md`，**未触碰产品代码与测试**，仓库无 `docs/review-checklist.md`。
- Round 9 三项缺陷主项**闭环**：`:351` 的 `diff ∈ [4.0%,5.0%]` 硬区间已删除，替换为「备源昨结绑定自身 `field7`」断言（`:374-378`，错绑东财 `f60` 时必然转红）；未定义标识 `resolveFallbackQuote` 已由 §3.1.2 行 8（`:195`）定义为 `parseSinaGlobalFuture`；`:59`/`:72`/`:124` 的 HSI 声明已收敛为「日盘结算后（16:30 后）」并与 `:289-297` 采样时段一一对应（本轮 18:38 复测东财 `f60 = 24688 = trends2.preClose`，新浪字段 7 亦为 `24688`）。
- P3-3 主项**实测成立**：港美股（`f107 = 116 / 105 / 106`）东财 `qt` 价格字段统一 ×1000，与同源 `trends2`、腾讯为 1:1（`116.00700` `f43=433400`/`f60=438800` 对 `trends2` 末根 `433.400`/`preClose=438.8`；`105.AAPL` `331340`；`106.BABA` `109340`）。
- 文档交付物结构自检通过：24 处 `#` 标题**唯一无副本**；`\|`=12 全部还原后 §3.1.2 的 17 行列数一致为 4 列、无畸形行；`\d`=8 转义保留（与 Round 9 恢复后一致）；三张表列数 9/8/4。
- 独立执行 5 组真源/仓库探针（10 品种三源同刻比对、东财 `m:105–134` 市场号全量枚举 + 10 个 secid 交叉取数、港美股 `qt/trends2/kline` 量纲复核、A 股 `f107` 对照组、方案契约逐字回放），**3 组命中缺陷**（见第二节）；`qt.f43/qtDivisor` 对同 secid `trends2` 末根比值 10/10 恒为 `1.00000`，`f169 == f43 - f60` 10/10 成立。

---

## 二、审查发现与缺陷清单

### P2-1 港美股市场号枚举漏掉东财第三个美股市场 `m:107`（AMEX/`腾讯 .AM`）→ 该市场 4774 只标的东财侧恒 `rc:100` 不可达；若补号而沿用登记集合则价格 10× 错位

- **严重级别**：P2
- **文件与行号**：
  - 市场号枚举：`docs/handoff/2026-09-16-global-market-feasibility-and-architecture-handoff.md:88`（「东财 `qt/stock/get` 端点对美股（`m:105, 106`）…」）、`:127-135`（新增 §3.1.1.1 港美股量纲表，仅 5 行样例且全部为 `105/106/116`）
  - secid 解析契约：`:191`（§3.1.2 行 5「支持…港股 `hk*`（`116.*`）与美股 `us*`（`105/106.*`）」）
  - 兜底分派契约：`:192`（§3.1.2 行 6 ②「港股（`f107: 116`）与美股（`f107: 105, 106`）回退 **`div1000`**，彻底消除…10 倍错位」）
  - 验收断言：`:381-392`（§5 港美股量纲断言仅用 `f107: 116` 与 `f107: 105`）
- **触发条件**：Phase 3 中任何东财市场号为 `107` 的美股/美股 ETF 标的（用户代码输入即可添加，见 `:348`），在腾讯未返回该 code 时走东财兜底（兜底链路 `src/js/api.js:150 → :167 → :172 → :113-119`）。
- **实际行为与期望行为**：

  **2026-09-16 18:38 真源实测**（`push2delay.eastmoney.com`，同探针同刻）：

  | 请求 | `rc` | `data` |
  |---|---|---|
  | `105.SPY` | `100` | `null` |
  | `106.SPY` | `100` | `null` |
  | `107.SPY` | `0` | `f43=757390, f60=760880, f107=107, f58=标普500ETF-SPDR` |
  | `105.IMO` / `106.IMO` | `100` | `null` |
  | `107.IMO` | `0` | `f43=135350, f107=107, f58=帝国石油` |
  | `105.RLGT` / `106.RLGT` | `100` | `null` |
  | `107.RLGT` | `0` | `f43=9810, f107=107` |

  - **可判别性对照**：`107.SPY` 三端点全部可用（`qt` `rc=0`；`trends2` `n=391`、`preClose=760.88`；`kline(klt=1)` `n=390`）⇒ 失败原因不是端点/网络，而是**市场号缺失**；同探针 `105.TSLA` `rc=0`、`107.TSLA` `rc=100`，双向对照成立。
  - `clist?fs=m:107` → `total=4774`；腾讯对同标的的交易所后缀为 `.AM`（`v_usSPY="…~SPY.AM~757.39~760.88~…"`、`v_usIMO="…~IMO.AM~…"`），与 `usAAPL→.OQ→105`、`usBABA→.N→106` 构成「腾讯后缀 ↔ 东财市场号」的一致对照 ⇒ `107` 是真实且独立的美股市场（AMEX/Arca 组），非笔误。
  - **期望**（按 `:192` 字面）：`hk*`/`us*` 的东财 `qt` 除数由 `f107` 分派后得到真实价格。
  - **实际**：按 `:191` 只能构造 `105/106.*` → 上述标的恒 `rc:100 data:null`（**无报价可兜底**）；若实现者发现并改用 `107.*`，则 `f107=107` 不在 `{0,1}`/`{116}`/`{105,106}` 任一集合，落空后按 `div100` 归一化 → `757390/100 = 7573.90` 对真值 `757.39`，**10× 错位**。`§5` 的港美股断言只覆盖 `f107: 105`（`:388`），两种错法均拦不住（`:392` 的变异只覆盖「改成 100」一种）。
- **根因**：`:88`/`:127-135`/`:191`/`:192` 的港美股市场号枚举由 `105.AAPL`（纳斯达克）与 `106.BABA`（纽交所）**两个样本外推**为「美股 = 105/106」，未枚举美股全市场号，也无「符号 → 市场号」的解析规则（`us*` 只有前缀形态，没有市场归属判据）。与 Round 1 P2-1「单样本外推为全局规则」、Round 8 P2-1「登记粒度 ≠ 实施粒度」同族。
- **影响范围**：Phase 3（美股）在腾讯失败时的东财兜底路径上，`m:107` 全市场（实测 4774 只，含 SPY 等头部 ETF）恒无报价；若按 §3.1.2 只改除数不改市场号，则 `toEastmoneySecId('usSPY')` 不可达。同时 `:88`「美股（`m:105, 106`）」的实测结论对美股全市场不成立。
- **复现方法/运行证据**：`node` 探针（仓库外，未改产品代码）直连 `https://push2delay.eastmoney.com/api/qt/stock/get?secid=<105|106|107>.SPY&fields=f43,f57,f58,f60,f107`、`…/trends2/get`、`…/kline/get`，并配 `https://qt.gtimg.cn/q=usSPY`（后缀 `.AM`）与 `clist?fs=m:107` 计数；同刻 A 股对照组 `1.600519 → f107=1`、`0.000001 → f107=0`（证明 `qt.f107` 即市场号，非随请求回显的伪字段）。
- **修复建议**：
  1. §3.1.1.1 表新增 `107`（AMEX/腾讯 `.AM`）行：`SPY` `f43=757390 → 757.39`（实测除数同为 `÷1000`），并把表头限定语从「美股（`m:105, 106`）」改为「美股（`m:105, 106, 107`）」；
  2. §3.1.2 行 6 ② 的集合改为 `港股 116` / `美股 105, 106, 107`；
  3. §3.1.2 行 5 必须给出 `us*` → 东财市场号的解析规则（建议：按符号查 `usCatalog`；未收录时对 `105/106/107` 依次探测，首个 `rc=0` 者为准），并注明「`105/106` 取 `107` 标的必 `rc:100`」；
  4. §5 增补 `f107: 107` 的量纲断言（`parseEastmoney({ data: { f107: 107, f57: 'SPY', f43: 757390, f60: 760880 } }).price === 757.39`）。
- **修复后验收标准**：`hk*`/`us*` 的市场号在方案中**逐市场显式登记**（含 107）且有真源采样支撑；`§5` 断言覆盖 `f107 ∈ {105,106,107}`；把 `107` 从分派集合移除或把其除数改为 100（变异）时用例**必须确定性转红**。

---

### P2-2 本轮新增的「`ChinaFuturesSessionStrategy` 必须委托 `getFuturesSession(code, …)`」与既有「无 code 入参的单例策略接口」互斥 → §5 等价性门禁不可落码，品种级夜盘精密规则仍存在静默回归通道

- **严重级别**：P2
- **文件与行号**：
  - 新增的委托要求：`:222-225`（§3.2.1 项 2「**必须直接委托**既有 `src/js/futures/session.js` 中的 `getFuturesSession(code, now, tradingDates)` 与 `isFuturesTradingTime(code, now, tradingDates)`」）、`:198`（§3.1.2 行 12「其中国内期货策略直接委托既有 `src/js/futures/session.js`」）
  - 被冲突的既有契约：`:211-218`（`interface MarketSessionStrategy`，全部方法唯一入参为 `now`）、`:239-251`（`resolveSessionStrategy(code)` 返回模块级单例）
  - 新门禁：`:356`（§5「在各时段断言 `ChinaFuturesSessionStrategy` 产生的结果与既有 `getFuturesSession(code, now, tradingDates)` **严格等价**」）
- **触发条件**：按方案落地 `ChinaFuturesSessionStrategy` 并执行 §5 的国内期货会话等价性门禁。
- **实际行为与期望行为**：
  - 期望（按 `:222-225` 字面）：策略内部把 `getFuturesSession` 的现网精密规则原样透出，§5 可据此做等价性断言。
  - 实际：**策略对象不携带品种代码** —— `:211-218` 的接口方法签名只有 `(now)`、`(now, cfg, tradingDates)`；`:239-251` 的 `resolveSessionStrategy` 直接 `return chinaFuturesStrategy;`（按资产类型共用同一对象）；且 `:179-180` 的既有断言以**对象同一性**强制同类型代码共用一个策略（`assert.equal(resolveSessionStrategy('hf_CL'), globalFuturesStrategy)`）。而 `getFuturesSession(instrument, now, tradingDates)` 的结果**逐品种不同**（`src/js/futures/session.js:12-138`）：中金所金融期货 `IF/IH/IC/IM/T/TF/TS/TL` 完全无夜盘、国债日盘收盘 `15:15` 而股指 `15:00`、集合竞价起点 `09:10/09:25`；商品期货日盘 `09:00–10:15 / 10:30–11:30 / 13:30–15:00`；夜盘 `RB0` 至 `23:00`、`AU0` 至次日 `02:30`（既有回归证据 `tests/codeReviewRegressions.test.js:157-171`）。⇒ 「单例 + 无 code 入参」与「必须委托 `getFuturesSession(code, …)`」不可同时成立；`§5:356` 的「严格等价」在无 code 维度的情况下无法构造（也未给出 `sessionStatus → getSession` 的映射关系）。
  - 反证：现网 `src/js/marketSession.js:135` 是在**逐 code 循环内**调用 `getFuturesSession(future, now, tradingDates)`（`future` 为该轮标的），恰说明该维度不可省。
- **根因**：本轮把「委托既有精密规则」这一**品种相关**要求叠加到既有「无 code 入参的单例策略」契约上，只改了正文要求（`:198`/`:222-225`/`:356`），未同步调整接口签名（`:211-218`）与分派器契约（`:239-251`）——错层修复（与 Round 5 P2-2「闭环只落一处落点」、Round 9 待确认风险 2 的原始动因同源）。
- **影响范围**：按字面实现时，策略无法获知品种 → `节前夜无夜盘`、`02:30 凌晨续段收盘`、`国债 15:15 收盘` 等品种级规则在策略路径上静默丢失（即 Round 9 待确认风险 2 想要消除的回归风险**仍未闭环**）；若实现者自行扩参，则 §5 门禁与 §3.1.2 行 12 的调用式（`resolveSessionStrategy(code).isVoiceAllowed(now, cfg, tradingDates)`）双双失配。
- **修复建议**：二选一并写进 §3.2.1/§3.2.2：
  1. 接口方法增参 `code`（`getSession(now, code)`、`getIntradaySessionRanges(now, code)`、`isVoiceAllowed(now, code, cfg, tradingDates)`），并同步 §3.1.2 行 12 的调用式；或
  2. 明确 `resolveSessionStrategy(code)` 返回**按 code 绑定**的策略实例（工厂/闭包），并相应修改 `:179-180` 的对象同一性断言（改为断言 `assetType` 相同，而非对象相同）。
  同时 §5 门禁须给出 code 维度（至少 `RB0`（23:00 收盘）、`AU0`（02:30 收盘）、`T0`（无夜盘、15:15 收盘）、`IF0`（无夜盘）四类）与结果映射关系。
- **修复后验收标准**：按 §5 门禁字面落码后可在上述四类品种上跑通；把 `ChinaFuturesSessionStrategy` 变异为「不委托、按 A 股二元分派」时，节前夜 `21:30`（`RB0`/`AU0` 应开市）与 `02:40`（`AU0` 应闭市）两个时刻的用例**必须确定性转红**。

---

### P3-1 §3.2.1 新增文本引用未定义标识 `isFuturesTradingTime`（全仓不存在，真实导出为 `isFutureTrading`），并扩散至 `STATUS.md:16`

- **严重级别**：P3
- **文件与行号**：`:223`（`…与 `isFuturesTradingTime(code, now, tradingDates)`；`）、`STATUS.md:16`（同句）
- **触发条件**：按 §3.2.1 项 2 字面落地委托，或据 §5:356 编写等价性门禁用例。
- **实际行为与期望行为**：`grep -rn "isFuturesTradingTime" --include=*.js --include=*.md .` 仅命中该文档 `:223` 与 `STATUS.md:16`；`src/js/futures/session.js` 的真实导出为 `isFutureTrading(instrument, now, tradingDates)`（`:145`），另有 `isAnyFutureTrading`（`:157`）、`isFuturesMarketOpenFallback`（`:171`）、`getFuturesSession`（`:12`）——**无 `isFuturesTradingTime`**。原始 `grep` 亦显示 `src/js/marketSession.js:50/116/121` 使用的是 `isFutureTrading`。按字面落码为 `SyntaxError: The requested module … does not provide an export named 'isFuturesTradingTime'`。期望：§5/正文出现的每个标识均可在方案内定位到定义或落点。
- **根因**：本轮为闭环待确认风险 2 而新写的委托句，凭「语义合理」拼出函数名，未回代码求证导出名（与 Round 9 P3-1「引用未定义标识 `resolveFallbackQuote`」、Round 3 P3-1 同族；本轮为同类缺陷的第 3 次复现）。
- **影响范围**：该句是全仓 17 处改造点之外新引入的落点说明，实现者无法定位；`STATUS.md:16` 同步失真。
- **修复建议**：`doc:223` 与 `STATUS.md:16` 统一改为 `isFutureTrading(code, now, tradingDates)`；若确需新名，先在 §3.1.2 登记其落点与签名。
- **修复后验收标准**：`grep -rn "isFuturesTradingTime" .` 零命中；§5 与 §3.2.1 中每个标识均可在方案内定位。

---

### P3-2 §5 新增的 CL/NG 断言含未绑定伪变量 `sinaClPayload`/`primaryCl`，且 `< 0.6pp` 阈值仍是上游决定量（≡ 两源合约基差的日间变动）→ 不可执行且会随换月随机转红

- **严重级别**：P3
- **文件与行号**：`:374-378`（§5 断言）、`:300`（§3.4.2 新增「实测主备切换同一时刻跳变 < 0.6pp」）
- **触发条件**：按 §5 字面落码 Phase 1 的 CL/NG 门禁用例。
- **实际行为与期望行为**：
  - **(a) 伪变量未绑定**：`sinaClPayload`、`primaryCl` 全文仅出现于 `:375-377`，方案未给出 payload 构造与主源解析步骤（`primaryCl` 无任何定义处）→ 按字面不可执行（与 Round 9 P3-1 中被评为缺陷的 `sinaPayload` 同形，本轮以新客体复现）。
  - **(b) 阈值仍由上游决定**：`|ΔchangePercent| ≡ |今日基差 − 昨日基差|`。本轮实测验证该恒等式：东财 `102.CL00Y` `f43/f60 = 103.82/105.83`、新浪 `hf_CL` `field0/field7 = 99.227/100.750` ⇒ 昨结基差 `5.042%`、现基差 `4.790%`、差 `0.252pp`，与同刻实测跳变 `0.236pp` 吻合。而本轮新登记的基差区间为 `4.1%~5.1%`（跨度 1.0pp，见 `:63`/`:115`/`:300`），Round 8/9 记录的同一日基差移动已达 `0.31pp`（CL `4.97%~5.1%` → `4.79%`）与 `0.34pp`（NG `4.5%` → `4.1558%`）⇒ 基差单日移动超过 `0.6pp` 时门禁必然转红，而该量不由本方案任何一行代码控制（与 Round 9 P2-1 的失效率同为「把上游漂移量写成实现门禁」）。
  - **同刻实测序列（本机 `push2delay` + `hq.sinajs.cn`）**：`18:26 → 0.41pp`（Round 9 记录）→ `18:38 → 0.236pp` → `18:41~18:43` 十次连采 `0.306 / 0.337 / 0.337 / 0.352 / 0.352 / 0.331 / 0.331 / 0.315 / 0.315 / 0.306 pp`（NG 同刻恒 `0.012pp`）⇒ 16 分钟内抖动幅度 `0.17pp`，距 `0.6pp` 仅余 ~`0.24pp` 余量，无采样依据与漂移余量说明。
- **根因**：本轮为消除 Round 9 P2-1 的硬区间，改写了断言方向（正确），但 (a) 未补 payload 绑定、(b) 把阈值整体从「绝对价差区间」平移到「涨跌幅差阈值」，未识别该差值同样 ≡ 上游基差的日间变动。
- **影响范围**：CL/NG 门禁不可执行（(a)）且会随换月/基差漂移随机转红（(b)），实现者仍只能改断言或造数；`§5` 与 `:300` 的 `< 0.6pp` 措辞相互绑定，改错一处即失配。
- **修复建议**：① 补齐绑定：`const sinaClPayload = <hf_CL 原始响应体>;`、`const primaryCl = parseEastmoney(<102.CL00Y 的 qt 响应>);`，并对同一 `now` 取值；② 把 `0.6` 明确标注为「上游不可控量的健壮性上限（≡ 两源合约基差日间变动，随换月漂移，不作为实现正确性判据）」，或改为「切换前后各自昨结一致 + 差值不超过实测上限 + 1 倍余量（并记录采样时刻）」；③ 保留「错绑东财 `f60` 必转红」的变异验证（该条已成立）。
- **修复后验收标准**：该条断言按字面可执行；连续两个交易日各采一次，门禁结果一致；把备源昨结错绑为主源 `f60`（变异）时用例确定性转红。

---

## 三、待确认风险与未验证项

### 待确认风险

1. **§3.1 新增的「`hf_*` 备源符号严格内部化」准入契约未登记落点（`hf_*` 入池防线的执行位置缺失）**：`:106-108` 要求「`normalizeCode` 与搜索联想严禁将其输出为可自选代码」，但 §3.1.2 的 17 行清单只纳管了 `normalizeCode` 行（`:190`），**搜索联想/自选池准入过滤未进清单**，执行位置须实现者自定。怀疑依据：Round 9 待确认风险 1 的验证方法即要求核对「搜索联想/`normalizeCode` 输出对象的准入过滤是否排除 `hf_`」；当前现码 `normalizeCode('hf_CL') === null`（本轮实测），故无即时回归，但 Phase 1 新增联想词库若直接产出 `hf_*` 将无人拦截（同品种双报价、`hf_*` 无东财 secid → 无图表）。**验证方法**：Phase 1 落地后断言搜索联想输出集合 ∩ `^hf_` = ∅，且 `normalizeCode` 对 `hf_*` 恒返回 `null`。

### 未验证项（含残余风险）

1. **§2.1 `:32` 本轮新增括注的量化值不可复现**（该行文本本轮被改写，故按新增文本记录）：「新建 TLS 连接建立约 200~600ms，连接复用后 `< 100ms`」。本机对 `hq.sinajs.cn/list=hf_CL` 连续 8 次请求（Keep-Alive 复用）实测 `923, 631, 209, 211, 209, 207, 211, 208 ms` —— 稳态 `207~211ms`（min `207ms`），**「复用后 `< 100ms` 不成立**。该量与本出口网络 RTT 强相关，不作为缺陷定级，但方案不应把它写成无范围限定的「实测」定值（建议改为「同出口实测量级」或删除量化值）。需在与测量者相同出口复测方可定论。
2. **Round 9 P2-1 的验收标准「连续两个交易日各采一次，门禁结果一致」本轮无法验证**（本次审查仅覆盖 2026-09-16 单日）；其实质风险已并入 P3-2。
3. **受限网络「先失败 → 后轮转 `push2delay`」的转移时序仍不可复现**（承接 Round 6/8/9）：本机 `push2his.eastmoney.com` 可达、`push2.eastmoney.com` 恒 502，与方案 §2.1 `:51` 自述的出口画像不同；`§5:406-407` 的轮转兜底断言无法在此环境落地。残余风险：轮转实现后的切换时延与 `server/proxyService.js:35` 的 `timeoutMs: 15_000`、前端刷新预算的叠加约束方案仍未给出。
4. **Phase 2/3 根数与字段口径未重采**：本轮仅旁证「港股 `trends2` 331 根」「美股 `trends2` 391 根」成立（`n=331` / `n=391`），`klt=1` 的 330/390、腾讯 `r_hk` 78 字段、美股「71 + 偶发 73」未重采（沿用 Round 2/3 结论）。

### 附带非阻塞（未按缺陷定级）

- `docs/handoff/INDEX.md:7`（「前序方案（v9…）」行）链接后多出一段重复文本 `(2026-09-16-global-market-feasibility-and-architecture-handoff.md)`，渲染为可见的冗余片段——属纯排版文书细节，不计入 P0~P3。

---

## 四、推荐修复顺序与复审验收标准

**推荐修复顺序**：P2-1（港美股市场号，直接决定 Phase 2/3 取数契约与 §5 断言可否满足）→ P2-2（策略接口契约，决定「待确认风险 2」是否真闭环）→ P3-1（一改即净，注意同步 `STATUS.md:16`）→ P3-2（补 payload 绑定 + 收敛阈值口径）→ 待确认风险 1。

**复审验收标准**：

1. `hk*`/`us*` 的市场号在 §3.1.1.1 / §3.1.2 中**逐市场显式登记且含 `107`**，并有真源采样支撑（含 `105/106` 取 `107` 标的必 `rc:100` 的可判别性对照）；§5 港美股量纲断言覆盖 `f107 ∈ {105,106,107}`，把 107 移出集合或改其除数时用例必须转红。
2. `ChinaFuturesSessionStrategy` 的委托要求与策略接口/分派器契约**自洽**（接口带 code，或 `resolveSessionStrategy` 改为按 code 绑定并在 `:179-180` 断言改口径）；§5 门禁可在 `RB0`/`AU0`/`T0`/`IF0` 四类上跑通，且「不委托」变异时确定性转红。
3. `isFuturesTradingTime` 在正文与 `STATUS.md` 中全部改为 `isFutureTrading`，全仓 `grep` 零命中；§5 中出现的每个标识均可在方案内定位到定义/落点。
4. §5 的 CL/NG 断言 payload 全部绑定、按字面可执行；`< 0.6pp` 已标注为上游不可控量的健壮性上限（或改为带采样依据的形式），且「错绑 `f60` 必转红」保持成立。
5. Round 9 的四项缺陷与待确认风险 1 的闭环结论**保持**（本轮已独立复测成立，不得回退）；本文 2×P2 + 2×P3 与待确认风险 1 **100% 修复/定论闭环**；未验证项保持明确列明。

**审查通过条件**：上述全部达成，且不得引入新的结构性/契约性缺陷，方可判定通过。
