# A–E 剩余工作完成记录

目标来源：[最新复审剩余项](2026-09-09-followup-review-and-remaining-stages.md)。从 `abecf9a` 开始逐阶段实施，以下只记录已经验证的里程碑。

## 最终验收

文档中的工程实施项 A–E、T1/T2 已完成。没有未实施的阶段；联网真实行情 smoke 和真实设备长时间实播不作为默认门禁，本轮未执行，验证范围见下表和测试说明。

| 原始要求 | 实现与验证证据 |
| --- | --- |
| R1 去重/手动播报/字段开关 | voiceController 实际回写、price/percent 分别变化、多轮静默、手动播报记忆、退订清理、字段重新启用测试 |
| R2 午休/混合夜盘/国债/手动偏好 | voiceSchedule + voiceController；固定时钟单测和真实 app 浏览器检查器验证 |
| R3 分组/排序/置顶/图表保留 | limitUpLifecycle 单测及 limit-up-lifecycle E2E；炸板/回封/连板跨组、删除、批量清理 |
| R4 强刷与并发 | 普通/SWR 晚响应不能覆盖 force 缓存；实际按钮 E2E 观察绕缓存请求 |
| R5/R6 旧任务隔离 | 生产控制器 stop/start 成功/失败/abort；真实图表周期/日期/销毁；涨停日历和列表旧响应隔离 |
| R7 合约/日历/收盘边界 | 共享 session；23:00/01:00/02:30、国债/股指收盘点前后一分钟、前后端等价、节前周六凌晨对照 |
| B 请求归属 | 前端 requestScope；后端 jobId + 按键串行提交，十分钟替换和写盘中替换测试 |
| C/E 会话与控制器 | 纯决策、voiceController/monitorController、依赖注入；app 保留路由/视图及兼容导出 |
| D 增量与资源 | 按 code/group 保留节点；删除才销毁图表；批量关闭一次更新；停止后无新播报/预加载/日历启动 |
| T1 离线可靠性 | 临时缓存、固定基准时钟、未提供 fixture 的 fetch 直接失败；录制响应调用生产 parser/service |
| T2 启动与门禁 | Playwright 直接 Node/Vite 启动，不修改 PowerShell 策略；最终 709 单测、59 E2E、lint/build |

提交里程碑：`44da944` 会话与语音；`8091e8a` 请求归属；`e0bd8f0` 涨停增量；`24e88ad` 监控控制器；最后提交包含 T1、浏览器语音验收和资源/边界审计。

## C 会话决策与 E 语音控制器

- 已新增纯 `voiceSchedule` 决策及 `voiceController`；播报记忆、Worker/降级定时器、调度检查器由控制器私有持有。clock/speech/storage/timers 通过依赖注入。
- 股票午休只暂停，股票单独订阅保持收盘自动关闭；含期货订阅保持用户开启偏好，日盘至夜盘自动恢复。凌晨最后合约收盘只提醒一次。当天手动关闭持久化 `manualDisabledDate`，不会在当前集合竞价内被自动开启覆盖；下一交易日显式配置的集合竞价自动开启仍有效。
- 手动测试播报及自动播报共用增量记忆；字段重新启用清空记忆；取消订阅清理记忆。停止后迟到的日历预热不会重启定时器。
- 图表初始交易日、实时分时更新传入实际合约与交易日历。旧布尔 `isLiveTradeDate` 参数保留兼容，但生产调用方使用代码。
- 验证：完整 CI（lint、694 单测、57 E2E、build）通过；之后新增实际合约/日历断言，voiceController 定向 6/6 通过。

## T1 与最终资源审计

- `scripts/run-tests.mjs` 每次创建独立临时缓存，校验路径后清理；默认 fetch 拒绝并记录未提供 fixture 的请求，不能吞异常后仍算通过。测试时间从固定基准推进，避免实际日期改变会话断言。直接 QUnit 也隔离缓存。
- 原复制正则的期货测试改为录制 fixture → 生产 fallback/service；固定 now 覆盖缓存分时均价、周/月 K。额外透传 now 至实时合成依赖。显式 integration 保留真实时钟/网络，输出来源、stale、目标日期及数据区间，详见 [测试说明](../testing.md)。
- 补真实 app 夜盘/手动关闭/stop 后静默 E2E；补涨停停止后日历/列表回调失效测试。
- 收盘边界统一为左闭右开，避免凌晨及日盘收盘多运行一分钟。Worker 启动失败先销毁再降级，stop 清理语音队列；监控预加载有独立 scope 和可取消 timer。
- 语音检查器仅在自动启停改变开关时重绘设置栏，普通检查不会打断正在编辑的间隔输入。
- 退订导致无可播标的不再被误报为收盘。最终完整 CI 为 709/709 单测、59/59 E2E、lint/build 全通过；随后补退订提醒断言并再次运行全部单测、lint/build，仍通过。真实浏览器语音场景在注入 fixture 前等待启动请求完成，避免日历预热覆盖测试场景。

## B 请求归属

- `requestScope` 的 begin/isCurrent/cancel 已接入图表 K 线/分时独立请求、动量扫描/轮询、涨停列表；涨停保留日期与 requestSeq 隔离，stop 使附属 enrichment 失效。
- 图表 destroyAll 同时清理尚未挂载和仅分时实例；AbortSignal 已取消时也不能提交。
- 服务端 `jobRegistry` 使用独立 jobId、替换前失效与按任务键串行提交。初始进度、批量进度、最终结果、success 缓存和错误结果均通过 commit 检查；已经进入写盘的旧写入先完成，新任务写入随后执行。
- 固定时钟模拟十分钟替换，覆盖旧成功/进度/失败/清理与写盘中替换；真实图表覆盖周期/日期切换和销毁后迟到响应。完整 CI：702 单测、57 E2E、lint/build 通过。

## D 涨停增量视图

- 视图维护 code→row/chartRow、group→section 索引；共享 view groups 处理置顶和普通分组。报价修改单元格，结构变化移动现有节点，新增/删除才创建/移除。
- 控制器不再在重新渲染时销毁全部图表；删除标的清理 selection/expanded/instances，批量关闭一次销毁循环后一次更新视图。
- 真实控制器单测覆盖组内反转、炸板/回封/连板分桶、置顶、删除及关闭；真实浏览器验证跨组移动保留 DOM 和图表实例、重新加载发起实际绕缓存请求。
- 验证：703 单测、lint/build 通过；既有 57 E2E 通过。新增 E2E 首次因测试代码使用不存在的 fixture 代码失败，改为实际 fixture 后定向 1/1 通过；最终阶段将再次运行全部 58 项。

## E 监控控制器

- `monitorController` 私有持有请求 scope、刷新定时器、日历检查器生命周期；管理列表增删、监控/订阅/涨停刷新代码集合、报价提交与刷新状态。storage/fetch/clock/timers/回调注入，app 保留 DOM 事件和跨页面协调。
- 删除后迟到行情不能重新插入已删除报价；stop 后旧请求、日历预热不得重新提交或启动定时器。手动删除的批量资源清理由 app 注入一次执行。
- app 的独立分时后台回填也统一委托 ChartRowManager，消除旁路响应覆盖新图表的风险。
- 验证：完整 CI 704 单测、58 E2E、lint/build 通过；后台回填迁移后再跑 704 单测及 12 项图表 E2E、lint/build 通过。
