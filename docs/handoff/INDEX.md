# 交接文档索引 (Handoff Index)

本文档归档并索引项目演进过程中产生的所有阶段交接（Handoff）、代码审查与缺陷验证报告。

> **当前最新状态速读**：
> - 状态综述：查阅项目根目录 [`STATUS.md`](../../STATUS.md)
> - **最新审查（集合竞价方案设计 Round 3 终审复查）**：[`2026-09-18-workbuddy-code-review-round3-handoff.md`](2026-09-18-workbuddy-code-review-round3-handoff.md)（独立复查 `f4b7b92`（基准 `ad0c609`，实际审查增量 `ad0c609..f4b7b92` = 5 文件 / +662 −2，纯文档；本轮增量自身 3 文件 / +157 −171）：**未通过**，**1×P1 + 3×P2 + 1×P3**。① **P1-1** §3.2.1 的 `opts.isFuture === undefined && hasNonStockHours` 使启发式兜底在唯一调用点（`chartRowController.js:288` 传 `isFuture: isFutureCode(code)`，恒为布尔）成为死代码 → 港股/美股/国际期货落回 A 股 253 网格，真实 `chart.js` 定向变异实跑 港股 6/6→253/3、美股 5/5→253/0、`gl_HSI` 4/4→253/0（「有轴无点」），与 §3.1.1「非 A 股零跨品种误伤」冲突；② **P2-1** §3.3.1 的 `opening-auction` 分支漏掉 `if (!cfg.enabled) return true`（兄弟策略 `:154/:193/:229` 与 `:49` 均有）→ 「智能交易时段」关闭（UI 可达且 `autoStartAuction` 被置灰冻结）时 09:15-09:20 由放行变拦截（实跑 09:17 HEAD=true / v3=false）；③ **P2-2** §3.1.2 的 09:25 前预览柱规则未落到追加分支（§3.1.3(1) 限定「≥09:25」、§四 只写「不执行 `Math.min`」）→ `items` 末柱为昨日时 09:18 虚假低点被写死不可回弹（实跑末柱 `low=18`，§5.1(1) 期望 20），且用例未规定前置状态；④ **P2-3** 验证矩阵对分时轴零判别力（M4 变体一实测与期望一致不转红、变体二因短路零效果；仍无港股/美股/国际期货轴用例）；⑤ **P3-1** §四 清单未登记必需 import（`kline.js` 缺 `chartSecondsToTime`、`voiceController.js` 缺 `getBeijingClockParts`），§3.3.1 片段误用不存在的 `this.getMarketSession`（实跑 TypeError）。**通过项（真闭环核实）**：R2-P1-1 真闭环（实测 09:20-09:29 判据恒真，注入式时钟下记忆在 09:26/09:31 未被 `memory.clear()` 清空）；R2-P2-1 真闭环（语音专属方法，数据刷新门禁零改动）；R2-P2-2 基本闭环；R2-P3-1 闭环；R2-P2-4 部分闭环（M5 已具杀红力、253 断言自洽）。**待确认风险**：新网格无 09:26-09:29 槽位而数据侧区间仍放行该窗口点；上游竞价分时可得性；TTS 队列按 code 合并；`inferAssetType` 不可识别字符串残留 fail-open；真机 Worker/`checker` 切点顺序）
> - **方案终审定稿（集合竞价日K下影线消除、分时图集合竞价纳入与语音播报时段去重优化方案 v3 定稿版）**：[`2026-09-18-call-auction-kline-intraday-voice-handoff.md`](2026-09-18-call-auction-kline-intraday-voice-handoff.md)（提交方声明彻底闭环 Round 2 审查 1×P1 + 4×P2 + 1×P3 缺陷：① 语音使能覆盖 09:20-09:30 保持定时器持续运行，保护记忆基线；② 落地于语音专属 chinaStockStrategy.isVoiceAllowed，零副作用；③ applyLiveQuoteToKline 显式传参 code，水密隔离 STOCK_CN，非 A 股 fail-closed 绝对放行；④ chartRowController 显式注入 isFuture，收窄 hasNonStockHours，锁定 253 标准网格槽位；⑤ 09:26-09:29 静默期保护 09:25 撮合点；⑥ 补齐 memory 实际值断言与 M1~M6 确定性杀红变异。经 Round 3 终审复查**未通过**，遗留 1×P1 + 3×P2 + 1×P3：R2-P2-3 未闭环且新引入港股/美股/国际期货分时轴回归，另含智能时段关闭态语音回归、09:25 前追加分支守卫缺失、M4 无判别力与 §四 import 缺口）
> - **前序交付（集合竞价方案设计 v2 闭环版）**：[`2026-09-18-call-auction-kline-intraday-voice-handoff.md`](2026-09-18-call-auction-kline-intraday-voice-handoff.md)（全面闭环 Round 1 审查 2×P1 + 4×P2 缺陷：① 解除 09:20-09:25 语音使能门禁；② 日K盘前守卫严格限定 STOCK_CN 并放行外盘真实交易；③ 重构分时图 hasNonStockHours 确保固定网格生效；④ 全量播报分支生成有效 spoken 闭环 09:25 重复播报；⑤ 追加分支全面隔离外部脏 quoteLow；⑥ 补齐五大判别用例与 M1~M5 确定性变异。经 Round 2 复查**未通过**，遗留 1×P1 + 4×P2 + 1×P3）
> - **前序审查（集合竞价方案设计 Round 1 复查）**：[`2026-09-18-workbuddy-code-review-round1-handoff.md`](2026-09-18-workbuddy-code-review-round1-handoff.md)（独立复查 `9687347`（基准 `ad0c609`，增量 3 文件 / +226 −1，纯文档）：**未通过**，**2×P1 + 4×P2**。① **P1-1** 语音方案遗漏时段使能门禁——09:15-09:29 为 `opening-auction`，默认 `autoStartAuction=false` 下 `eligibleCodes=[]`、`timerShouldRun=false`（实跑），仅改 `speakCodes` 的 `dedupe` 无法实现「09:20-09:25 强制按间隔播报」；② **P1-2** 日K盘前守卫无品种区分机制——`applyLiveQuoteToKline(item,quote,period)` 无资产入参且被港股/美股/国际期货日K共用，字面实现会把美股夜盘/CME/A50/HSI 真实交易误判为盘前竞价（实跑 98–103 → 100.5/100.5）；③ **P2-1** `chart.js:732-741` 的 `hasNonStockHours` 旁路固定网格，§3.2.1 对 `:744` 的改动成为死代码（实跑 displayTimes 242→3）；④ **P2-2** `formatQuoteSpeech` 返回字符串致 `spoken:null`，§3.3.2 记忆基线衔接不可实现（实跑 memory.size=0、09:31 重复播报）；⑤ **P2-3** `kline.js:472-489` 追加分支仍消费外部脏 `quote.low`；⑥ **P2-4** 验证矩阵判别力/变异杀伤力缺口。**通过项**：引用行号与 HEAD 逐行一致、Bug 1/Bug 2 根因实跑成立。另记：任务书待审全 SHA `9687347a83…` 仓库中不存在，以短 SHA + 提交信息 + 父提交=基准锁定实际 HEAD）
> - **阶段交付（Phase 1 国际期货与外盘基础落地完成）**：[`2026-09-17-global-futures-phase1-completion-handoff.md`](2026-09-17-global-futures-phase1-completion-handoff.md)（交付提交 `a09115b`，基准 `adf057f`；10 个国际期货品种全链路接入、跨市场会话策略与持久化归档完整落地；**WorkBuddy 双智能体对抗审查 Round 5 审核通过，0 缺陷达成**；全仓 `npm test` 877/877 全绿，`npm run lint` 0 错误，`npm run build` 成功）
> - 最新审查（代码落地 Round 5）：**审查通过（0 缺陷）**，被审提交 `a09115b`（基准 `adf057f`，审查增量 `853bb15..a09115b` = 3 文件 / +131 −13）；夏令时 DST 来源改由归档会话时间戳自身推导，4 条夏令时切换周临界测试与品种级变异测试全绿，完整闭环 Round 4 P3-1 缺陷，无新缺陷。
> - 前序审查（代码落地 Round 4）：[`2026-09-17-global-futures-phase1-workbuddy-code-review-round4-handoff.md`](2026-09-17-global-futures-phase1-workbuddy-code-review-round4-handoff.md)（独立复查 `6241d9c`，基准 `adf057f`，本轮实际审查增量 `7f33014..6241d9c` = 3 文件 / +158 −18，Phase 1 国际期货与外盘基础落地代码 Round 3 修复：**未通过**，**1×P3**（本轮新增首根校验引入的回归）。**Round 3 两项缺陷（P2-1/P3-1）单元层已实测真闭环**——品种级 `sessionStart/EndBeijingMin`/`minBars` 登记（CME 族 DST 420/360–300/360、A50 固定 540/315/800、HSI 固定 555/180/500）+ 首末根双侧校验；变异 M1（HSI endMin 还原美盘统一值）与 M2b（拆首根校验 + A50 minBars→500）确定性转红；端到端实测 `GL_A50 date=2026-09-16` 缺头归档 `archiveComplete=false`（Round 3 误标 true），修复生效；`npm test` 873/873 全绿（+4）。**P3-1（新回归）完整性判据的 DST 取自 `generatedAt` 而非归档会话自身**（`server/intradayService.js:156`）——美国夏令时切换周末之前的 CME 型交易日（8 品种，每年 2 天），其完整归档在切换后首次抓取时被永久误判不完整（春令时方向：冬令时会话首根 07:00=420 > startMin 360+15；秋令时方向为旧末根判据既有同根因缺陷）；纯单元 4 案例决定性反例：同一份完整归档 genAt 切换前 true、切换后 false。数据可见无静默残缺故 P3，按门槛阻断。**待确认风险/未验证项**：HK/SGX 完整归档捕获窗（北京 03:00–09:15）存在性未证实——现时点实测上游已清洗 HSI 日盘段，`GL_HSI date=2026-09-16` 判不完整属诚实标记（数据确缺日盘半段，非 Round 3 误判形态）；A50 美盘夏令时 `[0,300]` ranges 滤除夜盘 05:00–05:15 真实 bar（既有行为，末根判据 0 裕量通过，仅登记）。**门禁**：`npm test` 873/873 全绿）
> - 前序审查（代码落地 Round 3）：[`2026-09-17-global-futures-phase1-workbuddy-code-review-round3-handoff.md`](2026-09-17-global-futures-phase1-workbuddy-code-review-round3-handoff.md)（独立复查 `5e02539`，基准 `adf057f`，实际审查增量 `9389bcd..5e02539` = 24 文件 / +2501 −196，Phase 1 国际期货与外盘基础落地代码修复：**未通过**，**1×P2 + 1×P3**（均为本轮修复新增代码的缺陷）。**Round 2 全部 8 项缺陷（P2-1/P2-2/P3-1..P3-6）已实测真闭环**——9 组定点反向变异确定性转红（日历日判据回退、服务端并集判据回退、旧启发式还原、撤销 session ranges 接线、双层 `hf_` 过滤同拆、`breakEnd` 固定 360、还原 `known||'105'`、小数位 `{3,4}` 白名单回退、`quoteDate` 还原 10 位）；真源端到端复核 `toEastmoneySecId('usBA')→null`、`GL_CL0 date=2026-09-17` API 与直连 `trends2` **差额 0**（232=232）、`GL_HSI date=2026-09-16` 跨午夜夜盘段（09-17 00:00–03:00 共 181 根）完整保留。① **P2-1（新）本轮新增的 `isHistoricalSnapshotComplete` futures_global 分支（`server/intradayService.js:148/:158`）把美盘 DST 结算时刻（05:00/06:00 前 15 min）套用于全部 10 品种**——HKFE 恒指夜盘真实末根 03:00（180 min < 285/345）**全年**、SGX A50 夜盘末根 05:15（315 < 冬令时 345）**冬令时**下，已完成交易日的完整归档被永久判 `archiveComplete=false`：响应 `stale` 恒真（`:336/:360`）、历史快照信任门 `:313` 永不通过（每次重拉上游）；实测全新缓存根 `GET /api/cache/intraday?code=GL_HSI&date=2026-09-16` 返回 600 根完整归档（first 17:01 / last 03:00）仍标不完整；判据末根门变异为恒真后全量 869/869 仍绿（零品种级覆盖）。② **P3-1（新）同判据只校验末根不校验首根**——上游（push2delay kline `lmt=1000`）仅保留最近交易日窗口（A50 实测自 T 日 16:46 起，缺日盘 09:00–16:35 约 455 根）时，缺头归档被标 `archiveComplete=true` 并经 `:313` 信任门永久缓存（实测 `GL_A50 date=2026-09-16` n=735 first=16:46 标完整），Round 2 P2-1「静默残缺」危害形态的头部残留变体。**待确认风险/未验证项**：CME 型已完成交易日全量条数对齐未能整段复测（北京 06:00 换日后上游仅存当前会话，非本轮回归）；S17 性能预算用例并行负载偶发失败（预存基建敏感性）；`push2his`/`90.push2his` 仍 100% 连接重置、轮转至 `push2delay` 生效（与既有记载一致）。**门禁**：`npm test` 869/869 全绿）
> - 前序审查（代码落地 Round 2）：[`2026-09-16-global-futures-phase1-workbuddy-code-review-round2-handoff.md`](2026-09-16-global-futures-phase1-workbuddy-code-review-round2-handoff.md)（独立复查 `9389bcd`，基准 `adf057f`，实际审查增量 `4373343..9389bcd` = 12 文件 / +455 −178，Phase 1 国际期货与外盘基础落地代码：**未通过**，**2×P2 + 6×P3**。Round 1 的 P1-1/P1-2 **已实测真闭环**——真实服务端 + 真实 `fetchQuotes` 端到端 **9/9** 报价 code/type/price/prevClose/decimals/currency 全对，真实 payload 回放 `parseEastmoney` **15/15 PASS**，变异 M1（`splitCodes` 退化为 2 桶）19/20 转红、M2（去掉 `f107`）19/20 转红；但 P2-1/P2-2/P3-1..5 **均未闭环**。① **P2-1 服务端共享缓存分时仍用日历日切分**——`server/intradayService.js:230` 的 `filterKlineItemsByDate(klineData.items, common.date)` 未随策略化改造，`:159/:181` 又对历史日期提前返回使 `trends2`/腾讯两路根本不参与；**全新缓存根**实跑 `GET /api/cache/intraday?code=GL_CL0&date=2026-09-16` 得 **1079 根** vs 真值 **1381 根**（丢 302 根 = 21.9%），且响应仍标 `archiveComplete: true`（静默残缺，消费方无从察觉；Round 1 报的 257 根系旧缓存残留）。② **P2-2 跨午夜交易日过滤被实现为「旧日历日 ∨ 新 `getTradingDay`」并集**——`src/js/parser.js:535-536`（`itemDate !== selectedDate && chartTimeToDate(time) !== selectedDate`）、`src/js/api.js:350-353`、`server/intradayService.js:54-57` 三处同语义，旧行为未被替换；真实 payload 复现：对 2026-09-16 交易日数据请求 `date='2026-09-17'` 返回 **301 根**（`getTradingDay` 判定其全部属 09-16，正确应为 **0**）。③ **P3-1** `src/js/parser.js:_resolveRowTradingDay` 硬编码 `if (min < 360)` 为结算断档上界，忽略夏令时（应 DST 360 / 非 DST 420），冬令时 05:00–07:00 归属错日。④ **P3-2** `src/js/tts.js`/`alert.js`/`monitorTableView.js`/`batchExportService.js` 小数位消费仅白名单 `{3,4}`，`GL_GC0`/`GL_A50`/`GL_YM0`/`GL_HSI` 精度与注册表不符。⑤ **P3-3** `src/js/parser.js:146` 仍 `known || '105'`，未收录美股符号静默落 105（实测 `usBA → 105.BA → rc:100 data:null`，`106.BA` 才 `f58='波音'`）。⑥ **P3-4** `src/js/parser.js:396` 的 `quoteDate` 8 位契约已改对但无断言守护。⑦ **P3-5** `getFuturesSessionRanges` 对国内期货仍不可达（`fetchIntraday` 首行即转 `fetchFuturesIntraday`），唯一消费方是测试，`t.true(length > 0)` 为空验收。⑧ **P3-6** `tests/globalFutures.test.js` 的 `^hf_` 不相交断言恒真（索引内 `hf_` 条目实测 0），§5 验收矩阵 ③⑤⑦ 缺项。**变异测试盲区（本轮新增证伪手法）**：M3（去掉 `priceDecimals` 生产）、M4（回退跨午夜归属）、M5（`quoteDate` 改 10 位）、M6（撤销服务端策略接线）四项变异后 `tests/globalFutures.test.js` 均 **20/20 全绿** → 新增用例对上述实现零判别力。**待确认风险**：A50 夜盘 05:15 收盘未采样证实、港股/美股腾讯路径报价实测不带 `priceDecimals`、代理轮转未做压力实测、`quoteDate` 跨模块契约未全仓核对。**门禁**：`npm run lint` 0 问题、`npm test` 863/863、`npm run build` 成功）
> - 前序审查（代码落地 Round 1）：[`2026-09-16-global-futures-phase1-workbuddy-code-review-round1-handoff.md`](2026-09-16-global-futures-phase1-workbuddy-code-review-round1-handoff.md)（独立复查 `4373343`，基准 `adf057f`，实际审查范围 `adf057f..4373343` = 18 文件 / +1492 −150，Phase 1 国际期货与外盘基础落地代码：**未通过**，**2×P1 + 3×P2 + 5×P3**。① **P1-1 `splitCodes` 未按 §3.1.2 改造为资产类型分发器** —— `src/js/api.js:103-104` 仍只认 `STOCK_RE`/`FUTURE_RE`，`GL_*`/`hk*`/`us*` 三族既不入 stocks 也不入 futures（实测 `isFutureCode('GL_CL0')===false`、`FUTURE_RE.test('GL_CL0')===false`）→ `fetchQuotes` 的 `tasks` 为空，探针实测 `fetchQuotes(['GL_CL0'])` **网络请求 0 次**、`quotes=[]`、`failedCodes=['GL_CL0']`（`hk00700`/`usAAPL` 同）；本轮只扩展了 `buildTencentUrl` 的入参正则（错层修复），10 个外盘品种的报价/提醒/播报/导出全无数据（K 线不受影响，呈"有图无价"）。② **P1-2 `parseEastmoney` 市场号取自生产从不请求的 `f116`（实为总市值）** —— `src/js/parser.js:229` 为 `f107 ?? f116`，而 `src/js/api.js:34` 的 `EASTMONEY_FIELDS` **不含 `f107`**；真源实测（`push2delay`）`f107` 才是市场号回显（102/101/104/134/116/107/105/1/0），`f116` 是总市值（外盘 8 品种恒 0、`116.00700`=3.94e12）→ 注册表反查与 `marketId===116|105|106|107` 分支**恒不命中**，全部落入 `:256` A 股兜底：身份退化为 `szcl00y`/`szgc00y`/`szhg00y`/`szcn00y`/`szhsi_m`/`sz00700`/`szaapl`/`szspy`（被 `monitorController.js:44` 的 `currentCodes.has(quote.code)` 丢弃），`type='stock'`，`div100` 使黄金 10×／美铜 100×／恒指 100×／A50 10×／港美 10× 错位；按生产字段表喂真实 payload 复现 **8/8 FAIL**。③ **P2-1 服务端共享缓存分时未同步策略化** —— `server/intradayService.js:15-18/29-35/37-47` 仍用 A 股 `SESSION_RANGES`，实跑 `/api/cache/intraday?code=GL_CL0&date=2026-09-16` 仅 **257 根**（同刻 `trends2` 1375 根，丢弃 81%），`date=2026-09-17` 返回 **0 根且 `ok:true`**（A 股 `sh600519` 240 根正常）。④ **P2-2 跨午夜交易日对齐未实现** —— `api.js:283`/`parser.js:489` 仍按北京日历日切分，实测 CL00Y 单窗 1375 根 → `date=09-16` 1080 根、`date=09-17` 295 根；`getTradingDay` 契约全仓无生产消费点。⑤ **P2-3 测试夹具与真实接口契约不符** —— `tests/globalFutures.test.js:112/122/…/178`（`f116: catalog.GL_*.marketId`）与 `:187/:194`（`f116: 116|105`）把市场号注入 `f116`，与实现共享同一错误假设 → 断言对 P1-2 零判别力。⑥ **P3×5**：报价未写入 `priceDecimals`/`currency`（`GL_HG0` 播 6.44 而非 6.4400）；美股 `US_MARKET_MAP` 无探测、`localStorage` 缓存键无写入点（`usBA→105.BA→rc:100`，`106.BA` 才正确）；`parseSinaGlobalFuture` 的 `quoteDate` 为 10 位带横线（`kline.js:461` 长度守卫使追加分支成死代码）；`getFuturesSessionRanges` 对国内期货不可达（`fetchIntraday` 首行转 `fetchFuturesIntraday`），唯一消费方是恒真断言；`tests/globalFutures.test.js:396-407` 的 `hf_` 不相交断言恒真＋§5 七项验收断言缺项。**通过项**：`inferAssetType`/`resolveSessionStrategy` 值域与分派正确、`globalFuturesStrategy` 冬夏窗口与周界 9/9 边界实测正确、`voiceSchedule.js:28` 判据逐字改造且保留 `after-close` 维度、`getPriceLimit→null` 消费方短路已补齐、TTS 货币后缀正确、门禁 lint 0 问题 + 858/858 + gzip 119.53 KB）
> - 历史审查：[`2026-09-16-workbuddy-code-review-round1-handoff.md`](2026-09-16-workbuddy-code-review-round1-handoff.md)（同日的方案文档 Round 1，被审 `a2b8791`，与本轮代码落地审查不同任务）
> - 方案定稿交付：[`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md)（国际期货与国际股票接入方案 v12：闭环 5 大工程落地盲点——美股带点/横线特殊代码正则支持、东财市场探测双层缓存保护、国内期货分时区间导出补齐、出网代理快速超时与主机熔断、CL/NG 换月基差自适应解耦；设计方案完全定稿，交付就绪直接实施，0 生产代码改动）
> - 前序方案（v11 终审闭环版）：[`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md)（国际期货与国际股票接入方案 v11：全面闭环 Round 10 审查 2×P2 + 2×P3 缺陷与待确认风险，多智能体设计审查正式收敛定稿，交付就绪实施）
> - 前序审查：[`2026-09-16-workbuddy-code-review-round10-handoff.md`](2026-09-16-workbuddy-code-review-round10-handoff.md)（独立复查 `be39cd9`，基准 `45593a5`，本轮实际审查增量 `cc760c4..be39cd9` = 3 文件 / +89 −34（纯文档；主方案 417 行），国际品种接入方案 round 10：**未通过**，**2×P2 + 2×P3**。① **P2-1 港美股市场号枚举漏掉东财第三个美股市场 `m:107`（AMEX / 腾讯后缀 `.AM`）**——`:88`（「美股（`m:105, 106`）」）、`:127-135`（§3.1.1.1 量纲表）、`:191`（`us*` → `105/106.*`）、`:192`（`f107: 105, 106` 回退 `div1000`）系按 `105.AAPL`（纳斯达克）/`106.BABA`（纽交所）两个样本外推；本轮 18:38 真源实测 `105.SPY`/`106.SPY`/`105.IMO`/`106.IMO`/`105.RLGT`/`106.RLGT` **全部 `rc:100 data:null`**，而 `107.SPY` `rc=0 f43=757390 f107=107`、`107.IMO`/`107.RLGT` 均 `rc=0`，`107.SPY` 三端点全可用（`trends2` n=391、`kline` n=390），`clist fs=m:107 → total=4774`，腾讯同标的为 `v_usSPY="…SPY.AM~757.39~760.88…"` ⇒ 属**市场号缺失**：该市场标的在腾讯失败走东财兜底时恒无报价；若补号而沿用登记集合则 `f107=107` 落空按 `div100` → `7573.90` 对真值 `757.39` **10× 错位**，§5 港美股断言仅覆盖 `f107: 105`（`:388`）拦不住。② **P2-2 本轮新增的「`ChinaFuturesSessionStrategy` 必须委托 `getFuturesSession(code, now, tradingDates)`」（`:198`/`:222-225`）与「无 code 入参的单例策略接口」互斥**——接口唯参为 `now`（`:211-218`）、`resolveSessionStrategy` 返回模块级单例（`:239-251`）、`:179-180` 以对象同一性强制同类型共用一个对象 ⇒ 策略不携带品种；而 `getFuturesSession` 逐品种不同（`src/js/futures/session.js:12-138`：中金所无夜盘、国债 15:15、`RB0` 至 23:00、`AU0` 至 02:30；`tests/codeReviewRegressions.test.js:157-171`）⇒ `§5:356` 等价性门禁不可落码，待确认风险 2 的品种级夜盘回归通道仍未闭环（反证：现网 `marketSession.js:135` 逐 code 传入品种）。③ **P3-1 `:223` 与 `STATUS.md:16` 引用未定义标识 `isFutureTrading`**（全仓 `grep` 仅命中该两处文档；真实导出为 `isFutureTrading`，`session.js:145`）→ 按字面落码 `SyntaxError`（与 Round 9 P3-1 同族，第 3 次复现）。④ **P3-2 §5 新增 CL/NG 断言（`:374-378`）含未绑定伪变量 `sinaClPayload`/`primaryCl`，且 `< 0.6pp` 仍是上游决定量**——实测恒等式 `|ΔchangePercent| ≡ |今日基差 − 昨日基差|`（`103.82/105.83` 对 `99.227/100.750` ⇒ 昨结基差 `5.042%`、现基差 `4.790%`、差 `0.252pp` ≈ 同刻 `0.236pp`），登记基差区间跨 1.0pp 且单日移动已达 `0.31pp`（CL）/`0.34pp`（NG），16 分钟内实测序列 `0.41pp`→`0.236pp`→`0.306~0.356pp`（十连采）⇒ 门禁随换月漂移随机转红。**待确认风险**：§3.1 `:106-108` 的「`hf_*` 仅内部化」准入契约未登记执行落点（§3.1.2 清单缺搜索联想/自选池准入过滤项）。**通过项**：Round 9 三项缺陷主项全部闭环（`[4.0%,5.0%]` 硬区间已删除并改为 `field7` 绑定断言；`resolveFallbackQuote` 已由 §3.1.2 行 8 定义为 `parseSinaGlobalFuture`；HSI 声明收敛为「日盘结算后」并与 `:289-297` 采样一一对应，18:38 复测 `f60 = 24688 = trends2.preClose` = 新浪字段 7；港美股 `116/105/106` 的 `qt` 价格字段统一 ×1000 实测成立）；文档结构自检通过（标题唯一、`\|`=12 还原后 17 行 4 列无畸形行、`\d`=8）；清单 17 行 `文件:行号` 逐条相符；`qt.f43/qtDivisor` 对同 secid `trends2` 末根比值 10/10 恒 `1.00000`、`f169 == f43 - f60` 10/10 成立）
> - 前序方案（v10 全量闭环版，round 10 被审版）：[`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md)（国际期货与国际股票接入方案 v10：全面闭环 Round 9 审查 1×P2 + 3×P3 缺陷与两项待确认风险。① 消除 CL/NG 上游基差硬编码门禁，断言备源昨结绑定 field7 及主备切换涨跌幅跳变 <0.6pp 平滑连续，对价差做资料性说明；② 消除未定义标识 resolveFallbackQuote，在 parser.js:129 显式导出并统一引用 parseSinaGlobalFuture；③ 收敛 HKFE 声明为「日盘结算后（16:30 后）」对齐采样，追加 18:26 偏差归零复测样本；④ 建立港美股东财 qt 端点量纲表（div1000），parseEastmoney 按 f107 分派 A 股 div100 与港美股 div1000 默认除数，§5 补齐港美股量纲断言；⑤ 明确自选池准入策略排除 hf_* 备用源符号；⑥ ChinaFuturesSessionStrategy 显式委托既有 futures/session.js 并在 §5 设置等价性门禁；发起 Round 10 审查）
> - 前序审查：[`2026-09-16-workbuddy-code-review-round9-handoff.md`](2026-09-16-workbuddy-code-review-round9-handoff.md)（独立复查 `4beb59c`，基准 `45593a5`，本轮实际审查增量 `b344926..4beb59c` = 4 文件 / +256 −58（纯文档），国际品种接入方案 round 9：**未通过**，**1×P2 + 3×P3**。① **P2-1 §5 `:350-351` 新增的 CL/NG 跨源断言把「上游合约基差」固化为实现门禁 `diff ∈ [4.0%, 5.0%]`** —— 该量由上游合约结构决定（新浪 `hf_CL`/`hf_NG` 跟踪当月合约、东财 `CL00Y`/`NG00Y` 为连续/换月指数），随换月漂移且不由实现控制：本项目自身实测序列跨度为 **4.1558% ~ 5.1%**（Round 1 报告 `:81` 早段实测 **4.97%~5.1%** 已越 §5 上界 5.0%；Round 8 17:25 测 `CL 4.75%~4.79%`/`NG 4.27%~4.49%`；**本轮 18:26 独立复测 `CL 4.605%`、`NG 4.1558%`，NG 已跌出本轮新登记的「4.2%~4.5%」区间**），同探针同刻其余 8 品种跨源偏差均 ≤0.04%（排除网络/时刻因素），东财内部 `qt.f60/qtDivisor == trends2.preClose` 10/10 误差恒 0 → 与 Round 8 P2-2「方案自身门禁必然失败、实现者只能改断言或造数」同一失效模式，且 `:351` 与 §3.4.2 `:279`「严禁按 1:1 比对绝对价格」措辞互斥。② **P3-1 §5 `:353` 引用未定义标识 `resolveFallbackQuote`**（全仓+文档 `grep` 仅命中该处，§3.1.2 的 17 条改造点与 §3.4.2 均未定义，`sinaPayload` 亦未绑定）→ 按字面无法落码。③ **P3-2 P3-1（HKFE）闭环证据表未覆盖 Round 8 要求的「日盘（16:00 前）/夜盘（17:15 后）各 ≥3 次」**：`:271-276` 四行中 16:35 自标「日盘收盘后」、17:07 在夜盘开盘前、17:23/17:28 同值且均沿用既采 → **日盘盘中样本 0**，而 `:59` 新增的正是「日盘常规交易时段 1:1」（本轮 18:26 复测机制方向成立：东财 `f60 = 24688 = trends2.preClose`，新浪字段 7 亦收敛至 24688）。④ **P3-3 Phase 2/3 港美股未登记东财 `qt` 量纲，而 §3.1.2 行 5（`:175`）兜底恰为 `div100`**：本轮首次真源实测 `116.00700`/`116.09988`/`105.AAPL`/`106.BABA`/`105.TSLA` 的东财 `qt` 价格字段一律 **×1000**（`433400`→`433.400`、`331340`→`331.34`），同源 `trends2` 与腾讯为真实单位 → 按 `:175` 字面实现将**错 10 倍**，且 §5 无港美股量纲断言（兜底路径 `api.js:150 → :167 → :172`）。**待确认风险**：`hf_*` 与 `GL_*` 同被分类为 `futures_global` 但无入池准入策略（同一品种会并存两个不同水平报价且 `hf_*` 无东财 secid）；`ChinaFuturesSessionStrategy` 未锚定既有 `futures/session.js`。**通过项**：Round 8 五项缺陷主项全部闭环（判据逐字一致、转义与三表结构恢复正常、`sinaScale` 按真源回填、HSI 表述限定、§5 断言补齐 `previous`）；§3.1.2 清单 17 行 `文件:行号` 逐条相符；独立验证 10 品种三源探针 + 仓库外副本回放 §5 四条语音断言 **4/4 GREEN** 且两处变异确定性转红）
> - 前序方案（v9 全量闭环版，round 9 被审版，round 9 判定未通过）：[`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md)（国际期货与国际股票接入方案 v9：全面闭环 Round 8 审查 3×P2 + 2×P3 缺陷与待确认风险。① 严格统一 §3.1.2 与 §3.2.2 对 voiceSchedule.js:28 的自动关闭判据为 if (cfg.enabled && enabled && !hasExtendedHoursAssets && session === 'after-close' && cfg.autoStopAfterClose) enabled = false;，严格保留 session === 'after-close' 维度，消除互斥与全天非交易段永久关闭风险，彻底杜绝午休 11:30–13:00 纯 A 股池被误关的 2026-09-09 M1 回归；② 彻底解决 CL 与 NG 跨源合约月差异与回填问题，登记 4.0%~5.0% 合约月基差，备源切换直接绑定新浪自身的 field7 昨结重校基准以保持涨跌幅连续，§5 断言增加两源合约差区间与重校校验，HG 经 sinaScale=0.01 消除 100 倍量纲差，其余 7 个品种保持同刻 ≤0.05%~0.5% 跨源对齐；③ 完整恢复全文 22+ 处丢失的反斜杠转义（\d 与 \|），消除正则退化，彻底修复 §3.1.2 表 5 行畸形拆列与内容截断；④ 彻底闭环 HKFE 恒指期货日夜盘基准衔接，§2.1 与 §3.4.2 消除互斥，补齐 16:35、17:07、17:23、17:28 四次实测采样表与 7 点差持续说明，确立以东财 trends2.preClose 为最高权威基准；⑤ 补齐 §5 反向断言中 decideVoiceSchedule 的完整 previous 入参，补充午休只暂停断言与变异验证；⑥ 核实 NQ/ES 的 sinaScale=1.0 成立；发起 Round 9 审查）
> - 前序审查：[`2026-09-16-workbuddy-code-review-round8-handoff.md`](2026-09-16-workbuddy-code-review-round8-handoff.md)（独立复查 `b344926`，基准 `45593a5`，本轮实际审查增量 `8a05917..b344926` = 4 文件 / +279 −63（纯文档），国际品种接入方案 round 8：**未通过**，**3×P2 + 2×P3**。① **P2-1 §3.1.2 行 12（`:182`）与 §3.2.2 项 2（`:238`）对 `voiceSchedule.js:28` 的关闭判据互斥**——清单写 `!hasExtendedHoursAssets && !allowed`（丢掉原有的 `session === 'after-close'` 维度，且被复制到 `STATUS.md:16`），正文保留 `session === 'after-close'`；**仓库外副本变异回放**（真实 `decideVoiceSchedule` + 真实 `getVoiceEligibleCodes` + 固定时钟）实测：按清单字面实现后北京 **12:00 午休**、纯 A 股自选 `['sh600519']` → `enabled=false`（永久关闭；现网判据同刻 `true` 仅暂停），`!allowed` 在午休/盘前/非交易日同为 `false` → 关闭窗口被放大到全天非交易时段，与 `marketSession.js:60-70` 记载的 2026-09-09 M1 回归同形，`voiceController.js:93-94` 立即持久化、`:126` 每 30s 复评使手动重开失效；**现有门禁拦不住**（`resolveVoiceScheduleAction` 无生产消费方，`tests/codeReviewRegressions.test.js:279-345` 的 4 条 M1 用例只断言该纯函数）。② **P2-2 §3.1.1 `sinaScale` 列把东财数值回填为「新浪」值（`GL_CL0`/`GL_NG0` 实测证伪）→ §5 `:335` 的 10/10 跨源 ≤0.5% 断言对两品种不可满足**——三轮同刻采样（`push2delay` + `hq.sinajs.cn`，17:25 北京）`CL` 东财 `104.83~104.84` 对 新浪 `100.04~100.08` = **4.75%~4.79%**、`NG` `2.916` 对 `3.046~3.053` = **4.27%~4.49%**（同刻 `NQ/ES/GC/SI/HG/YM` 全在 0.05% 内 → 排除网络/时刻因素），文档登记的「新浪 `104.450` / `2.903`」分别**等于同刻东财 `trends2` 末根**（Round 7 只比对过 5 个品种）；东财内部 `qt.f60/qtDivisor == trends2.preClose` 10/10 误差恒 0 佐证问题仅在跨源登记值；§3.4.2 只登记 HG 的 100× 与 HSI 基准滚动，无 CL/NG 处置。③ **P2-3 本轮删除全文 22 处反斜杠转义 → 6 处正则契约语义损坏 + §3.1.2 表 5 行结构损坏（新引入回归）**——`8a05917` 中 `\d` 8 次、`\|` 14 次、反斜杠合计 22，当前 HEAD **0/0/0**；`\d{6}`/`\d{5}`/`\d{14}` 退化为字面 `d`；按 GFM 还原列数后旧版仅 4/8 列，当前出现 5 个畸形行（`:170`=6、`:173`=7、`:176`=8、`:178`=5、`:186`=6 列，表头 4 列）致「改造目标」列渲染后内容丢失。④ **P3-1 Round 7 待确认风险 1 闭环不完整**——§2.1 `:59` 仍写「完全同源一致」与同轮新增 `:267`「常规时段」+`:268` 夜盘滚动机制互斥，且无「日/夜盘各复采 ≥3 次」证据；本轮 17:23/17:28（夜盘已开）复采 东财 `f60=24688` 对 新浪字段 7 `24681`，偏差 7 点/0.028% 持续存在。⑤ **P3-2 §5 `:346` 反向断言按字面不可满足**——`transitionNotice` 依赖 `voiceSchedule.js:24-26` 的 `previous` 判据，文档未给出该入参（实测不带 `previous` 得 `null`）。**通过项**：Round 7 四项缺陷主项基本落地——§3.1.1 逐品种除数登记真源复测 **10/10 成立**（除数 `CL/NQ/ES=100、GC/A50=10、SI/NG=1000、HG=10000、YM/HSI=1`；`qt.f43/qtDivisor` 对 `trends2` 末根偏差 ≤0.02%、`qt.f60/qtDivisor == trends2.preClose` 误差恒 0、`f44/f45/f46` 同量纲；`HG` 的 `sinaScale=0.01` 经 `648.447×0.01=6.4845` 对 `6.4805` 复核成立）；`(d.f107,d.f57)` 反查键对 10 个 secid 逐一等于 secid；新增行号 `parser.js:34-40`、`marketData.js:196`、`klineService.js:41`、`tts.js:217-218`、`alert.js:87-88`、`monitorTableView.js:95/339`、`kline.js:522`、`batchExportService.js:102` 逐条相符，`formatQuoteSpeech`/`decideVoiceSchedule` 均为真实导出。**待确认风险**：`GL_NQ0`/`GL_ES0` 的「新浪」登记值 `29085.50`/`7673.05` 与本轮实测无法区分是采样时点差异还是同族回填）
> - 前序方案（v8 全量闭环版，round 8 被审版）：[`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md)（国际期货与国际股票接入方案 v8：全面闭环 Round 7 审查 2×P1 + 2×P3 缺陷与待确认风险。① 在 §3.1.1 显式登记 10 品种东财 qt 报价除数 qtDivisor 与新浪换算系数 sinaScale，parseEastmoney 全价格字段逐品种归一化消除 10~100 倍错位，美铜应用 0.01 消除美分/美元差异，§5 升级为 10/10 品种全量缩放断言与变异测试；② 彻底消除 15:00 误关语音，重构 voiceSchedule.js:28 判据为 !hasExtendedHoursAssets && !allowed，清单纳管 voiceSchedule.js:12, 28 与 marketSession.js:46-52，§5 补齐 22:00 与 16:00 真实 decideVoiceSchedule 隔离双向断言；③ 纳管 toEastmoneySecId（parser.js:34-40）支持 GL_*、hk*、us* 返回非空 secid；④ 同步 type='futures_global' 判别至 tts.js、alert.js、monitorTableView.js、kline.js、batchExportService.js 等 5 处消费点，消除外盘尾附“元”；⑤ 处置 HKFE 16:30–17:15 日夜盘昨结滚动衔接；校准 §2.1 快照数值与 §3.3.1 函数名定位；发起 Round 8 审查。**round 8 复审判定：未通过，3×P2 + 2×P3**）
> - 前序方案（v7，round 7 被审版）：[`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md)（国际期货与国际股票接入方案 v7：全面闭环 Round 6 审查 2×P2 + 1×P3 缺陷与待确认风险。① 扩充 A50 端点报价单位归一化至全部价格字段（f43/f44/f45/f46/f60/f169 均 ÷10），彻底消除 openChangePercent 畸变为 -90% 缺陷；② 纳入 parseEastmoney 反向规范代码身份推导（根据 (f107, f57) 输出 GL_* 代码与 futures_global 类型），杜绝报价在 monitorController 写入门禁被丢弃；③ 纠偏 §2.1 新浪字段 2/3 为买一价（99.940）与卖一价（99.960）；④ 显式将 proxyService.js:10, 31 纳入代理出网轮转与重试清单；⑤ 校准 §1.1 历史引用为 §3.2 与 §12.1；发起 Round 7 审查。**round 7 复审判定：未通过，2×P1 + 2×P3** —— ① §3.1.1 端点报价单位登记表对 8 个非 A50 品种失真（实测 `qt` 侧原始缩放逐品种不同，沿用 `div100` 使 `GC/SI/HG/NG/YM/HSI` 价格错 10–100×），且本轮删除「其余外盘与港美标的按自身口径解析」使其余 9 品种再无缩放指令，`§5:303` 断言只覆盖 A50；② §3.2.2「绝不误关语音」被 `src/js/services/voiceSchedule.js:28` 的 `!futures.length` 永久关闭分支证伪（北京 ≥15:00 即 `enabled=false` 持久化 + 播「已收盘」），`§5:312` 断言因 `allowed=true` 恒返回 idle 而零判别力，清单未纳管 `marketSession.js`/`voiceSchedule.js`；③ `toEastmoneySecId`（`src/js/parser.js:34-40`）未纳入任何改造点致外盘东财三端点不可达；④ 本轮新增 `type='futures_global'` 未同步 `tts.js:217-218`/`alert.js:87-88` 等 `type` 判别消费点）
> - 前序方案（v6，round 6 被审版）：[`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md)（国际期货与国际股票接入方案 v6：全面闭环 Round 5 审查 3×P2 + 2×P3 缺陷。① 彻底清除结构性重复正文，全篇严格单份正文与闭合成表；② A50 10× 单位差异登记为东财内部端点属性，落实 `parseEastmoney` 入口归一化消除 1/10 错位；③ 将浏览器代理出网路径 `proxyRoutes.js:10, 13` 与 `klineService.js:40` 纳入多主机轮转与 `push2delay` 兜底；④ 契约正则完整补齐 `$` 终结锚点，全值域单测断言 10/10 PASS；⑤ 收敛根数表述为下限+采样时刻并限定受限网络实测范围；发起 Round 6 审查。**round 6 复审判定：未通过，2×P2 + 1×P3** —— ① §3.1.2 行 5 的单位归一化只枚举 `f43/f60/f169`，而 §3.1.1 已按「端点」登记「东财 qt = 0.1 点」，`f44/f45/f46` 仍走 A 股 `div100`（`src/js/parser.js:99/114/115`）→ 按字面实施后 A50 的 `open/high/low` 仍为真值 1/10（`1426.7/1441.8/1419.8` vs `14267/14418/14198`）且 `openChangePercent` 由 `-0.028%` 变 **`-90.003%`**（自选表开盘列 `monitorTableView.js:131/360` 用户可见），`§5:303` 断言只校验 `price/prevClose` 零判别力；② 改造清单未覆盖 `parseEastmoney` 的 code 身份推导（`parser.js:106-107` 对非 A 股恒走 `('sz') + f57` 兜底），真源 payload 实跑得 `code='szcn00y'`（A50）/`'szhsi_m'`（HSI），在 `monitorController.js:44` 的 `currentCodes.has(quote.code)` 门禁被整段丢弃 → `§3.4:243`「消费层全面 1:1 精确对接」不可达、`api.js:161-167` 恒判「缺」并反复触发兜底请求；③ §2.1 字段 2/3 被改标为「买一量/卖一量」且示例值改写为 9.940/9.960，与同段 `:33` payload `99.940,99.960` 及真源（`hf_CL` 第 2/3 位 `99.980/99.990` 夹住最高/最低；`hf_CHA50CFD` 第 2 位 `14365.000` == 同刻 `trends2` 末根；真正量在 `index10/11`）矛盾，属本轮新引入回归）
> - 前序方案（v5，round 5 被审版）：[`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md)（国际期货与国际股票接入方案 v5：全面闭环 Round 4 审查 3×P2 缺陷。① 登记东财 SGX 市场 104（`104.CN00Y`）以 0.1 点为单位报价事实，注册表新增源间缩放系数 0.1，明确备源切换执行 `×0.1` 缩放归一化与基准误差为 0 验收断言；② 纠偏主机表述，明确记录在受限网络下仅 `push2delay.eastmoney.com` 稳定可达，并将之正式追加至 `EASTMONEY_TRENDS_HOSTS` 与 `klineService.js` 轮转列表，`qt` 增加兜底；③ 扩充 `inferAssetType(code)` 契约值域覆盖 `GL_` 前缀与 `hf_` 形态（映射 `futures_global`）及 `r_hk`，提供全值域覆盖单测断言，杜绝 A 股误路由；发起 Round 5 审查。**round 5 复审判定：未通过，3×P2 + 2×P3** —— ① 方案正文 §1–§3.1.2 被整体重复 4 份（`# 国际期货…` H1 出现 4 次，`:157`/`:313`/`:469` 三行行尾直接拼接下一份副本 H1、三个表行均未闭合），§3.1.2 的 `src/js/parser.js:11-25` 契约行被切成 `:157`① / `:313`②③ / `:469`④ / `:625`⑤⑥ 四段交错于约 465 行重复正文（全文 60%）中；② A50 10× 单位实测发生于**东财内部端点之间**（`push2delay` 同时刻 `qt` `f43=143900`/`f60=142710` vs 同源 `trends2` 末根 `14390.0`/`preClose=14271`、`kline(klt=1)` 末根 `14390.0`，比值恒 `10.000`；`trends2`末根/新浪 `f0` = `0.99991`），方案却登记为「源间(东财:新浪)」且只在「备源切换」时应用 → §3.4.1「同源…100% 吻合」对 `GL_A50` 被证伪、默认主源经 `parser.js:97-98` 的 `div100` 会输出 `1439.00`（真值 1/10，与同源图表差 10 倍）；③ `push2delay` 仅进了服务端轮转数组，浏览器侧 `api.js:52`/`:66`、`kline.js:73` 经 `proxyRoutes.js:10`(`push2his`)/`:13`(`push2`) 静态 target 仍连不通（本轮逐主机实测 `push2his`/`90.push2his`/`1.push2.*` 五端点全 `fetch failed`、`push2` 恒 502、仅 `push2delay` 三端点 `rc=0`）→ §5 三端点验收断言仍不可满足；④ §3.1.2 契约 ①③④ 丢失 `$` 锚点，按文档逐字实现后新增断言 `inferAssetType('unknown_foo')` 失败（实得 `'stock_us'`）且与 `STATUS.md:10` 的带锚点版本互斥；⑤ `m:134`「`trends2` 与 `kline` 938 根」不可复现（本轮同时刻 964/963）且与同文 `:213` 的 914/913 互斥**）
> - 前序审查：[`2026-09-16-workbuddy-code-review-round6-handoff.md`](2026-09-16-workbuddy-code-review-round6-handoff.md)（独立复查 `823f3df`，基准 `45593a5`，本轮修复增量 `bb2e47d..823f3df` = 3 文件 / +71 −507（纯文档；主方案 769 → 315 行），国际品种接入方案 round 6：**未通过**，**2×P2 + 1×P3**。① **P2-1 §3.1.2 `:164` 的单位归一化只枚举 `f43/f60/f169`**——`§3.1.1 :107` 已按「**端点**报价单位」登记 A50 为 `0.1点 : 1点 : 1点`，而实施指令只覆盖 3 个字段，`f44/f45/f46`（最高/最低/开盘）仍经 A 股 `div100`（`src/js/parser.js:99/114/115`）→ 真源同时刻模拟（`104.CN00Y`：`f43=143650 f44=144180 f45=141980 f46=142670 f60=142710 f169=940`，`push2delay` 16:35）得 `open=1426.7`/`high=1441.8`/`low=1419.8`（真值 `14267/14418/14198`，新浪字段 8/4/5），且因 `open` 与 `prevClose` 量纲不再一致，`openChangePercent` 由 `-0.028%` 变为 **`-90.003%`**（`monitorTableView.js:131/360` 自选表开盘列用户可见；`momentumController.js:347` 的 `q.high` 追踪使 `maxHigh` 恒被 `price` 覆盖、回撤恒 0）；`§5:303` 断言只校验 `price/prevClose`，零判别力。② **P2-2 改造清单未覆盖 `parseEastmoney` 的 `code` 身份推导**——`src/js/parser.js:106-107` 对非 A 股恒走 `('sz') + f57` 兜底（`normalizeCode('CN00Y')===null`、`normalizeCode('HSI_M')===null` 实测），真源 payload 实跑仓库真实 `parseEastmoney` 得 A50 `code='szcn00y'`、HSI `code='szhsi_m'`，而 `src/js/controllers/monitorController.js:44` 以 `currentCodes.has(quote.code)` 为写入门禁 → 报价永不进 `state.quotes`、图表报价合并跳过、语音无播报，`§3.4:243`「消费层全面 1:1 精确对接」不可达，`api.js:161-167` 的 `gotCodes`/`missing` 判定恒为真并反复触发兜底请求；`§5:303` 断言（不含 `code`）无法拦截。③ **P3-1 §2.1 `:37-38` 字段 2/3 被改标为「买一量（9.940）/卖一量（9.960）」**（本轮新引入回归，round 4 文本为「买一（99.940）/卖一（99.960）」，`git show bb2e47d:<doc> | sed -n '37,38p'` 佐证），与同段 `:33` payload `99.940,99.960` 及真源矛盾（`hf_CL` 第 2/3 位 `99.980/99.990` 夹住 `index4=100.610` 最高与 `index5=99.050` 最低；`hf_CHA50CFD` 第 2 位 `14365.000` == 同刻 `trends2` 末根，真正量在 `index10/11` = `2/7`）→ 2/3 应为买一价/卖一价。**通过项**：Round 5 的 P2-1 结构（`awk '/^#/'` 得各标题唯一、章节单调、§3.1.2 表 11 行全部 `|` 闭合）、P2-3 出网点（`proxyRoutes.js:10/13`、`klineService.js:40` 入清单；逐主机实测 `push2his`/`90.push2his`/`1.push2`/`push2` 不可达、`push2delay` 的 `qt`/`trends2`/`kline` 三端点全 `rc=0`）、P3-1 契约锚点（按文本逐字实现后 10/10 PASS）、P3-2 根数（全篇单处权威表述，16:18 的 964/963 与本轮 16:35 实测 976/975 按 HKFE 分钟增量自洽）经独立复测**全部成立**；§3.1.1 既有导出断言块 9/9 PASS；`§3.4` A50 昨结 1:1 成立（`142710 × 0.1 == 14271 == trends2.preClose == 新浪字段 7`）。**待确认风险**：§3.1.2 行 3 只列静态 target 常量，而「多主机轮转 + 失败重试」的落点须在 `server/proxyService.js:10/31`（`resolveProxyTarget` 返回单一 `target.url`）→ 按清单字面只改常量会得到「换了 host 但没有轮转」。**附带非阻塞建议**：`:15` 的引用由 `§3.2` 改为 `§1.2`，而该 plan 文档 `### 3.2 本方案不包含`（第 52 行）才含「外盘期货、外汇、数字货币」，`## 1.` 无子节。本轮 diff 全为 `.md`，未触碰产品代码与测试）
> - 前序审查：[`2026-09-16-workbuddy-code-review-round5-handoff.md`](2026-09-16-workbuddy-code-review-round5-handoff.md)（独立复查 `cfd243c`，基准 `45593a5`，本轮修复增量 `f637dd9..cfd243c` = 3 文件 / +529 −23（纯文档），国际品种接入方案 round 5：**未通过**，**3×P2 + 2×P3**。① **P2-1 方案正文 §1–§3.1.2 被整体重复 4 份、§3.1.2 契约表行被切成 4 段**——`# 国际期货…` H1 出现 4 次（`:1` 及 `:157`/`:313`/`:469` 的行尾拼接），`## 1.`/`## 2.`/`### 3.1`/`#### 3.1.1`/`#### 3.1.2` 各 4 次，`src/js/parser.js:11-25` 契约行被切成 `:157`①/`:313`②③/`:469`④/`:625`⑤⑥ 四段、与约 465 行重复正文（全文 769 行的 60%）交错，`:157`/`:313`/`:469` 三个表行均未闭合（行尾直接拼接下一份副本 H1）；② **P2-2 A50 10× 单位约定登记在错误边界**——`push2delay` 同时刻实测 `104.CN00Y`：`qt/stock/get` `f43=143900`/`f60=142710`，同源 `trends2` 末根 `14390.0`/`preClose=14271`、`kline(klt=1)` 末根 `14390.0`，新浪 `hf_CHA50CFD` `14392.800`/`14271.000` → `f43/trends2末根 = 10.000`、`f60/preClose = 10.000`、`trends2末根/sina.f0 = 0.99991`，即 10× 分歧在**东财内部（qt ↔ trends2/kline）**、trends2/kline 与新浪同级；方案却登记为「源间(东财:新浪)」并只在「备源切换」时应用 → §3.4.1「qt 与 trends2 同源…最新价/涨跌幅/分时末端 **100% 吻合**」对 `GL_A50` 被证伪（143900 vs 14390.0；142710 vs 14271），默认主源路径经 `src/js/parser.js:97-98` 的 `div100`（A 股口径）会输出 `price=1439.00`/`prevClose=1427.10`（真值 1/10，与同源图表正好差 10 倍），若把 `×0.1` 误施于 `trends2`/`kline` 则图表反向缩小 10 倍，§5 `:762` 断言只覆盖备源昨结、三种错位均拦不住（对照 `134.HSI_M` 为 1:1：`qt.f43=24701`、`trends2` 末根 `24701`、`f60=preClose=24676`）；③ **P2-3 Round 4 P2-2 闭环不完整：`push2delay` 未落到浏览器可见的东财出网路径**——§2.1 `:206` 要求「对 `qt/stock/get` 增加 `push2delay` 兜底」，但 §3.1.2 只列 `server/marketData.js:18-22, 160, 194` 与 `klineService.js:95`（服务端 `/api/cache/*` 用），浏览器侧 `src/js/api.js:52`/`:66`、`src/js/kline.js:73` 经 `server/index.js:376 → proxyService.js:10 → proxyRoutes.js:10`(`push2his`)/`:13`(`push2`) 静态 target 直连；逐主机实测该两主机五端点全不可达（`push2his`/`90.push2his`/`1.push2.*` = `fetch failed`，`push2` 恒 502），**仅 `push2delay` 三端点 `rc=0`**（`m:134` 964/963、`m:104` 1189、`qt` rc=0）→ §5:760「三端点拉取」在方案自述的受限网络下仍不可满足，§3.4.1「东财第一源」静默退化为新浪单源；④ **P3-1 §3.1.2 契约 ①③④ 丢失 `$` 锚点**（截断点恰在锚点处）→ 按方案文本逐字实现 `inferAssetType` 后本轮新增的 10 条断言 9 通过 1 失败（`inferAssetType('unknown_foo')` 实得 `'stock_us'`、断言要求 `'stock_cn'`），同 commit `STATUS.md:10` 记录的却是带 `$` 版本（Round 4 P2-3 的 `hf_`/`r_hk` 值域**内容层已闭环**：`hf_SI`/`hf_HSI`/`hf_CL`/`r_hk00700` 逐值正确，失败仅由锚点丢失引起）；⑤ **P3-2 §2.1 新增主机/根数断言作用域越界且不可复现**——`:206`「`trends2` 与 `kline` 938 根」实测此时刻为 964/963（Round 3 记 914/913、Round 4 记 935/938），与同文 `:213` 的 914/913 互斥，且两条端点口径天然差 1~3 根、写成同值不成立；`:214`「各类网络环境下均能稳定可达」超出实测范围。通过项：Round 4 P2-2 的**事实层**逐主机实测成立、Round 4 P2-3 的 `hf_`/`r_hk` 值域内容层闭环、`marketData.js:14-16/18-22/160/194` 与 `klineService.js:91-95/95`、`utils.js:76-86` 引用逐条相符、`m:134`/`m:104` 三端点 `push2delay` 可用；本轮 diff 全为 `.md`，未触碰产品代码与测试）
> - 前序审查：[`2026-09-16-workbuddy-code-review-round4-handoff.md`](2026-09-16-workbuddy-code-review-round4-handoff.md)（独立复查 `cb49106`，基准 `45593a5`，范围 6 文件 / +839 −2（纯文档；本轮修复增量 `7781fe1..cb49106` = 3 文件 / +46 −20），国际品种接入方案 round 4：**未通过**，**3×P2**。① **P2-1 §3.4.2 `:219` 的 A50 合约锚点「完全一致」被真源证伪**——新浪 `hf_CHA50CFD` 字段 7 昨结 `14271.000` 对东财 `104.CN00Y` `f60` `142710`（`f43=143980` 对最新 `14400.000`）**相差 10 倍**，÷10 后才对齐，而同段 `hf_HSI` 一档确为 1:1（`24676` == `24676.000`）→ 方案未登记"市场 104（SGX）以 0.1 点为单位"的缩放约定，按字面实现备源切换会使 `GL_A50` 昨结基准 10× 错位，与 §3.4「保证…无缝对接」/§3.4.1「100% 吻合」冲突（`142710/14271.0 = 10.0007`）；② **P2-2 §2.1 `:58` 新增主机断言不可复现**——对 `134.HSI_M` 逐主机 4/4 实测 `push2his`/`90.push2his`/`1.push2.*` 全部 `UND_ERR_SOCKET`、`push2` 恒 502，**仅 `push2delay` 可达**（`trends2` rc=0 935 根、`kline` 938 根、`qt/stock/get` rc=0），且仓库自身注释记载 `push2his` 主主机 `0/10` 失败（`server/marketData.js:14-16`、`server/klineService.js:91-92`），`EASTMONEY_TRENDS_HOSTS`（`server/marketData.js:18-22`）与 `klineService.js:95` 的 hosts 均**不含** `push2delay` → Round 3 待确认风险 1 被未复现的实测断言"闭环"，按 §2.1 `:50` 复用既有轮转机制则 Phase 1 全部 10 品种的东财三端点在本机不可达；③ **P2-3 §3.1.2 `:137` 新增 `inferAssetType` 契约值域遗漏 `hf_`**——按契约逐字实现后 `hf_SI`/`hf_HSI`/`hf_CL` 全部回落 `'stock_cn'`（`isFutureCode('hf_SI')===false`，无分支可救），与 §3.4.2 `:218`（新浪 `hf_` 为外盘**全量**备源）、§5 `:271`（`hf_SI` 应映射 COMEX 白银）矛盾，`resolveSessionStrategy('hf_HSI')` 落 `chinaStockStrategy` → Round 1 P2-4 的 22:00 播"已收盘"且 `autoStop` 关语音失效模式；Round 3 P3-1 明确要求的「同步核对 `hf_` 形态归属」未闭环，`:129` 的"**全值域**正交断言"措辞与枚举不符（另 `r_hk00700` 同族回落 `stock_cn`）。通过项：Round 3 P2-1 主项（`GL_HSI`→`134.HSI_M`：`m:134` `total=112`、`qt rc=0 f58=恒生指数期货主力 f60=24676`、`kline(klt=1)` 938 根，反例 `134.HSI00Y`/`134.MHI_M` 均 `rc=100 data:null`）、P3-1 断言块改为仓库既有导出后**实跑 9/9 PASS**、P3-2 `kline.js:326-341`/`:347` 与源码逐字相符、P3-3 美股「基准 71 + 偶发追加 2 尾部空字段至 73」三轮采样复现成立、`m:100–104` 规模 63/104/117/178/33、门禁 `npm test` 843/843；本轮 diff 全为文档，未触碰产品代码与测试）
> - 前序方案（v4，round 4 被审版）：[`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md)（国际期货与国际股票接入方案 v4：全面闭环 Round 3 审查 1×P2 + 3×P3 缺陷；经东财 `m:134` 真源实测，恒指期货主力合约 `134.HSI_M` 三端点完备可用且昨结与新浪 `hf_HSI` 24676.000 同源，确立东财为第一源、新浪为全量备源，彻底消除单源降级；启动期正交隔离断言改为基于仓库既有可用模块直接可执行；精确纠偏 `kline.js:347` 消费方行号；完善美股 71/73 字段稳定性表述与港股盘中验证条件；发起 Round 4 审查。**round 4 复审判定：§3.4.2 A50 锚点「完全一致」实为 10× 单位未登记、§2.1:58 「`push2delay` 与 `push2his` 实测均可稳定拉取」不可复现（本机 `push2his` 族 4/4 `UND_ERR_SOCKET`）、§3.1.2:137 新增 `inferAssetType` 契约值域遗漏 `hf_` 形态致外盘备源代码回落 A 股——未通过（3×P2）**）
> - 前序审查：[`2026-09-16-workbuddy-code-review-round3-handoff.md`](2026-09-16-workbuddy-code-review-round3-handoff.md)（独立复查 `8a97cca`，基准 `45593a5`，国际品种接入方案 round 3：**未通过**，1×P2 + 3×P3。核心：① **P2-1 `GL_HSI`「东财无恒指期货合约」被真源证伪**——东财 `m:134` 实收录恒指期货（`total=112`，`134.HSI_M|恒生指数期货主力`：`qt rc=0 f60=24676`、`trends2 n=914`、`kline(klt=1) n=913`，两次采样一致；昨结与新浪 `hf_HSI` 24676.000 同源；反例 `134.HSI00Y`/`134.MHI_M` 均 `rc=100 data:null`），方案把 Round 2「不得在扫描未完成时断言全仓不存在」的负向结论作用域裁剪为 `m:100–124` 后升格为架构事实 → §2.1 `:57`、§3.1.1 `:102`、§3.4.2 `:207-209` 的「纯新浪单源 / 图表区无历史分时」降级契约与 §5 `:260` 验收断言均不成立；② P3-1 本轮新增的解析层正交断言仍引用仓库不存在的 `inferAssetType`（`§3.1.1 :108` 静态 import 实测 `SyntaxError: does not provide an export named 'inferAssetType'`）；③ P3-2 §3.1.2 行 9 新增的消费方行号 `kline.js:345` 应为 `347`（`345` 是 `const pc = Number(prevClose);`）；④ P3-3 §2.3 美股「实测 71 字段（非交易时段）」不可复现（连续三轮采样 `usAAPL=73/73/71`、`usNVDA=73/71/73`，差异为上游偶发追加的 2 个尾部空字段）。通过项：`GL_A50`→`104.CN00Y` 与其余 8 个 secid 双端点实测成立、CME 结算窗口夏令时侧真源验证（首根 `06:00`、`05:00–05:59` 零 bar）与 CST 算术自洽、`kline.js` `STOCK_CODE_RE` 210/218/232、港股 331/无 13:00 与美股 391/390 端点口径、`m:100–104` 规模 63/104/117/178/33、§3.4 锚点（新浪昨结 14271.000 = 东财 `f60` 142710）、§3.2.2 `default` 兜底；diff 全为文档）
> - 前序方案（v3，round 3 被审版）：[`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md)（国际期货与国际股票接入方案 v3：全面闭环 Round 2 审查 2×P2 + 5×P3 缺陷；纠正东财 A50 期指真实市场号为 `m:104`（`104.CN00Y`，实测 `trends2` n=1088）；东财无恒指期货合约，确立 `GL_HSI` 走新浪单源降级；恢复 CME 每日结算冬夏令时双变体；修复启动期冲突断言为行为学输出验证；纠偏 `kline.js` 正则位置；对 `getPriceLimit` 消费方做 `null` 显式短路；修正美股 71 字段与港美股分时/K线端点口径；发起 Round 3 审查。**round 3 复审判定：`GL_HSI` 单源结论被真源证伪、断言引用不存在标识、消费方行号错标、字段数不可复现——未通过**）
> - 前序审查：[`2026-09-16-global-market-workbuddy-code-review-round2-handoff.md`](2026-09-16-global-market-workbuddy-code-review-round2-handoff.md)（独立复查 `ea2bb5a`，国际品种接入方案 round 2：**未通过**，2×P2 + 5×P3。核心：① P2-1 在新增的 `m:100` 行原形复现——东财 `m:100` 经 `clist` 枚举实为**国际指数**市场（含现货 `HSI|恒生指数`、`XIN9|富时中国A50`），`100.CHA50CFD`/`100.HSI00Y` 双端点恒 `data:null`，真实 A50 期指 secid 为 `104.CN00Y`，`m:100-124` 全量扫描无恒指期货合约 → Phase 1 的 `GL_A50`/`GL_HSI` 无法取数；② P2-2 连带回归——本轮把 CME 每日结算窗口固化为「北京 05:00-06:00」并删去 Round 1 原文的冬季变体，标准时实际为 06:00-07:00，会把真实交易时段当休市剔除；③ P3×5：冲突断言恒真且引用不存在的 `DOMESTIC_PRODUCT_MAP`、`kline.js:331` 行号错标为 `STOCK_CODE_RE`、`getPriceLimit` 返回 `null` 被 `Number(limit)||10` 静默回落、美股 73 字段实测 71、港股 330 根与（区间,根数）自相矛盾且与美股端点口径不一致。通过项：P2-1 主项 8 个 secid、P2-2 命名空间、P2-3 窗口参数化、P2-4 分派器、P2-5 清单、P3-3 Gzip 基准 115.10 KB（实测 115,098 B）、P3-4 持仓量纠偏；门禁 843/843）
> - 前序审查：[`2026-09-16-workbuddy-code-review-round1-handoff.md`](2026-09-16-workbuddy-code-review-round1-handoff.md)（独立审查 `a2b8791`，国际品种接入方案 round 1：**未通过**，6×P2 + 4×P3。核心证伪：① 东财 secid 规则 `102.${symbol}00Y` 不成立[金属在 `m:101`、指数在 `m:103`]，Phase 1 清单 8/9 品种取数为空；② `SI0` 与广期所工业硅标识冲突；③ `INTRADAY_SESSION_RANGES` A 股窗口把美股 391 根分时全滤空；④ 缺"代码→策略"分派契约，混合自选 22:00 会永久关闭语音；⑤ 服务端等 ≥8 处归一化未纳入改造面；⑥ 报价源与图表源合约偏离约 5%）
> - 前序审查：[`2026-09-16-workbuddy-code-review-round2-handoff.md`](2026-09-16-workbuddy-code-review-round2-handoff.md)（独立复审 `1d08f60`，图表日期格式与成交量红绿修复 round 2：**全面审查通过**，0 缺陷；P2×3 缺陷实测全部闭环，变异验证与门禁 843/843 全绿通过）
> - 前序审查：[`2026-09-15-workbuddy-code-review-round1-handoff.md`](2026-09-15-workbuddy-code-review-round1-handoff.md)（独立审查 `46a33ca`，round 1：未通过，P2×3；已于 round 2 彻底闭环）
> - 最新缺陷修复：[`2026-09-16-chart-date-format-and-volume-color-handoff.md`](2026-09-16-chart-date-format-and-volume-color-handoff.md)（图表时间轴年-月-日中国习惯格式化与成交量红绿规则修复及 WorkBuddy round 1 P2×3 缺陷闭环：① 去除日K线无意义分钟 00:00，注册 zh-CN / yyyy-MM-dd 本土化时间轴十字线；② 修复高开低走假阴线收涨[如 9.11 新农开发]与一字涨停被误染为绿柱缺陷，导出 isVolumeBarUp 并增加 `pc>0` 严格守卫；③ 修复分时图浮层显式传参 `'1m'` 恢复 HH:mm；门禁 843/843 + 12/12 + lint 0 + build 成功）
> - 前序功能交付：[`2026-09-15-close-snapshot-handoff.md`](2026-09-15-close-snapshot-handoff.md)（停播提示后补播「最后一轮选中字段」：`已收盘`/`中午休市`/期货日夜空档均覆盖，只播用户勾选字段——只勾价格即「已收盘，1272.75元」；快照集合取上一拍 `eligibleCodes` 并按当前订阅过滤；不加开关。11 条断言在改动前全红、E2E 变异验证；门禁 837/837 + 75/75 + lint 0 + build 成功）
> - 前序功能交付：[`2026-09-15-voice-dedupe-toggle-handoff.md`](2026-09-15-voice-dedupe-toggle-handoff.md)（语音播报新增「相同报价不重复播报」开关，作为交易时段那一排第 5 个开关；含 1 项真实接线缺陷的发现与修复——视图契约 `(key, checked)` 与单参数 handler 错位导致"点开关没反应"，仅 E2E 可覆盖；判别力与变异验证齐备，门禁 826/826 + 75/75 + lint 0 + build 成功）
> - 最新调查（图表）：[`2026-09-14-limit-up-chart-date-flip-investigation-and-resolution-handoff.md`](2026-09-14-limit-up-chart-date-flip-investigation-and-resolution-handoff.md)（涨停看板切前一天图表逻辑错误根因剖析、调度层解耦与解决方案；round 16 缺陷在 T-2 侧已闭环，T-1 侧残留 2 项 P3 见 round 17）
> - 最新交接（审查，代码落地）：[`2026-09-14-workbuddy-code-review-round2-handoff.md`](2026-09-14-workbuddy-code-review-round2-handoff.md)（独立审查 `76e9cd9`，代码落地阶段 round 2：**未通过**，P3×2；round 1 两项缺陷主项均实测闭环——M1 变异回退 `app.js:1511` 使用例 7 确定性变红、M6 变异 `chartRowController.js:391` 使用例 5 确定性变红、门禁 820/820 + lint 0 + build 成功；残留：用例 5 未覆盖 `:546` 注入点（M7 变异 6/6 仍绿，生产装配探针可捕获）、用例 7 泄漏 `state.limitUp.timer` 桩句柄致跨文件顺序依赖）
> - 前序交接（审查，代码落地）：[`2026-09-14-workbuddy-code-review-round1-handoff.md`](2026-09-14-workbuddy-code-review-round1-handoff.md)（独立审查 `892f1b8`，代码落地阶段 round 1：**未通过**，P2×1 / P3×1；产品代码三项改造与门禁 820/820 均实测生效，但测试矩阵相对 §4.3 有两项未落地 —— 用例 7 未建 `#/limit-up` 前置致 `app.js:1511` 回退变异后仍全绿（假通过，无法证伪验收标准 3）、(b)(c) 端到端断言缺失；盘前锚定注入零覆盖）
> - 前序审查（文档阶段）：[`2026-09-14-workbuddy-code-review-round17-handoff.md`](2026-09-14-workbuddy-code-review-round17-handoff.md)（独立审查 90a0d60，round 17：**未通过**，P3×2；节点 F 的 T-1 保留下界「≥77~80」越出自身 `x ≤ 240` 前提、T-1 收盘 77~79 被错误归因于 `n` 达 241/243）
> - 前序审查：[`2026-09-14-workbuddy-code-review-round16-handoff.md`](2026-09-14-workbuddy-code-review-round16-handoff.md)（独立审查 9c78a3c，round 16：**未通过**，P3×1；T-2 保留根数与滑出阈值的 241/243 日长维度缺失，已于最新方案 T-2 侧确认闭环，T-1 侧残留项见 round 17）
> - 前序审查：[`2026-09-14-workbuddy-code-review-round15-handoff.md`](2026-09-14-workbuddy-code-review-round15-handoff.md)（独立审查 bd0e939，round 15：**未通过**，P3×1；§4.3 取值域枚举遗漏 `60000`，已于 round 16 复审确认闭环）
> - 前序审查：[`2026-09-14-workbuddy-code-review-round14-handoff.md`](2026-09-14-workbuddy-code-review-round14-handoff.md)（独立审查 33b8f53，round 14：**未通过**，P3×2；2 项主项已于 round 15 复审确认闭环）
> - 前序审查：[`2026-09-14-workbuddy-code-review-round13-handoff.md`](2026-09-14-workbuddy-code-review-round13-handoff.md)（独立审查 c3f5bc0，round 13：**未通过**，P2×2 / P3×3；主项已闭环，残留项见 round 14）
> - 更早审查：[`2026-09-14-workbuddy-code-review-round12-handoff.md`](2026-09-14-workbuddy-code-review-round12-handoff.md)（独立审查 7b34bc5，round 12：**未通过**，P3×4；主项已闭环，残留项见 round 13）
> - 更早审查：[`2026-09-14-workbuddy-code-review-round11-handoff.md`](2026-09-14-workbuddy-code-review-round11-handoff.md)（独立审查 147573a，round 11：**未通过**，P2×1 / P3×4；P2 主因已闭环，残留项见 round 12）
> - 更早审查：[`2026-09-14-workbuddy-code-review-round10-handoff.md`](2026-09-14-workbuddy-code-review-round10-handoff.md)（独立审查 0305357，round 10：**未通过**，P1×1 / P2×1 / P3×1；已闭环）
> - 更早审查：[`2026-09-14-workbuddy-code-review-round9-handoff.md`](2026-09-14-workbuddy-code-review-round9-handoff.md)（独立审查 e2a8f0d，round 9：**未通过**，P1×1 / P2×1 / P3×4；引用准确性全通过）
> - 更早审查：[`2026-09-13-workbuddy-code-review-round3-handoff.md`](2026-09-13-workbuddy-code-review-round3-handoff.md)（独立审查 2f94e08，round 3：**通过**，无 P0/P1/P2；遗留 2 项 P3）
> - 修复交接：[`2026-09-13-storage-lru-tiebreak-and-parser-ratio-round2-closure-handoff.md`](2026-09-13-storage-lru-tiebreak-and-parser-ratio-round2-closure-handoff.md)（round 2 缺陷闭环：LRU tie-break 确定性加固、Eastmoney 量比缺失语义对齐）
> - 审查交接：[`2026-09-13-workbuddy-code-review-round2-handoff.md`](2026-09-13-workbuddy-code-review-round2-handoff.md)（独立审查 ffe92b3，round 2：P0/P3 修复有效，但新增 LRU 淘汰单测偶发失败 P2 未通过）
> - 修复交接：[`2026-09-13-storage-orphan-and-parser-ratio-closure-handoff.md`](2026-09-13-storage-orphan-and-parser-ratio-closure-handoff.md)（round 1 缺陷闭环：storage 孤岛副本清除、量比缺失语义与 pinnedSort 消除）
> - 审查交接：[`2026-09-13-workbuddy-code-review-round1-handoff.md`](2026-09-13-workbuddy-code-review-round1-handoff.md)（独立审查 ad69e35：BUG-02/03 确已闭环，但新引入 storage 孤岛副本 P0 回归）
> - 独立审查：[`2026-09-13-bug03-regression-and-storage-visibility-closure-handoff.md`](2026-09-13-bug03-regression-and-storage-visibility-closure-handoff.md)（BUG-03 回归修复与存储降级可见性闭环）
> - 独立核验：[`2026-09-12-review-fix-verification.md`](2026-09-12-review-fix-verification.md)（独立核验 b601d1b，发现 BUG-03 回归）
> - 独立审查：[`2026-09-12-independent-full-codebase-review-handoff.md`](2026-09-12-independent-full-codebase-review-handoff.md)（全仓架构与工程审查结论）

---

## 1. 2026年9月：全量审查闭环、联想搜索与架构加固

| 日期 | 文档 | 说明 |
| :--- | :--- | :--- |
| 2026-09-16 | [`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md) | **架构方案与可行性调研（v11 终审闭环与设计收敛定稿版）**：国际期货（外盘期货）与国际股票（港美股）接入方案 v11，全面闭环 Round 10 审查 2×P2 + 2×P3 缺陷与待确认风险。① 纳管东财美股第三大市场 m:107（AMEX/Arca，实测 4774 只标的，含 107.SPY），统一回退 div1000 默认除数消除 10 倍错位；② 会话策略接口增加 code 参数，ChinaFuturesSessionStrategy 严格委托既有 session.js 并覆盖 RB0/AU0/T0/IF0 等价性与变异门禁；③ 彻底清除未定义标识 isFutureTrading，统一采用真实导出 isFutureTrading；④ §5 CL/NG 断言补齐完整夹具变量绑定，收敛阈值为上游基差日间变动健壮性上限守卫；⑤ 搜索联想输出与自选池存储严格排除 hf_* 内部备源代码；多智能体设计审查正式收敛，定稿交付就绪实施 |
| 2026-09-16 | [`2026-09-16-workbuddy-code-review-round10-handoff.md`](2026-09-16-workbuddy-code-review-round10-handoff.md) | **独立代码审查（国际品种方案 Round 10 复查）**：被审 HEAD `be39cd9`，基准 `45593a5`，本轮实际审查增量 `cc760c4..be39cd9` = 3 文件 / +89 −34（纯文档；主方案 417 行）。**未通过：2×P2 + 2×P3**。P2：① **P2-1 港美股市场号漏掉东财第三个美股市场 `m:107`（AMEX/腾讯 `.AM`）**——`:88`/`:127-135`/`:191`/`:192` 仅枚举 `105/106`（由 `105.AAPL`、`106.BABA` 两样本外推）；实测 `105.SPY`/`106.SPY`/`105.IMO`/`106.IMO`/`105.RLGT`/`106.RLGT` 全 `rc:100 data:null`，`107.SPY` `rc=0 f43=757390 f107=107`（三端点全可用：`trends2` n=391、`kline` n=390），`clist fs=m:107 → total=4774`，腾讯 `v_usSPY="…SPY.AM~757.39~760.88…"` ⇒ `m:107` 标的走东财兜底恒无报价；若补号却沿用登记集合则 `f107=107` 落空按 `div100` → `7573.90` 对真值 `757.39`，**10× 错位**，而 §5 断言只覆盖 `f107: 105`。② **P2-2 新增「`ChinaFuturesSessionStrategy` 必须委托 `getFuturesSession(code, …)`」（`:198`/`:222-225`）与「无 code 入参的单例策略接口」互斥**（接口唯参 `now`、`:239-251` 返回单例、`:179-180` 强制对象同一性；而 `getFuturesSession` 逐品种不同：中金所无夜盘、国债 15:15、`RB0` 23:00、`AU0` 02:30）⇒ `§5:356` 等价性门禁不可落码，待确认风险 2 的品种级夜盘回归通道未闭环。P3：① **P3-1 `:223`/`STATUS.md:16` 引用未定义标识 `isFutureTrading`**（真实导出 `isFutureTrading`，`session.js:145`）→ 落码 `SyntaxError`（Round 9 P3-1 同族第 3 次）；② **P3-2 §5 CL/NG 断言（`:374-378`）含未绑定伪变量 `sinaClPayload`/`primaryCl`，且 `< 0.6pp` 为上游决定量**（实测恒等式 `\|ΔchangePercent\| ≡ \|今日基差−昨日基差\|`：`5.042%−4.790%=0.252pp` ≈ 同刻 `0.236pp`；16 分钟内 `0.41pp`→`0.236pp`→`0.306~0.356pp`）。**待确认风险**：§3.1 `:106-108` 的 `hf_*` 准入契约未登记执行落点。**通过项**：Round 9 三项缺陷主项闭环（硬区间删除并改为 `field7` 绑定、`parseSinaGlobalFuture` 已定义、HSI 声明收敛为「日盘结算后」且 18:38 复测三源同为 `24688`、港美股 `÷1000` 实测成立）；文档结构自检通过、清单 17 行行号相符、10/10 内部比值恒 `1.00000`。**未验证项**：`:32` 新增括注「复用后 `< 100ms`」本机 8 连发实测稳态 `207~211ms`；「连续两日门禁一致」单日不可验；受限网络轮转时序不可复现；Phase 2/3 的 `klt=1` 根数与腾讯字段数未重采。本轮 diff 全为 `.md`，未触碰产品代码与测试 |
| 2026-09-16 | [`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md) | **架构方案与可行性调研（v10，round 10 待审版）**：国际期货（外盘期货）与国际股票（港美股）接入方案 v10，全面闭环 Round 9 审查 1×P2 + 3×P3 缺陷与两项待确认风险。① 消除 CL/NG 上游基差硬门禁，断言备源昨结绑定 field7 及切换后涨跌幅跳变 <0.6pp 保证百分比平滑；② 消除未定义标识 resolveFallbackQuote，在 parser.js 显式导出并统一使用 parseSinaGlobalFuture；③ 收敛 HKFE 声明为「日盘结算后（16:30 后）」对齐采样时段，追加 18:26 偏差归零复测样本；④ 建立港美股东财 qt 端点量纲表（div1000），parseEastmoney 按 f107 分派 A 股 div100 与港美股 div1000 默认除数，§5 补齐港美股量纲断言；⑤ 明确自选池准入策略排除 hf_* 备用源符号；⑥ ChinaFuturesSessionStrategy 显式委托既有 futures/session.js 并在 §5 设置等价性门禁 |
| 2026-09-16 | [`2026-09-16-workbuddy-code-review-round9-handoff.md`](2026-09-16-workbuddy-code-review-round9-handoff.md) | **独立代码审查（国际品种方案 Round 9 复查）**：被审 HEAD `4beb59c`，基准 `45593a5`，本轮实际审查增量 `b344926..4beb59c` = 4 文件 / +256 −58（纯文档，主方案 361 → 379 行）。**未通过：1×P2 + 3×P3**。P2：① **P2-1 §5 `:350-351` 的 CL/NG 跨源断言把漂移的上游合约基差固化为实现门禁 `diff ∈ [4.0%, 5.0%]`** —— 观测序列 **4.1558% ~ 5.1%**（Round 1 `:81` 早段 `4.97%~5.1%` 已越上界；Round 8 17:25 `CL 4.75%~4.79%`/`NG 4.27%~4.49%`；本轮 18:26 `CL 4.605%`/`NG 4.1558%`，NG 跌出本轮登记区间「4.2%~4.5%」），下界断言非实现的正确性属性（上游换月收敛即必然转红），且 `:351` 与 §3.4.2 `:279`「严禁按 1:1 比对绝对价格」互斥。P3：① **P3-1 §5 `:353` 引用未定义标识 `resolveFallbackQuote`**（全仓+文档仅 1 处命中，未纳入 §3.1.2 清单，`sinaPayload` 未绑定）→ 无法落码（同 Round 3 P3-1 族）；② **P3-2 HKFE 复采表（`:271-276`）未覆盖 Round 8 要求的「日盘/夜盘各 ≥3 次」**（16:35 为收盘后、17:07 在夜盘开盘前、17:23/17:28 同值且沿用既采 → 日盘盘中 0 样本），`:59` 的「日盘常规交易时段 1:1」缺盘中支撑（本轮 18:26 复测东财 `f60 = 24688 = trends2.preClose`、新浪收敛至 24688，机制方向成立）；③ **P3-3 Phase 2/3 港美股未登记东财 `qt` 量纲**（实测 `116.00700`/`105.AAPL`/`106.BABA`/`105.TSLA` 一律 **×1000**，同源 `trends2`/腾讯为真实单位），而 §3.1.2 行 5（`:175`）兜底为 `div100` → 按字面实现错 10 倍且 §5 无港美股量纲断言。**待确认风险**：`hf_*` 与 `GL_*` 同分类但无入池准入策略（并存两个水平报价、`hf_*` 无东财 secid）；`ChinaFuturesSessionStrategy` 未锚定既有 `getFuturesSession`（`tests/reviewFollowup.test.js:21-22` 仅 2 条用例）。**通过项**：Round 8 五项缺陷主项全部闭环（`:182` 与 `:238` 判据逐字一致、转义恢复 `\d`=8/`\|`=12 且三表列数 9/4/6 无畸形行、`sinaScale` 按真源回填、`:59` 表述限定、§5 补齐 `previous`）；§3.1.2 清单 17 行 `文件:行号` 逐条 `sed` 复原相符；独立验证 10 品种三源同刻探针（内部偏差 ≤0.0068%、`qt.f60/qtDivisor == trends2.preClose` 10/10 精确、`f169 == f43-f60` 10/10）+ 仓库外副本按方案字面实现后 §5 四条语音断言 **4/4 GREEN**、两处变异（`!hasExtendedHoursAssets && !allowed`、`!futures.length`）确定性转红、13:00 自动恢复成立。**未验证项**：受限网络「先失败→后轮转 `push2delay`」转移时序（本机 `push2his` 可达、`90.push2his` hang up、`push2` 恒 502，非同一出口画像）；`:32` 的 `<100ms` 延迟阈值；HKFE 日盘盘中样本；Phase 2/3 根数口径未重采。本轮 diff 全为 `.md`，未触碰产品代码与测试 |
| 2026-09-16 | [`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md) | **架构方案与可行性调研（v9，round 9 被审版）**：国际期货（外盘期货）与国际股票（港美股）接入方案 v9，全面闭环 Round 8 审查 3×P2 + 2×P3 缺陷与待确认风险。① 统一 voiceSchedule.js:28 判据为 if (cfg.enabled && enabled && !hasExtendedHoursAssets && session === 'after-close' && cfg.autoStopAfterClose) enabled = false;，严格保留 session === 'after-close' 维度杜绝午休误关回归；② 处理 CL 与 NG 跨源合约月差异，备源切换绑定新浪 field7 昨结重校基准，§5 补充合约差区间校验，HG 经 sinaScale=0.01 消除 100 倍量纲差；③ 恢复全文 22+ 处反斜杠转义（\d 与 \|），修复 §3.1.2 表格拆列与内容截断；④ 消除 HKFE §2.1 与 §3.4.2 互斥，补齐 4 次日夜盘复采记录表与 trends2.preClose 权威基准；⑤ 完善 §5 反向断言 previous 入参与午休只暂停断言；⑥ 核实 NQ/ES sinaScale=1.0 成立 |
| 2026-09-16 | [`2026-09-16-workbuddy-code-review-round8-handoff.md`](2026-09-16-workbuddy-code-review-round8-handoff.md) | **独立代码审查（国际品种方案 Round 8 复查）**：被审 HEAD `b344926`，基准 `45593a5`，本轮实际审查增量 `8a05917..b344926` = 4 文件 / +279 −63（纯文档）。**未通过：3×P2 + 2×P3**。P2：① **P2-1 §3.1.2 行 12（`:182`）与 §3.2.2 项 2（`:238`）对 `voiceSchedule.js:28` 的关闭判据互斥**——清单写 `!hasExtendedHoursAssets && !allowed`（丢掉原有的 `session === 'after-close'` 维度），正文保留 `session === 'after-close'`，按清单字面实现后北京 12:00 午休纯 A 股池被永久关闭（复现 2026-09-09 M1 回归）；② **P2-2 §3.1.1 `sinaScale` 列把东财数值回填为「新浪」值（`GL_CL0`/`GL_NG0` 实测证伪）→ §5 `:335` 的 10/10 跨源 ≤0.5% 断言对两品种不可满足**——`CL` 偏差 4.75%~4.79%、`NG` 偏差 4.27%~4.49%，文档登记的新浪值等于同刻东财末根，§3.4.2 亦无 CL/NG 处置；③ **P2-3 删除全文 22 处反斜杠转义致 `\d` 退化为字面 `d`、§3.1.2 表 5 行被拆列致渲染丢内容**。P3：① **P3-1 Round 7 待确认风险 1 闭环不完整**（§2.1 与 §3.4.2 互斥，夜盘实测偏差 7 点持续）；② **P3-2 §5 `:346` 反向断言缺 `previous` 入参致按字面不可满足**。待确认风险：NQ/ES 回填存疑。门禁 843/843 全绿 |
| 2026-09-16 | [`2026-09-16-workbuddy-code-review-round7-handoff.md`](2026-09-16-workbuddy-code-review-round7-handoff.md) | **独立代码审查（国际品种方案 Round 7 复查）**：被审 HEAD `8a05917`，基准 `45593a5`，本轮实际审查增量 `5e01a90..8a05917` = 3 文件 / +42 −21（纯文档，主方案 316 行）。**未通过：2×P1 + 2×P3**。P1：① **P1-1 §3.1.1 端点报价单位登记表对 8 个非 A50 品种失真，§3.1.2 行 5 与 §5 断言仅覆盖 A50** —— 表 `:99-106` 的 `GL_CL0/GC0/SI0/HG0/NG0/NQ0/ES0/YM0` 八行均登记 `1:1:1 (均为 1 点)`，而两轮独立采样（`push2delay`，同 secid、同刻）实测 `qt.f43 / trends2末根` = `CL 10445/104.45=100`、`GC 43717/4371.7=10`、`SI 65060/65.06=1000`、`HG 64865/6.4865=10000`、`NG 2903/2.903=1000`、`NQ 2908461/29084.61=100`、`ES 767222/7672.22=100`、`YM 52579/52579=1`、`A50 143710/14371=10`、`HSI 24682/24682=1`（比值恒为 10 的整数次幂、两轮逐项一致），即 `qt` 侧原始缩放**逐品种不同**（0.0001/0.001/0.01/0.1/1 共 5 档）；按 §3.1.2 行 5 字面实施（仅 A50 ÷10，其余沿用既有 `div100`，`src/js/parser.js:91/97-99/101/114-115`）会使 `GC/SI/HG/NG/YM/HSI` 六个品种的 `price/prevClose/open/high/low/change` 错 **10–100×**（黄金 `437.17` 对真值 `4371.7`、道指 `525.79` 对 `52579`、恒指 `246.82` 对 `24682`），经 `state.quotes` → 自选表/TTS/K 线全链路用户可见；本轮修复删除了仅有的横向兜底句「其余外盘与港美标的按自身口径解析」使其余 9 品种再无缩放指令，而 `§5:303` 断言只枚举 A50 的 `price/prevClose/open/high/low`、`§5:299` 只校验三端点「取数成功」→ 与 Round 6 P2-1 根因（登记粒度 vs 实施粒度不一致）同源、横向未闭环（`:99-106` 文案自 Round 5 起未回真源复测）。交叉源佐证：`hf_GC 4373.530`、`hf_SI 65.087`、`hf_YM 52572.060`、`hf_HSI 24686.700`、`hf_CHA50CFD 14375.400` 均与除以对应除数后的端点值同级一致。附加实例：`GL_HG0` 的新浪 `hf_HG` 以美分/磅（`649.300`）报价、东财 `101.HG00Y` 以美元/磅（`6.4865`）报价，差 **100×**，与 `§3.1.1` HG 行「东财trends:新浪 = 1:1」及 `§3.4.2:248`「主备源切换…无需在切换逻辑中嵌入特殊分支」冲突。② **P1-2 §3.2.2「绝不误关语音」在 `src/js/services/voiceSchedule.js:28` 不成立，§5 断言零判别力** —— `:28`（`if (cfg.enabled && enabled && !futures.length && session === 'after-close' && cfg.autoStopAfterClose) enabled = false;`）的判据**不含 `allowed`/`eligibleCodes`**，而 `futures = list.filter(isFutureCode)`（`:12`）对外盘/港美股代码恒为空（`isFutureCode('GL_CL0'/'GL_A50'/'hf_CL'/'hk00700'/'usAAPL')` 实测全 `false`，且 `§3.1.1:124-126` 以断言强制该性质）→ 自选池含 `GL_*`/`hf_*`/`hk*`/`us*` 时北京 ≥15:00 即 `enabled = false`，`voiceController.js:93-94` 随即 `saveSettings({enabled:false})` **持久化关闭**并经 `:100-101` 播「已收盘」，`:126` 每 30s 复评使手动重开在下一拍再被关；真实模块探针（仅调用纯函数，未改产品代码）实测 16:00 与 22:00、`['sh600519','GL_CL0']` 均得 `{enabled:false, eligibleCodes:[], timerShouldRun:false, pauseReason:'closed', transitionNotice:'已收盘'}`，**即使 §3.2 策略正确放行 `GL_CL0`（`allowed=true`）该分支仍独立成立**；`§5:312`「会话与语音隔离断言」只校验 `resolveVoiceScheduleAction` 的 `pause/autoStop`，而 `marketSession.js:89` 在 `allowed` 为真时恒 `return idle`（实测 `{pause:false, autoStop:false, notice:null}`）→ 缺陷存续时断言照样通过；§3.1.2 的「全链路改造点清单」未纳管 `src/js/marketSession.js:46-52`（`getVoiceEligibleCodes` 二元分派）与 `src/js/services/voiceSchedule.js:12/28`（Round 1 P2-4「混合自选 22:00 播『已收盘』并永久关闭语音」的闭环未彻底）。P3：① **P3-1 `toEastmoneySecId`（`src/js/parser.js:34-40`）未出现在方案任何改造点** —— `inferMarket` 仅识别 `^(sh|sz|bj)\d{6}$` → `toEastmoneySecId('GL_A50') === null` → `buildEastmoneyUrl`（`src/js/api.js:50`）、`buildEastmoneyTrendsUrl`（`:56`）、`buildEastmoneyKlineUrl`（`server/klineService.js:41`）随之为 `null`，外盘东财 `qt`/`trends2`/`kline` 三端点全部不可达，§3.4.1「东财第一源」整链失效；§3.1.2 行 4 只要求 `normalizeCode` 输出 `{ code, type, market }` 而未定义 `market` 语义与消费方。② **P3-2 本轮新增的 `type='futures_global'` 未同步 `type` 判别消费点** —— `src/js/tts.js:217-218`、`src/js/alert.js:87-88`（`quote.type === 'future' ? '' : ' 元'`）在 `type='futures_global'` 且 `isFutureCode===false` 时落 else 分支 → 外盘播报尾附「元」（如「纽约黄金 4371.7 元」）、`decimals` 回落 2 位；`src/js/views/monitorTableView.js:95/339`、`src/js/kline.js:522`、`src/js/services/batchExportService.js:102` 同类，§3.1.2 清单（自称「覆盖全仓 11 处真实行号与消费点」）与 §3.5 均未列。**通过项**：Round 6 三项缺陷主项**全部闭环** —— `§3.1.2:164` 已枚举 `f43/f44/f45/f46/f60/f169` 并纳入 `(d.f107, d.f57)` 反查 `globalCatalog` 输出 `GL_*` 与 `type='futures_global'`（`§5:304` 补代码身份断言）；§2.1 字段 2/3 已按真源回改为「买一价（99.940）/卖一价（99.960）」（本轮实测 `hf_CL` 第 2/3 位 `99.900/99.910` 夹住最新价 `99.964`、`hf_HSI` `24687/24689` 夹住 `24686.7`，确为买卖价）；待确认风险 1（`proxyService.js:10, 31`）已补列；`§1.1` 引用已校准为 `§3.2`。另：`§3.1.1` 既有导出断言块独立实跑 **9/9 PASS**（含 `parseFutureInput('SI0')→gfex/工业硅连续`、`isFutureCode('GL_SI0')===false`、`inferMarket('GL_SI0')===null`）；§3.1.2 清单 11 行的 `文件:行号` 逐条 `awk` 复原**全部相符**（`utils.js:76-86`、`marketData.js:18-22/160/194`、`proxyRoutes.js:10/13`、`proxyService.js:10/31`、`klineService.js:40/95`、`parser.js:11-25/42/64/93-127/106-107/129`、`api.js:30-31/52/66`、`kline.js:210/218/232/326-341/347/360-367`）；A50 端点单位结论真源复测成立（`qt` 全价格字段 ×0.1 与同源 `trends2`、新浪逐项同级一致）。**待确认风险**：`134.HSI_M` 的 `f60` 在日/夜盘切换后与新浪昨结不再相等（17:07 实测东财 `f60=24688`、`trends2.preClose=24688` 对 新浪字段 7 `24676.000`，差 12 点/0.05%；同交易日 16:35 两侧同为 24676），与 `§2.1:59`、`§3.4.2:247`「完全 1:1 精确相等…无需在切换逻辑中嵌入特殊分支」不符，需日盘与夜盘各复采 ≥3 次定论。**未验证项**：受限网络「先失败→后轮转 `push2delay`」的转移时序无法在同一出口复现（承接 Round 6）；`proxyService.js:35` 的 15s 超时与多主机轮询叠加的预算约束方案未给；Phase 2/3 港美股字段数与 secid 未重采。本轮 diff 全为 `.md`，未触碰产品代码与测试 |
| 2026-09-16 | [`2026-09-16-workbuddy-code-review-round6-handoff.md`](2026-09-16-workbuddy-code-review-round6-handoff.md) | **独立代码审查（国际品种方案 Round 6 复查）**：被审 HEAD `823f3df`，基准 `45593a5`，本轮修复增量 `bb2e47d..823f3df` = 3 文件 / +71 −507（纯文档；主方案 769 → 315 行）。**未通过：2×P2 + 1×P3**。P2：① **P2-1 §3.1.2 `:164` 单位归一化只枚举 `f43/f60/f169`**——`§3.1.1 :107` 按「**端点**报价单位」登记 A50 = `0.1点 : 1点 : 1点`，而实施指令只覆盖 3 个字段，`f44/f45/f46` 仍经 A 股 `div100`（`src/js/parser.js:99/114/115`）→ 真源同时刻（`push2delay` 16:35，`104.CN00Y`：`f43=143650 f44=144180 f45=141980 f46=142670 f60=142710 f169=940 f170=66`）模拟「按 `:164` 字面实现」得 `open=1426.7`/`high=1441.8`/`low=1419.8`（真值 `14267/14418/14198`，新浪 `hf_CHA50CFD` 字段 8/4/5），且 `open` 与 `prevClose` 量纲不再一致 → `openChangePercent` 由 `-0.028%` 变 **`-90.003%`**（`monitorTableView.js:131/360` 自选表开盘列 `1,426.70 (-90.00%)` 用户可见；`momentumController.js:347` 以 `q.high` 追踪最高价 → `maxHigh` 恒被 `price` 覆盖、`pullbackPercent` 恒 0）；`§5:303` 断言只校验 `price/prevClose`，零判别力。② **P2-2 改造清单未覆盖 `parseEastmoney` 的 `code` 身份推导**——`src/js/parser.js:106-107` 对非 A 股恒走 `('sz') + f57` 兜底（`normalizeCode('CN00Y')===null`、`normalizeCode('HSI_M')===null`），真源 payload 实跑仓库真实 `parseEastmoney` 得 A50 `code='szcn00y'`/`price=1436.5`、HSI `code='szhsi_m'`（期望 `GL_A50`/`GL_HSI`），而 `src/js/controllers/monitorController.js:44` 以 `currentCodes.has(quote.code)` 为写入门禁 → 报价永不进 `state.quotes`、图表报价合并整段跳过、语音无播报，`§3.4:243`「消费层全面 1:1 精确对接」不可达；`api.js:161-167` 的 `gotCodes`/`missing` 判定恒为真，每轮刷新额外触发兜底请求。P3：① **P3-1 §2.1 `:37-38` 字段 2/3 被改标为「买一量（9.940）/卖一量（9.960）」**（**本轮新引入回归**，`git show bb2e47d:<doc> \| sed -n '37,38p'` = 「买一（99.940）/卖一（99.960）」；Round 5 两项 P3 均未要求改此处），与同段 `:33` payload `99.940,99.960` 及真源矛盾——`hf_CL` 第 2/3 位 `99.980/99.990` 恰夹住 `index4=100.610`（最高）与 `index5=99.050`（最低），`hf_CHA50CFD` 第 2 位 `14365.000` **等于同刻 `trends2` 末根**，真正的量在 `index10/11`（`hf_CL` = `2/7`、`hf_CHA50CFD` = `90/3`）→ 2/3 为买一价/卖一价。通过项：Round 5 的 P2-1 结构（`awk '/^#/'` 得 H1/`## 1.`~`#### 3.1.2` 各 1 次、章节严格单调、§3.1.2 表 11 行全部以 `|` 闭合）、P2-3 出网点（`server/proxyRoutes.js:10/13`、`server/klineService.js:40` 已入清单；逐主机实测 `push2his`/`90.push2his`/`1.push2`/`push2` 不可达、`push2delay` 的 `qt`(含 A 股 `1.600519`)/`trends2`/`kline(klt=1)` 三端点全部 `rc=0`）、P3-1 契约锚点（按 `:163` 文本逐字实现后 §3.1.1 NOTE 10 条断言 **10/10 PASS**，含 `unknown_foo→'stock_cn'`）、P3-2 根数（全篇仅 `:59` 一处权威表述，`16:18 实测 trends2 964 / kline 963` 与本轮 16:35 实测 `976/975` 按 HKFE 16:30 前每分钟 1 根自洽）经独立复测**全部成立**；`§3.1.1` 既有导出断言块（`instrument.js`/`parser.js`）**9/9 PASS**；`§3.4` A50 昨结 1:1 成立（`142710 × 0.1 == 14271 == trends2.preClose == 新浪字段 7`）；`§5:301` 容差下 `143650 × 0.1 == 14365.0 == trends2.last`。待确认风险：§3.1.2 行 3（`:162`）只列静态 target 常量（`proxyRoutes.js:10,13`、`klineService.js:40`），而「多主机轮转 + 失败重试」的落点须在 `server/proxyService.js:10/31`（`resolveProxyTarget` 返回单一 `target.url`、唯一的 `fetchWithTimeout` 在此）→ 按清单字面只改常量不改进程循环会得到「换了 host 但没有轮转/兜底」。附带非阻塞建议：`:15` 本轮把引用由 `§3.2` 改为 `§1.2`，而 `docs/plans/2026-09-03-futures-requirements-technical-plan.md` 的 `### 3.2 本方案不包含`（第 52 行）才含「外盘期货、外汇、数字货币」，`## 1. 背景与现状` 无子节（不存在 §1.2），`§12.1 已确定` 含「不在本轮实现期权、外盘和下单」。本轮 diff 全为 `.md`，未触碰产品代码与测试 |
| 2026-09-16 | [`2026-09-16-workbuddy-code-review-round5-handoff.md`](2026-09-16-workbuddy-code-review-round5-handoff.md) | **独立代码审查（国际品种方案 Round 5 复查）**：被审 HEAD `cfd243c`，基准 `45593a5`，本轮修复增量 `f637dd9..cfd243c` = 3 文件 / +529 −23（纯文档）。**未通过：3×P2 + 2×P3**。P2：① **P2-1 方案正文 §1–§3.1.2 被整体重复 4 份、§3.1.2 契约表行被切成 4 段**——`awk '/^#/'` 得 `# 国际期货…` H1 4 次（`:1` 及 `:157`/`:313`/`:469` 的行尾拼接）、`## 1./## 2./### 3.1/#### 3.1.1/#### 3.1.2` 各 4 次；`src/js/parser.js:11-25` 契约行被切成 `:157`①/`:313`②③/`:469`④/`:625`⑤⑥ 四段，与约 465 行重复正文（全文 60%）交错；`:157`/`:313`/`:469` 三个表行均未闭合（行尾直接拼接下一份副本的 H1 标题），第三/四份副本的 §3.2 之前正文完全一致（`diff` 仅首行不同）→ 交付物核心契约表无法就位阅读，4 份 §2 实测事实并存必致后续数值漂移，4 个同名 H1 使锚点引用歧义。② **P2-2 A50 10× 单位约定登记在错误边界**：`push2delay` 同时刻真源实测 `104.CN00Y` 的 `qt/stock/get` `f43=143900`/`f60=142710`，而同源 `trends2` 末根 `14390.0`/`preClose=14271`、`kline(klt=1)` 末根 `14390.0`，新浪 `hf_CHA50CFD` `14392.800`/`14271.000` → 比值 `f43/trends2末根 = 10.000`、`f60/trends2.preClose = 10.000`、`trends2末根/sina.f0 = 0.99991`，即 **10× 分歧在东财内部（qt ↔ trends2/kline）**、trends2/kline 与新浪同级；方案把系数登记为「源间(东财:新浪)」并在「备源切换」时应用（`:102`/`:709`）→ §3.4.1「qt 与 trends2 同源共享 preClose…最新价/涨跌幅/分时末端 100% 吻合」对 `GL_A50` 被证伪（143900 vs 14390.0；142710 vs 14271），默认主源路径经 `src/js/parser.js:97-98` 的 `div100` 会输出 `1439.00`/`1427.10`（真值 1/10，与同源分时图差 10 倍，语音同样 10 倍偏小），若把 `×0.1` 误施于 `trends2`/`kline` 则图表反向缩小 10 倍，而 §5 `:762` 的验收断言只校验备源昨结、三种错位均拦不住（对照 `134.HSI_M` 为 1:1：`qt.f43=24701`、`trends2` 末根 `24701`、`f60=preClose=24676`、新浪 `hf_HSI` 昨结 `24676.000`）。③ **P2-3 Round 4 P2-2 闭环不完整：`push2delay` 未落到浏览器可见的东财出网路径**：§2.1 `:206` 要求「对 `qt/stock/get` 增加 `push2delay` 兜底」，但 §3.1.2 只列 `server/marketData.js:18-22, 160, 194` 与 `server/klineService.js:95`（服务端 `/api/cache/*` 路径使用），浏览器侧 `src/js/api.js:52`（`/api/eastmoney/qt/stock/get`）、`src/js/api.js:66`（trends2）、`src/js/kline.js:73`（kline）经 `server/index.js:376 → proxyService.js:10 → proxyRoutes.js:10`(`push2his`)/`:13`(`push2`) **静态 target 直连**；本轮逐主机实测 `push2his.eastmoney.com`/`90.push2his.eastmoney.com`/`1.push2.eastmoney.com` 的五端点全部 `fetch failed`、`push2.eastmoney.com` 恒 502，**仅 `push2delay.eastmoney.com` 三端点 `rc=0`**（`m:134` `trends2` 964/`kline` 963、`m:104` 1189、`qt` rc=0、A 股 `1.600519` 亦 rc=0）→ §5:760「Phase 1 全部 10 个品种均能通过 qt+trends2+kline 三端点拉取」在方案自述的受限网络下仍不可满足，§3.4.1「东财第一源」静默退化为新浪单源；`grep -rn "push2his\|push2\.eastmoney" src server` 显示出网点尚有 `klineService.js:40`（默认 host）与 `proxyRoutes.js:11/13/14` 未纳入清单。P3：① **P3-1 §3.1.2 契约 ①③④ 丢失 `$` 锚点**（截断点恰落在锚点处：`:157` 截于 `^hf_[A-Za-z0-9_]+`、`:313` 截于 `^(?:hk\|r_hk)\d{5}`、`:469` 截于 `^(?:us)?[A-Za-z]+`）→ 按方案文本逐字实现 `inferAssetType` 后，本轮新增的 10 条断言 **9 通过 1 失败**（`assert.equal(inferAssetType('unknown_foo'), 'stock_cn')` 实得 `'stock_us'`，因 `unknown_foo` 命中未锚定的 ④ 且 `isFutureCode` 为 false），而同 commit 的 `STATUS.md:10` 记录的是带 `$` 的版本（`^hf_[A-Za-z0-9_]+$`/`^(?:hk\|r_hk)\d{5}$`/`^(?:us)?[A-Za-z]+$`）→ 两文件对同一契约记载互斥（Round 4 P2-3 的 `hf_`/`r_hk` 值域**内容层已闭环**）。② **P3-2 §2.1 新增主机/根数断言作用域越界且不可复现**：`:206`「能完整拉取 `m:134`（`trends2` 与 `kline` **938 根**）」实测此时刻为 `trends2` 964（首根 `2026-09-15 17:00`）/`kline(klt=1)` 963（首根 `17:01`），Round 3 记 914/913、Round 4 记 935/938，且与同文 `:213` 的「`trends2` n=914，`kline(klt=1)` n=913」互斥；两条端点口径天然相差 1~3 根，写成同值结构上不成立；`:214`「实测在各类网络环境下 `push2delay` 均能稳定可达」超出实测范围（仅本机单网络）。通过项：Round 4 P2-2 的**事实层**逐主机实测成立、Round 4 P2-3 的 `hf_`/`r_hk` 值域内容层闭环、`server/marketData.js:14-16/18-22/160/194`、`server/klineService.js:91-95/95`、`server/utils.js:76-86` 引用逐条相符、`m:134`/`m:104` 三端点在 `push2delay` 全部 `rc=0`；本轮 diff 全为 `.md`，未触碰产品代码与测试 |
| 2026-09-16 | [`2026-09-16-workbuddy-code-review-round4-handoff.md`](2026-09-16-workbuddy-code-review-round4-handoff.md) | **独立代码审查（国际品种方案 Round 4 复查）**：被审 HEAD `cb49106`，基准 `45593a5`，范围 6 文件 / +839 −2（纯文档；本轮修复增量 `7781fe1..cb49106` = 3 文件 / +46 −20）。**未通过：3×P2（无 P3）**。**P2-1 §3.4.2 `:219` A50 合约锚点「完全一致」被真源证伪**：文档写「新浪 `hf_CHA50CFD` 昨结（14271.000）与东财 `104.CN00Y` `f60`（142710）完全一致」，本机同时刻实测 `EM 104.CN00Y → rc=0 f58=A50期指当月连续 f60=142710 f43=143980`、`SINA hf_CHA50CFD → 最新14400.000 field7(昨结)=14271.000`，两值**相差 10 倍**（`142710/14271.0 = 10.0007`、`143980/14400.000 = 9.9986`），÷10 后才对齐；同段 `hf_HSI` 一档确为 1:1（`EM 134.HSI_M f60=24676` 对 `SINA hf_HSI field7=24676.000`）→ 同一方案内两品种缩放系数不同（HSI=1、A50=10）却未登记任何"市场 104（SGX）以 0.1 点为单位"的约定，按 §3.4.2 字面实现备源切换会使 `GL_A50` 昨结基准 10× 错位（涨跌幅约 1000% 量级），与 §3.4「保证第一源与备源切换时价格基准无缝对接」/§3.4.1「100% 吻合」冲突；根因为单点比对后横向套用（`142710` 系沿用 Round 3 报告 `:9` 与 `INDEX.md:44` 中未经数值校验的等式「14271.000 = 142710」）。**P2-2 §2.1 `:58` 新增主机断言「`push2delay` 与 `push2his` 实测均可稳定拉取 `m:134` 与 `m:100-104` 的 `trends2` 与 `kline`」不可复现**：对 `134.HSI_M` 的 `trends2` 逐主机实测 `push2his.eastmoney.com` / `90.push2his.eastmoney.com` / `1.push2.eastmoney.com` 全部 `UND_ERR_SOCKET`（4/4）、`push2.eastmoney.com` 恒 502，**仅 `push2delay.eastmoney.com` 返回 200 `rc=0`（trends=935）**；配套可判别性对照：`push2delay` 对 `m:134`/`m:100-104`/`1.600519` 的 `kline(klt=101)`、`kline(klt=1)`（`134.HSI_M` 938 根）与 `qt/stock/get` 全部 `rc=0`，而 `qt/stock/get` 经仓库默认主机 `push2`（`server/proxyRoutes.js:13` → `push2.eastmoney.com`）对 `134.HSI_M` 与 A 股 `1.600519` **均 502**；仓库自身注释亦记载 `push2his` 主主机 `0/10` 失败（`server/marketData.js:14-16`、`server/klineService.js:91-92`），且 `EASTMONEY_TRENDS_HOSTS = ['push2his.eastmoney.com','90.push2his.eastmoney.com','90.push2his.eastmoney.com']`（`server/marketData.js:18-22`）与 `klineService.js:95` 的 hosts 数组**均不含 `push2delay`** → 按 §2.1 `:50`「复用仓库既有 `EASTMONEY_TRENDS_HOSTS`」实现则 Phase 1 全部 10 品种的东财 `trends2`/`kline`/`qt` 在本机不可达、§5 三端点验收断言无法满足；Round 3 待确认风险 1（"须逐主机补测并登记约束"）被未复现的实测断言"闭环"（与 Round 3 P2-1「把未完成的扫描升格为架构事实」同一失效模式，作用对象换为主机可达性）。**P2-3 §3.1.2 `:137` 新增 `inferAssetType(code)` 契约值域遗漏 `hf_` 形态**：按该契约逐字实现后逐值实测 `SI0→futures_cn`、`GL_SI0→futures_global`、`hk00700→stock_hk`、`usAAPL→stock_us`、`sh600519→stock_cn` 均正确，但 `hf_SI`/`hf_HSI`/`hf_CHA50CFD`/`hf_CL` **全部回落 `'stock_cn'`**（不属 `GL_`、不匹配 `sh\|sz\|bj\|hk\|us`，且 `isFutureCode('hf_SI')===false`、`parseFutureInput('hf_SI')===null` 实测，无分支可救），`r_hk00700` 同族回落 `stock_cn` → 与 §3.4.2 `:218`（"新浪 `hf_` 仅作为外盘**全量**实时报价备用源"）、§5 `:271`（"`GL_SI0` / `hf_SI` 准确映射为 COMEX 白银"）自相矛盾，`resolveSessionStrategy('hf_HSI')` 落 `chinaStockStrategy` → 22:00 返回 `after-close` 触发 `autoStop` 永久关闭语音并播"已收盘"（Round 1 P2-4 失效模式），方案"彻底杜绝回归"前提不成立；Round 3 P3-1 修复建议第 ② 项已明确要求「同步核对 `hf_` 形态的归属」，未闭环，且 `§3.1.1 :129` 的"**全值域**正交断言"措辞与枚举不符。通过项：Round 3 P2-1 主项（`GL_HSI` → `134.HSI_M`）经本机真源独立复测成立（`clist fs=m:134 → total=112`，含 `134.HSI_M\|恒生指数期货主力`、`134.HSIU6\|恒生指数期货2609`、`134.MHIU6\|小型恒生指数期货2609`、`134.HHIM7/HHIZ6/HTIM7/HBIM7` 等；`134.HSI_M` `qt rc=0 f58=恒生指数期货主力 f60=24676`、`kline(klt=1)` 938 根）；反例 `134.HSI00Y`/`134.MHI_M`/`104.CN0` 均 `rc=100 data:null`（可判别）；Round 3 P3-1 断言块改为 `src/js/futures/instrument.js`（`parseFutureInput`/`isFutureCode`，`instrument.js:3/5` 确为既有导出）与 `src/js/parser.js`（`inferMarket`）后**实跑 9/9 PASS**；P3-2 `getPriceLimit` `326-341` 与消费方 `kline.js:347`（`const lim = Number(limit) \|\| 10;`）经 `awk` 逐行相符；P3-3 美股字段「基准 71 + 偶发追加 2 尾部空字段至 73」经三轮采样复现（`usAAPL=73/71/71`、`usTSLA=73/71/71`、`usNVDA=73/73/73`、`usBABA=71/71/71`、`r_hk00700=78`；73 字段 payload `idx69–72` 均空、71 字段截至 `idx70`），§2.2 港股 78 字段盘中实测成立；`m:100–104` `clist` 规模 63/104/117/178/33 与 `m:134`=112 逐条复现；门禁 `npm test` 843/843（0 fail）实跑通过；本轮 diff 全为 `.md`，未触碰产品代码与测试。未验证项/非阻塞观察：作者侧网络对 `push2his` 族的可达性；`m:100–104` 9 个 secid 三端点沿用 Round 2/3 结论未逐个重跑；`m:134` 的 `trends2`/`kline` 根数（`:57` 载 914/913）为时间点快照（本轮同时刻 935/938）且与 §2.2/§2.3 的"+1"口径不同 |
| 2026-09-16 | [`2026-09-16-workbuddy-code-review-round3-handoff.md`](2026-09-16-workbuddy-code-review-round3-handoff.md) | **独立代码审查（国际品种方案 Round 3 复查）**：被审 HEAD `8a97cca`，基准 `45593a5`，范围 5 文件 / +659 −2（纯文档；本轮修复增量 `6b1b9b2..8a97cca` = 3 文件 / +134 −101）。**未通过：1×P2 + 3×P3**。P2：① **P2-1 `GL_HSI`「东财无恒指期货合约 → 纯新浪单源」被真源证伪**——东财 `m:134` 实收录恒指期货（`total=112`：`恒生指数期货2609/2610/2611/2612…`、`134.HSI_M|恒生指数期货主力`、`134.HSIU6|恒生指数期货2609`、`134.MHI*|小型恒生指数期货*`、`H股指数期货*`、`恒指波幅/股息/恒生科技指数期货*`）；`134.HSI_M` 三端点取数成立且两次采样一致（`qt rc=0`、`f58=恒生指数期货主力`、`f60=24676`、`trends2 n=914`、`kline(klt=1) n=913`），其 `f60` 与新浪 `hf_HSI` 昨结 24676.000 同源（§3.4 锚点前提在换源后仍成立），反例可判别（`134.HSI00Y`/`134.MHI_M` 为 `rc=100 data:null`）→ 方案把 Round 2 待确认风险 1「**不得在扫描未完成时断言全仓不存在**」的负向结论作用域裁剪为 `m:100–124`（恰好排除 `m:134`）后升格为架构事实，据此写死的「纯新浪单源 + 图表区『该品种暂无历史分时』」降级契约（§2.1 `:57`、§3.1.1 `:102`、§3.4.2 `:207-209`）与 §5 `:260` 验收断言均不成立，Phase 1 的 `GL_HSI` 被无必要降级（实测东财分时/K线可用）。P3：① **P3-1** 本轮新增的「真实有效的解析层隔离断言」仍引用仓库不存在的标识 `inferAssetType`（`§3.1.1 :108` 静态 `import` 实测 `SyntaxError: The requested module '…/src/js/parser.js' does not provide an export named 'inferAssetType'`；`parser.js` 实际导出 `calcPercent/inferMarket/normalizeCode/parseEastmoney/parseEastmoneyTrends/parseSinaFuture/parseTencent/parseTencentMinute/toEastmoneySecId`，`contractCatalog.js` 导出 `EXCHANGES/PRODUCT_MAP/parseFutureInput`；全仓 `grep` 仅命中文档），且方案未定义该 API、未纳入 §3.1.2 九处清单 → Round 2 P3-1 验收标准「被引用标识与仓库实际导出一致」未达成（断言另一半 `parseFutureInput('SI0')` → `gfex/工业硅连续` 已实测成立）；② **P3-2** §3.1.2 行 9 新增的消费方行号 `kline.js:345` 与声称逻辑不符——`345` 是 `const pc = Number(prevClose);`，`Number(limit) \|\| 10` 实际在 **347**（`awk 'NR==345\|\|NR==347'` 证实），按 345 实施会漏改回退点使 P3-3 修复再次落空，Round 2「每条 `文件:行号` 可 `sed` 还原」标准未全表执行；③ **P3-3** §2.3 美股「实测 71 字段（取值条件：非交易时段实测）」不可复现——同处于美股非交易时段连续三轮采样得 `usAAPL=73/73/71`、`usNVDA=73/71/73`、`usTSLA=71/71/73`，差异为上游偶发追加的 2 个尾部空字段（73 字段 payload 的 `idx71=""`/`idx72=""`），一次探测即得 73，Round 2「字段数可由一次真源探测复现」未达成，须改为「基准 71 + 偶发尾部空字段至 73」并以下限守卫校验。通过项：`GL_A50`→`104.CN00Y`（`f58=A50期指当月连续`、`preClose=14271`）与其余 8 个 secid 双端点 2 轮复现（`trends2` 559–569）、§3.4 锚点（新浪 `hf_CHA50CFD` 昨结 14271.000 = 东财 `f60` 142710）、CME 结算窗口夏令时侧真源验证（`102.CL00Y`/`101.GC00Y` 首根 `06:00`、`05:00–05:59` 零 bar）与 CST 窗口算术自洽、`kline.js` `STOCK_CODE_RE` 210/218/232、港股 `trends2` 151/331 与「无 13:00 bar」、美股 `trends2` 391（21:30→04:00）/`klt=1` 390、`m:100–104` `clist` 规模 63/104/117/178/33、`m:100` 为国际指数现货市场（含 `HSI|恒生指数`）、§3.2.2 `default` 兜底已补；本轮 diff 全为 `.md`，未触碰产品代码与测试。待确认风险：`m:134` 的 `trends2/kline` 是否由 `EASTMONEY_TRENDS_HOSTS`（`server/marketData.js:18-22`）服务（本机仅 `push2delay` 可达，repo 主机 `UND_ERR_SOCKET`、`push2` 恒 502） |
| 2026-09-16 | [`2026-09-16-global-market-workbuddy-code-review-round2-handoff.md`](2026-09-16-global-market-workbuddy-code-review-round2-handoff.md) | **独立代码审查（国际品种方案 Round 2 复查）**：被审 HEAD `ea2bb5a`，基准 `45593a5`，范围 4 文件 / +465 −2（纯文档）。**未通过：2×P2 + 5×P3**。P2：① **P2-1 在新增 `m:100` 行原形复现**——东财 `m:100` 经 `clist` 全量枚举实为**国际指数**市场（63 只：`KOSPI200/N225/TWII/XIN9|富时中国A50/HSI|恒生指数/SPX/DJIA`…），非方案所称「新加坡/港期」；`100.CHA50CFD` 与 `100.HSI00Y` 在 `qt/stock/get` + `trends2` 双端点、多主机多轮重试恒 `data:null`；真实 A50 期指 secid 为 `104.CN00Y`（`f58=A50期指当月连续`，`trends2` n=1088）；`m:100–124` 全量扫描未发现恒指期货合约 → Phase 1 的 `GL_A50`/`GL_HSI` 无法取数，与方案自身 §5.2「10 品种 secid 非空」验收断言及 §3.4「东财单源优先」冲突；② **P2-2 连带回归**——本轮把 CME 每日结算窗口固化为单一「北京 05:00-06:00」并**删去 Round 1 原文的冬季变体「或 06:00-07:00」**，CME 结算为 CT 16:00–17:00（随 DST 平移），标准时换算为北京 `06:00-07:00`，故标准时期间会把北京 05:00-06:00（真实交易）当休市剔除 → 外盘分时断裂，与 §5.2「外盘分时无日内断裂」冲突。P3：① §3.1.1 冲突断言恒真（global 键查 domestic 表，`GL_` 前缀下不可能相交）且引用仓库不存在的 `DOMESTIC_PRODUCT_MAP`（实为 `contractCatalog.js:16 PRODUCT_MAP`）；② §3.1.2 表把 `kline.js:331` 错标为 `STOCK_CODE_RE`（真实位置 210/218/232，331 是 `getPriceLimit` 内部行）→ 会改错位置并漏改两个腾讯 K 线 URL 守卫；③ `getPriceLimit` 返回 `null` 被消费方 `kline.js:345 Number(limit)\|\|10` 静默回落，实测 `classifyKlineBar(it,100,null)` 与 `(it,100,10)` 输出完全相同 → 「彻底消除 ±10% 限价」不成立；④ §2.3 美股「73 字段」实测为 71（`usAAPL`/`usBABA` 均 71，港股 78 一致），未回真源复测；⑤ §2.2 港股「330 根 = 150+180」的（区间,根数）自相矛盾：午后**无 13:00 bar**（实测 `12:00 → 13:01`），分时端点全日应为 **331** 根，K 线端点才 330 根，而美股 391 取分时端点 → 两市场口径不一致。通过项：P2-1 主项（`m:101/102/103` 上 8 个 secid 双端点取数成立）、P2-2 命名空间与 `CHA50CFD`、P2-3 窗口参数化（A 股窗口与 `api.js:36-39` 逐值一致）、P2-4 分派器、P2-5 清单（9 行含服务端 2 行、5 正则行号相符）、P3-3 Gzip 基准 115.10 KB（实测 `gzipSync` 115,098 B 精确一致）、P3-4 持仓量纠偏；门禁 843/843。**报告文件名加任务作用域前缀，因模板名已被图表审查链占用** |
| 2026-09-16 | [`2026-09-16-workbuddy-code-review-round1-handoff.md`](2026-09-16-workbuddy-code-review-round1-handoff.md) | **独立代码审查（国际品种方案 Round 1）**：被审 HEAD `a2b8791`，基准 `45593a5`，范围 3 文件 / +197 −1（纯文档）。**未通过：6×P2 + 4×P3**。P2：① 东财 secid 规则 `102.${symbol}00Y` 经实测证伪——外盘期货按交易所分市场（`m:101` COMEX `GC00Y/SI00Y/HG00Y`、`m:102` NYMEX `CL00Y`、`m:103` 指数 `ES00Y/NQ00Y`），Phase 1 清单 8/9 品种取数为空，且方案所引 `push2.eastmoney.com` 对 `trends2` 恒 502；② `SI0` 与广期所工业硅标识冲突（实跑 `isFutureCode('SI0')===true`），`CHA500` 非有效代码（应为 `CHA50CFD`）；③ `api.js:36-39 INTRADAY_SESSION_RANGES` 与 `chartTimeToDate` 把美股 391 根（21:30-04:00）全滤空、外盘/港股分时断裂，§3.3 "Unix 时间戳天然不受跨日限制"被证伪；④ §3.2 缺"代码→策略"分派契约，`getVoiceEligibleCodes` 二元分派 + `resolveVoiceScheduleAction` 的 `after-close` autoStop 使混合自选 22:00 播"已收盘"并永久关闭语音；⑤ 服务端 `server/utils.js:78`/`server/marketData.js:160,196` 等 ≥8 处归一化未纳入改造面，`TENCENT_LINE_RE` 对 `v_r_hk00700`/`v_usAAPL` 匹配数为 0；⑥ 新浪 `hf_CL` 与东财 `102.CL00Y` 同时刻各档位偏离 4.97%–5.1%（不同合约 + 昨结基准互斥）。P3：恒指期货无路线图落点且会播报"美元"（实为港币）；`getPriceLimit` 对港/美/外盘返回 10%；性能红线"不显著膨胀"与"<150KB"自相矛盾（现值 367KB）；§2.1 "持仓量字段完备"对 CME/ICE 不成立（index 9 恒为 0）。通过项：`hf_` 15 字段映射（含 index 9）经 8 品种实测逐位正确、时段算术正确、`105/106/116` 与美股 391 根可复现、843/75 基数一致 |
| 2026-09-16 | [`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md) | **架构方案与可行性调研（v8，round 8 被审版）**：国际期货（外盘期货）与国际股票（港美股）接入方案 v8，全面闭环 Round 7 审查 2×P1 + 2×P3 缺陷与待确认风险。① 登记 10 品种 qtDivisor 与 sinaScale，parseEastmoney 全价格字段逐品种归一化，美铜应用 0.01 消除 100 倍量纲差，§5 升级为 10/10 品种全量缩放断言与变异测试；② 重构 voiceSchedule.js:28 判据消除 15:00 误关语音，清单纳管 voiceSchedule.js 与 marketSession.js，§5 补齐 22:00 与 16:00 真实 decideVoiceSchedule 隔离双向断言；③ 纳管 toEastmoneySecId 支持 GL_*、hk*、us*；④ 同步 type='futures_global' 至 tts.js、alert.js 等 5 处消费点；⑤ 处置 HKFE 日夜盘昨结衔接并校准快照数值与函数名定位 |
| 2026-09-16 | [`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md) | **架构方案与可行性调研（v7，round 7 被审版）**：国际期货（外盘期货）与国际股票（港美股）接入方案 v7，全面闭环 Round 6 审查 2×P2 + 1×P3 缺陷与待确认风险。① 扩充 A50 端点报价单位归一化至全部价格字段（f43/f44/f45/f46/f60/f169 均 ÷10），彻底消除 openChangePercent 畸变为 -90% 缺陷；② 纳入 parseEastmoney 反向规范代码身份推导（根据 (f107, f57) 输出 GL_* 代码与 futures_global 类型），杜绝报价在 monitorController 写入门禁被丢弃；③ 纠偏 §2.1 新浪字段 2/3 为买一价（99.940）与卖一价（99.960）；④ 显式将 proxyService.js:10, 31 纳入代理出网轮转与重试清单；⑤ 校准 §1.1 历史引用为 §3.2 与 §12.1。**round 7 复审判定：未通过，2×P1 + 2×P3** —— ① `§3.1.1:99-106` 八行「1:1:1 (均为 1 点)」对 `qt` 侧不成立（两轮同刻实测 `qt.f43/trends2末根`：CL=100、GC=10、SI=1000、HG=10000、NG=1000、NQ=100、ES=100、YM=1、A50=10、HSI=1），沿用 `div100` 使 GC/SI/HG/NG/YM/HSI 价格错 10–100×，且本轮删除「其余外盘与港美标的按自身口径解析」后其余 9 品种再无缩放指令、`§5:303` 只断言 A50；② `§3.2.2`「绝不误关语音」被 `src/js/services/voiceSchedule.js:28` 的 `!futures.length && session==='after-close'` 分支证伪（判据不含 `allowed`/`eligibleCodes`，混合自选北京 ≥15:00 即 `enabled=false` 持久化 + 播「已收盘」；`§5:312` 断言在 `allowed=true` 时恒得 idle、零判别力）；③ `toEastmoneySecId`（`src/js/parser.js:34-40`）未纳入改造点致外盘东财三端点全返回 `null`；④ 本轮新增 `type='futures_global'` 未同步 `tts.js:217-218`/`alert.js:87-88` 等单位判别消费点 |
| 2026-09-16 | [`2026-09-16-global-market-feasibility-and-architecture-handoff.md`](2026-09-16-global-market-feasibility-and-architecture-handoff.md) | **架构方案与可行性调研（v6，round 6 被审版）**：国际期货（外盘期货）与国际股票（港美股）接入方案 v6，全面闭环 Round 5 审查 3×P2 + 2×P3 缺陷。① 彻底清除结构性重复正文，全篇严格单份正文与闭合成表；② A50 10× 单位差异登记为东财内部端点属性，落实 `parseEastmoney` 入口归一化消除 1/10 错位；③ 将浏览器代理出网路径 `proxyRoutes.js:10, 13` 与 `klineService.js:40` 纳入多主机轮转与 `push2delay` 兜底；④ 契约正则完整补齐 `$` 终结锚点，全值域单测断言 10/10 PASS；⑤ 收敛根数表述为下限+采样时刻并限定受限网络实测范围。**round 6 复审判定：未通过，2×P2 + 1×P3** —— ① `§3.1.2:164` 单位归一化只枚举 `f43/f60/f169`，`f44/f45/f46` 仍走 `div100` → A50 的 `open/high/low` 仍为 1/10、`openChangePercent` 由 `-0.03%` 变 `-90.00%`（`§5:303` 断言零判别力）；② 清单未覆盖 `parseEastmoney` 的 code 身份推导（`parser.js:106-107`），真源实跑得 `code='szcn00y'`/`'szhsi_m'`，在 `monitorController.js:44` 的 `currentCodes.has(quote.code)` 门禁被整段丢弃；③ §2.1 字段 2/3 被改标为「买一量/卖一量」且示例值 9.940/9.960 与同段 payload 及真源矛盾（本轮新引入回归） |
| 2026-09-16 | [`2026-09-16-workbuddy-code-review-round2-handoff.md`](2026-09-16-workbuddy-code-review-round2-handoff.md) | **独立代码审查（Round 2）**：被审 HEAD `1d08f60`，基准 `a334468`。**全面审查通过（0 缺陷）**。Round 1 指出的 3 项 P2 缺陷实测全面闭环：① `isVolumeBarUp` 补齐 `pc > 0` 守卫，边界测试确认 null/负值/0 等阴线平盘全绿；② `chart.js:666` 显式传参 `'1m'` 恢复分时浮层 `HH:mm`；③ 假阴真阳用例补齐真实昨收链与守卫断言；变异验证 M-A 与 M-B 确定性拦截；门禁 `npm test` 843/843 + `npm run lint` 0 + `npm run build` 成功。双智能体对抗审查正式闭环 |
| 2026-09-16 | [`2026-09-16-chart-date-format-and-volume-color-handoff.md`](2026-09-16-chart-date-format-and-volume-color-handoff.md) | **缺陷修复与审查闭环**：图表时间轴中国习惯年-月-日格式化与成交量红绿规则修复及 WorkBuddy round 1 P2×3 闭环。① 注册 `zh-CN` / `yyyy-MM-dd` 本土化配置，日周月 K 线关闭 `timeVisible` 消除无意义的 `00:00`，十字线浮层与时间轴均输出中国习惯 `YYYY-MM-DD`（分钟线输出 `YYYY-MM-DD HH:mm`）；② 修复高开低走假阴线收涨（如新农开发 2026-09-11 见顶日收涨 +0.83%）与一字涨停在原 `close > open` 逻辑下被误染为绿柱缺陷，导出 `isVolumeBarUp(it, prevClose)`，补全 `pc>0` 严格守卫防止首根阴线平盘误判，并补全实时行情 Tick 的 `prevClose` 上下文；③ 修复分时图浮层显式传参 `'1m'` 恢复 `HH:mm`。门禁 `npm test` 843/843 + `npx playwright test e2e/chart.spec.js` 12/12 + `npm run lint` 0 + `npm run build` 成功 |
| 2026-09-15 | [`2026-09-15-voice-dedupe-toggle-handoff.md`](2026-09-15-voice-dedupe-toggle-handoff.md) | **功能交付**：语音播报「相同报价不重复播报」开关（`voice.skipUnchanged`，默认开=原行为），作为交易时段那一排第 5 个开关复用 `.schedule-toggle` 样式、不受「智能交易时段」禁用影响；缺键归一化回落 `true`（防迁移极性静默改行为）、关闭去重改走全量 `formatQuoteSpeech`（防"只勾名字则永不出声"）。交付中发现并修复 1 项真实接线缺陷：视图契约 `onChange(key, checked)` 与单参数 handler `(checked)` 错位 → 收到字符串 `'skipUnchanged'`（恒真）→ 真实浏览器点击无反应，已改签名 + key 守卫。判别力：在 `ac09dde` worktree 上 5 条新断言全红（其余 821 全绿）；变异验证确认可拦住"向 delta 传 null"的错解；E2E 修复前确定性失败。门禁 `npm test` 826/826 + `npx playwright test` 75/75 + `npm run lint` 0 + `npm run build` 成功 |
| 2026-09-15 | [`2026-09-15-close-snapshot-handoff.md`](2026-09-15-close-snapshot-handoff.md) | **功能交付**：停播提示后补播「最后一轮选中字段」。`decideVoiceSchedule` 新增 `finalCodes`（= 上一拍 `previous.eligibleCodes` 按当前订阅过滤），控制器 `speakCodes` 新增 `full`（无视 `skipUnchanged`、不写去重记忆），`applySchedule` 播完提示后立即调用；三类提示（`已收盘`/`中午休市`/日夜空档 `休市`）全覆盖，不加开关。关键约束（实测）：发提示那一拍 `eligibleCodes` 必为空故快照集合只能取上一拍；必须按订阅过滤；上一拍可播标的被全部退订时**连提示都不发**（更正了交付前口头汇报中的相反判断）。验证：新增纯函数测试文件 8 条 + 控制器 3 新增/2 更新，`141ddef` worktree 上 11 条全红（其余 826 全过）；E2E 掐掉补播调用做变异 → 确定性失败。门禁 `npm test` 837/837 + `npx playwright test` 75/75 + `npm run lint` 0 + `npm run build` 成功 |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round2-handoff.md`](2026-09-14-workbuddy-code-review-round2-handoff.md) | 独立审查 `76e9cd9`（代码落地阶段 round 2）：**未通过**。round 1 两项缺陷**主项实测闭环**：M1 变异（回退 `app.js:1511` 为 `!hasLimitUpRoot`）使用例 7 确定性变红（`:334` `pollTimerAlive` actual `false`，(b)(c) 端到端链路已补齐）、M6 变异（`chartRowController.js:391` → `getBeijingDate()`）使用例 5 确定性变红（`:241` 长度 actual 3 / expected 2）、门禁 `npm test` 820/820 + `npm run lint` 0 问题 + `npm run build` 成功。P3×2：①用例 5（`:180-250`）盘前段只走 `loadKline`（`:391`），`chartRowController.js:546-549`（`applyLiveTick`）注入点零覆盖 —— 唯一覆盖该路径的用例 4 固定时钟 `10:00`，此刻 `resolveStockChartDate(dates) === getBeijingDate()`（均 `2026-09-14`）结构性不敏感；变异 M7（仅改 `:546`）后 6/6 仍绿，而独立生产装配探针（真实 `limitUpChartMgr`、`09:00` 盘前、无日期 Tick）在 M7 下确定性捕获 `2026-09-14` 幽灵 Bar，证明该变异可观测可测，对照 round 1 报告 `:42-43`「`:391`（及 `:546`）」验收未闭环；②用例 7 `finally`（`:348-357`）只 `monitorCtrl.stopTimer()`，未停涨停看板定时器 → `state.limitUp.timer` 残留桩句柄 `1002`（串行不变量探针实测），而 `app.js:1520` 是 `state.limitUp.timer` 全仓唯一消费点且判据为「假值即启动」，后续同进程用例建立 `#/limit-up` 前置将静默跳过启动（当前 820 例全绿，属潜在顺序依赖）；该泄漏为 `76e9cd9` 首次引入（`892f1b8` 未挂载 root，走 `app.js:1524-1525` `stopLimitUpTimer` 分支） |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round1-handoff.md`](2026-09-14-workbuddy-code-review-round1-handoff.md) | 独立审查 `892f1b8`（代码落地阶段 round 1）：**未通过**。P2×1：`tests/limitUpChartFixes.test.js:250-266` 用例 7 未建立 `#/limit-up` 前置（未 `limitUpCtrl.setRootEl`、不经路由）→ 恒 `hasLimitUpRoot === false`，而修复前 `applySchedule(allowed, !hasLimitUpRoot)` 在该前置下恒等于修复后 `applySchedule(allowed, true)`，回退 `app.js:1511` 后该文件仍 6/6 全绿（假通过，无法证伪验收标准 3），且 §4.3:465-469 的 (b)(c) 端到端断言（`pollTimerId` 驱动轮询回调 → `fetchQuotes` 批次含展开标的 → `state.quotes` 写入）完全缺失；P3×1：用例 5（`:179-205`）盘前子场景仅断言纯函数，把 `chartRowController.js:391/:546` 注入源变异为 `getBeijingDate()` 后 6/6 仍绿（盘前幽灵 Bar 无回归防护）。产品代码三项改造（`app.js:413`、`chartRowController.js:391/546`、`app.js:1508-1511`+`monitorController.js:14-22/142-148`）与 `npm test` 820/820、`npm run lint` 0 问题、`npm run build` 成功均经独立探针与变异实跑确认生效 |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round17-handoff.md`](2026-09-14-workbuddy-code-review-round17-handoff.md) | 独立审查 90a0d60（round 17）：**未通过**。P3×2（均限 T-1 侧）：①`doc:23` mermaid 节点 F 的 `min(n, 320-x)` 在自述前提 `x ≤ 240`、`n ∈ {240,241,243}` 下保留根数下界恒为 80，却被写作「≥77~80」（77 需 `x=243`，越出前提），且与 `doc:71`「保持在 80~240 根」自相矛盾；②`doc:71`/`doc:135` 把 T-1 收盘保留 77~79 归因于「`n` 达到 241/243 根」，复算 `min(n, 320-240) = 80` 对三个 `n` 均成立，`79/77` 仅由当日 `x=241/243` 触发（`n` 真正的线性驱动项是 T-2 的 `max(0, 320-x-n)`，`doc:68` 正确）。T-2 侧（`doc:29/:68/:72/:138-139/:143`）已确认闭环；round 15 取值域枚举/`pollTimerId` 纯身份键、`STATUS.md` 历史段逐字未变、`npm test` 814/814 均复核通过 |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round16-handoff.md`](2026-09-14-workbuddy-code-review-round16-handoff.md) | 独立审查 9c78a3c（round 16）：**未通过**。P3×1：§2.2/§3 的 320 根滑动窗口 T-2 保留根数「严格服从 `max(0, 80-x)`」与「第 80 根 Bar（约 10:48）滑出」阈值，以「每个交易日恰为 240 根」为隐含前提，而同文件 `:65` 与 `STATUS.md:96` 自述单日可因 9:25 集合竞价点达 241/243 根、该窗口取自 `tencent-legacy` 同源缓存 → 实际为 `max(0, 320-x-n)`，`x=0` 时 T-2 上限 80/79/77 根、滑出阈值 `x ≥ 80/79/77`；`doc:67` 的 `min(240, 320-x)` 同理低估。round 13 仅补了 T-1 的「当日 x ≤ 240」条件，历史日自身 241/243 根维度未处理（同类 4 处未横向同步）。round 15 的 1 项 P3（`doc:419`/`doc:385` 取值域枚举遗漏 `60000`）经探针复算确认闭环；T-2 侧已于 round 17 复审确认闭环 |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round15-handoff.md`](2026-09-14-workbuddy-code-review-round15-handoff.md) | 独立审查 bd0e939（round 15）：**未通过**。P3×1：§4.3（`doc:419`/`doc:385`）对 `state.refreshInterval` 合法取值域的枚举遗漏 `60000`（`app.js:151-156` 的 `REFRESH_OPTIONS` 含 60000，`:803` 以其校验、`:1569` 载入持久化值），使 `state.limitUp.refreshInterval === state.refreshInterval === 60000` 成为第三个同 ms 碰撞档位未被覆盖；根因为扩写括注时沿用 round 14 报告的错误枚举而未回代码求证。round 14 的 2 项 P3 主项（`STATUS.md:95` 引文逐字还原与归属闭合、§4.3 纯身份键 `pollTimerId` 绑定）经探针与实跑复核确认闭环 |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round14-handoff.md`](2026-09-14-workbuddy-code-review-round14-handoff.md) | 独立审查 33b8f53（round 14）：**未通过**。P3×2：①`STATUS.md` round 12 闭环记录被改写成 round 13 才引入的条件式（round 12 实为无条件 `恒 ≥80`，当前文档为「常规 x≤240 时 ≥80」），并与同文件 round 13 段就「谁补齐该条件」互斥；②§4.3 沙盒以数值 `ms` 选中回调，在 `state.limitUp.refreshInterval === state.refreshInterval`（合法取值交集含 10000/30000）时同路径注册两个同 `ms` 定时器，文档要求的「唯一」断言不成立。round 13 的 P2-1/P2-2/P3-1/P3-2/P3-3 主项经探针与实跑复核确认闭环；2 项主项已于 round 15 复审确认闭环 |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round13-handoff.md`](2026-09-14-workbuddy-code-review-round13-handoff.md) | 独立审查 c3f5bc0（round 13）：**未通过**。P2×2：定时器沙盒单槽覆盖写导致步骤 (b) 捕获错回调、STATUS.md 残留 1607 与 Fake Timers 已证伪断言；P3×3：过强结论、A1 未按路径同步、保留根数与 241/243 前提冲突。主项已闭环，残留 2 项 P3 见 round 14 |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round12-handoff.md`](2026-09-14-workbuddy-code-review-round12-handoff.md) | 独立审查 7b34bc5（round 12）：**未通过**。P3×4：保留根数公式残留 / 1607 变异不可证伪 / A1 行 targetDate 矛盾 / Fake Timers 机制缺失。主项已闭环，残留项见 round 13 |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round11-handoff.md`](2026-09-14-workbuddy-code-review-round11-handoff.md) | 独立审查 147573a（round 11）：**未通过**。P2：§4.3 用例 7 的集成到达性断言无法证伪 P1（定时器存活断言无可达接入点，(b)(c) 分支绕过调度层）；P3×4：绝对化表述、A4 行矛盾、保留根数公式对齐、量化数值成立条件。P2 主因已于 round 12 复审确认闭环，残留 4 项 P3 见 round 12 |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round10-handoff.md`](2026-09-14-workbuddy-code-review-round10-handoff.md) | 独立审查 0305357（round 10）：**未通过**。P1：改造点四只改 `getRefreshCodes()`，`#/limit-up` 路由下行情轮询器被停摆；P2：§4.1.4/§4.2 绝对化表述与腾讯主源矛盾；P3：mock 桩缺信封。三项缺陷已在最新方案中彻底闭环，提交 round 11 复审 |
| 2026-09-14 | [`2026-09-14-workbuddy-code-review-round9-handoff.md`](2026-09-14-workbuddy-code-review-round9-handoff.md) | 独立审查 e2a8f0d（round 9）：**未通过**。P1：修复方案未覆盖"历史看板下标的报价脱离刷新集合"（无报价→无合并/Tick/分时刷新）；P2：改造点二遗漏 `applyLiveTickToKlineChart` 同类调用点；P3×4：窗口阈值未量化、§2.4 无影响项、§4.3 缺分时 mock、§2.3/§3.2 前提互斥 |
| 2026-09-14 | [`2026-09-14-limit-up-chart-date-flip-investigation-and-resolution-handoff.md`](2026-09-14-limit-up-chart-date-flip-investigation-and-resolution-handoff.md) | 涨停看板切前一天图表逻辑错误根因剖析、分钟线 320 根滑动窗口穿透机制、调度保活与活跃图表订阅双解耦解决方案（T-2 侧已闭环至 round 16 缺陷；T-1 侧残留 2 项 P3 见 round 17） |
| 2026-09-13 | [`2026-09-13-workbuddy-code-review-round3-handoff.md`](2026-09-13-workbuddy-code-review-round3-handoff.md) | 独立审查 2f94e08（round 3）：**通过**，无 P0/P1/P2；遗留 2 项 P3（Eastmoney 空串量比、LRU tie-break localeCompare 非字节序） |
| 2026-09-13 | [`2026-09-13-storage-lru-tiebreak-and-parser-ratio-round2-closure-handoff.md`](2026-09-13-storage-lru-tiebreak-and-parser-ratio-round2-closure-handoff.md) | WorkBuddy 审查 round 2 缺陷闭环（P2 LRU tie-break 确定性加固、单测稳定性、P3 Eastmoney 量比缺失语义对齐） |
| 2026-09-13 | [`2026-09-13-workbuddy-code-review-round2-handoff.md`](2026-09-13-workbuddy-code-review-round2-handoff.md) | 独立审查 ffe92b3（round 2）：P0/P3 有效，但 LRU 淘汰单测偶发失败（P2）+ Eastmoney 量比语义未对齐（P3），未通过 |
| 2026-09-13 | [`2026-09-13-storage-orphan-and-parser-ratio-closure-handoff.md`](2026-09-13-storage-orphan-and-parser-ratio-closure-handoff.md) | WorkBuddy 审查缺陷全面闭环（P0 storage 孤岛副本消除、P1 LRU 单测、P2 缺失量比语义对齐、P3 pinnedSort 消除） |
| 2026-09-13 | [`2026-09-13-workbuddy-code-review-round1-handoff.md`](2026-09-13-workbuddy-code-review-round1-handoff.md) | 独立审查 ad69e35（round 1）：确认 BUG-02/03 闭环，指出新引入 storage 孤岛副本 P0 回归 |
| 2026-09-13 | [`2026-09-13-review-round1-repro.mjs`](2026-09-13-review-round1-repro.mjs) | round 1 审查复现与判别力证据脚本（双模式，broken 基线 `ad69e35^`） |
| 2026-09-13 | [`2026-09-13-bug03-regression-and-storage-visibility-closure-handoff.md`](2026-09-13-bug03-regression-and-storage-visibility-closure-handoff.md) | BUG-03 回归修复与存储降级可见性彻底闭环 |
| 2026-09-12 | [`2026-09-12-review-fix-verification.md`](2026-09-12-review-fix-verification.md) | 核实 b601d1b 对独立审查缺陷修复（BUG-03 未闭环且引入回归） |
| 2026-09-12 | [`2026-09-12-review-fix-verification-repro.mjs`](2026-09-12-review-fix-verification-repro.mjs) | 独立审查缺陷修复复现与证据脚本 |
| 2026-09-12 | [`2026-09-12-independent-full-codebase-review-handoff.md`](2026-09-12-independent-full-codebase-review-handoff.md) | 全项目代码独立审查与演进路线交付 |
| 2026-09-11 | [`2026-09-11-search-suggest-fix-verification.md`](2026-09-11-search-suggest-fix-verification.md) | 联想搜索审查缺陷修复验证与证据脚本 |
| 2026-09-11 | [`2026-09-11-search-suggest-fix-repro.mjs`](2026-09-11-search-suggest-fix-repro.mjs) | 联想搜索缺陷复现与断言脚本 |
| 2026-09-11 | [`2026-09-11-search-suggest-code-review-handoff.md`](2026-09-11-search-suggest-code-review-handoff.md) | 联想搜索第一轮独立审查交接文档 |
| 2026-09-10 | [`2026-09-10-r1r8-fix-review.md`](2026-09-10-r1r8-fix-review.md) | R1-R8 缺陷修复与复审记录 |
| 2026-09-10 | [`2026-09-10-review-repro.mjs`](2026-09-10-review-repro.mjs) | 动量峰值复现验证脚本 |
| 2026-09-10 | [`2026-09-10-m1m6-fix-verification.md`](2026-09-10-m1m6-fix-verification.md) | M1-M6 缺陷修复独立验证 |
| 2026-09-10 | [`2026-09-10-momentum-peak-touch-review.md`](2026-09-10-momentum-peak-touch-review.md) | 动量峰值触达与交互复审 |
| 2026-09-10 | [`2026-09-10-momentum-peak-fix-closure.md`](2026-09-10-momentum-peak-fix-closure.md) | 动量峰值计算缺陷闭环记录 |
| 2026-09-10 | [`2026-09-10-code-review-requirements-and-modularization-handoff.md`](2026-09-10-code-review-requirements-and-modularization-handoff.md) | 需求对齐与模块化解耦审查交接 |
| 2026-09-09 | [`2026-09-09-vps-diagnostics-investigation.md`](2026-09-09-vps-diagnostics-investigation.md) | VPS 故障本机复现、新浪快照回退与扫描覆盖修复 |
| 2026-09-09 | [`2026-09-09-completion-goal-progress.md`](2026-09-09-completion-goal-progress.md) | A–E 全部剩余实施项完成记录、验收矩阵及测试边界 |
| 2026-09-09 | [`2026-09-09-followup-review-and-remaining-stages.md`](2026-09-09-followup-review-and-remaining-stages.md) | 最新提交复审、四项边界修复及 A–E 实际剩余项 |
| 2026-09-09 | [`2026-09-09-code-review-defects-closure-and-session-model-handoff.md`](2026-09-09-code-review-defects-closure-and-session-model-handoff.md) | 缺陷闭环与期货会话模型收敛交接 |
| 2026-09-08 | [`2026-09-08-r1-r7-t1-t2-fixed-handoff.md`](2026-09-08-r1-r7-t1-t2-fixed-handoff.md) | R1-R7 与 T1/T2 修复闭环交接 |
| 2026-09-08 | [`2026-09-08-code-review-bugs-and-refactor-handoff.md`](2026-09-08-code-review-bugs-and-refactor-handoff.md) | 代码审查缺陷与架构重构交接 |
| 2026-09-07 | [`2026-09-07-workbuddy-round4-code-review-handoff.md`](2026-09-07-workbuddy-round4-code-review-handoff.md) | WorkBuddy 第四轮代码审查交接（最新提交核验 + 整体复审） |
| 2026-09-07 | [`2026-09-07-workbuddy-round3-code-review-handoff.md`](2026-09-07-workbuddy-round3-code-review-handoff.md) | WorkBuddy 第三轮代码审查交接 |
| 2026-09-05 | [`2026-09-05-workbuddy-round2-code-review-defects-closure-handoff.md`](2026-09-05-workbuddy-round2-code-review-defects-closure-handoff.md) | WorkBuddy 第二轮全量审查缺陷彻底闭环与控制器解耦交接 |
| 2026-09-05 | [`2026-09-05-workbuddy-code-review-defects-closure-handoff.md`](2026-09-05-workbuddy-code-review-defects-closure-handoff.md) | WorkBuddy 第一轮全量代码审查缺陷闭环交接 |
| 2026-09-05 | [`2026-09-05-code-review-defects-closure-and-views-decoupling-handoff.md`](2026-09-05-code-review-defects-closure-and-views-decoupling-handoff.md) | 代码审查遗留缺陷全量闭环与视图组件解耦交接 |
| 2026-09-04 | [`2026-09-04-aktools-data-source-architecture-and-stability-handoff.md`](2026-09-04-aktools-data-source-architecture-and-stability-handoff.md) | AKTools 数据源稳定性根因分析与架构演进方案 |
| 2026-09-04 | [`2026-09-04-code-review-defects-and-architecture-refactor-handoff.md`](2026-09-04-code-review-defects-and-architecture-refactor-handoff.md) | 全面代码审查结论、缺陷清单与架构重构实施交接 |
| 2026-09-04 | [`2026-09-04-full-code-review-handoff.md`](2026-09-04-full-code-review-handoff.md) | 全量代码审查基线与缺陷分类 |
| 2026-09-03 | [`2026-09-03-futures-complete-defects-resolution-handoff.md`](2026-09-03-futures-complete-defects-resolution-handoff.md) | 境内期货全链路支持与代码审查缺陷闭环重构交接 |
| 2026-09-03 | [`2026-09-03-remaining-defects-and-remediation-handoff.md`](2026-09-03-remaining-defects-and-remediation-handoff.md) | 核心缺陷闭环差距分析与剩余未完成缺陷交接 |
| 2026-09-03 | [`2026-09-03-gemini-implementation-code-review-handoff.md`](2026-09-03-gemini-implementation-code-review-handoff.md) | 第三方与 GPT 详细代码审查与缺陷诊断清单 |
| 2026-09-03 | [`2026-09-03-phase4-futures-full-pipeline-handoff.md`](2026-09-03-phase4-futures-full-pipeline-handoff.md) | 境内期货全链路支持与中低缺陷重构初版交接 |
| 2026-09-02 | [`2026-09-02-runtime-bugs-chart-benchmark-handoff.md`](2026-09-02-runtime-bugs-chart-benchmark-handoff.md) | 10 日涨幅扫描、分时/K 线实时更新与行情图对照修复 |
| 2026-09-01 | [`2026-09-01-competitor-ai-code-review-handoff.md`](2026-09-01-competitor-ai-code-review-handoff.md) | 竞品 AI 代码审查交接 |
| 2026-09-01 | [`2026-09-01-competitor-ai-review-fixes-handoff.md`](2026-09-01-competitor-ai-review-fixes-handoff.md) | 竞品审查缺陷修复记录 |
| 2026-09-01 | [`2026-09-01-plan-requirements-implementation-audit-handoff.md`](2026-09-01-plan-requirements-implementation-audit-handoff.md) | 计划与需求实现审计交接 |

---

## 2. 2026年6月：早期构建与基础阶段交接

| 日期 | 文档 | 说明 |
| :--- | :--- | :--- |
| 2026-06-06 | [`2026-06-06-intraday-data-source-fix-report.md`](2026-06-06-intraday-data-source-fix-report.md) | 分时数据源修复报告 |
| 2026-06-05 | [`2026-06-05-phase8-chart-rerender-fix-handoff.md`](2026-06-05-phase8-chart-rerender-fix-handoff.md) | 涨停页图表重新挂载修复 |
| 2026-06-05 | [`2026-06-05-phase8-cache-preload-multi-chart-handoff.md`](2026-06-05-phase8-cache-preload-multi-chart-handoff.md) | K 线持久化 + 预拉 + 涨停页多图表 |
| 2026-06-05 | [`2026-06-05-phase7-reason-and-date-handoff.md`](2026-06-05-phase7-reason-and-date-handoff.md) | 涨停原因 + 日期选择 |
| 2026-06-05 | [`2026-06-05-phase7-date-fix-handoff.md`](2026-06-05-phase7-date-fix-handoff.md) | 日期格式 Bug 修复 + 前一天/后一天按钮 |
| 2026-06-05 | [`2026-06-05-aktools-upgrade-handoff.md`](2026-06-05-aktools-upgrade-handoff.md) | AKTools 涨停数据源升级交接 |
| 2026-06-05 | [`2026-06-05-phase5-bugfixes-handoff.md`](2026-06-05-phase5-bugfixes-handoff.md) | Phase 5 与 K 线图修复记录 |
| 2026-06-05 | [`2026-06-05-phase4-handoff.md`](2026-06-05-phase4-handoff.md) | Phase 4（涨停看板）完整交接 |
