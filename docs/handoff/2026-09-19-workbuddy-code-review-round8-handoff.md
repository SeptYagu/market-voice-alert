# WorkBuddy 独立代码审查 Round 8 Handoff — 集合竞价日K下影线消除 / 分时图纳入 / 语音时段去重（修复轮复查 7）

> **结论**：**未通过（1×P2 + 2×P3）**
> 被审 HEAD：`ee4c38a7350a0f72aef4f0796b3cf973ebc8c744`
> 基准提交：`7c955b0cbcdba12adff9b42b5b159fe7c9663823`
> 本轮实际增量（修复提交自身）：`2ccb1f3..ee4c38a` = 2 文件 / +188 −39（`src/js/kline.js` +28 −16、`tests/callAuction.test.js` +160 −23）
> 任务书范围：`7c955b0..ee4c38a` = 17 文件 / +2727 −27
> 审查日期：2026-09-19

---

## 一、审查基本信息与通过项简述（≤10 行）

- 版本核验：分支 `main` 已配置上游 `origin/main`，`git pull --ff-only` 返回 `Already up to date`，工作区干净；实际 HEAD `ee4c38a` 与任务给定待审 SHA **完全一致**，`7c955b0` 为其祖先；仓库内无 `docs/review-checklist.md`。
- 门禁复核（仓库外 pristine 导出 `git archive ee4c38a` + `node_modules` 目录联接）：`node scripts/run-tests.mjs` **913/913 全绿**、`eslint` 0 问题。
- 通过项（极简）：需求 1/2/3 的主形态与 R7 三项缺陷的主体均已生效——追加分支已改为「时效未知一律 `preview`」（R7 P2-1 主形态闭环，独立探针 09:24:59/09:30:00/09:35 三拍均 `preview` 且 14:55 `low === 20.50`）；语音 09:20-09:25 保证播报 + 09:25 平滑去重经真实控制器逐拍复核（09:20 +1、09:21 +1、09:24:59 +1、09:25:00 +0、09:26 +0）；R7 P3-1/P3-2 的判别力缺口已补齐（见下）。
- 独立设计并执行 14 点定点变异 + 5 组边界/反例探针（含真实 `parseEastmoney` 通道、真实 `createVoiceController` 逐拍、`_isTradingMinute` 边界、`getQuoteBeijingTimeMinutes` 非法/截断输入）：**13 项有义变异全部确定性转红**（R7 点名的 M19b/M19c/M20b/M20c 等价形态 V1–V4、追加分支时效门 K1–K3、原地分支合取项 K4/K5、分时资产隔离 K6/K7、图表网格 C1/C2），**1 项存活 + 1 组反例序列命中**（见第二节）。
- 结论：**未通过**，最高严重级别 **P2**。
- 证据基建说明：全部探针/变异/实验脚本均为**仓库外**临时文件（`_audit_r8/{probe,probe2,probe3,mutate,experiment}.mjs`，工作目录 `d:/AiPrograms/project1/_audit_r8`，基线由 `git archive ee4c38a | tar -x` 导出 + `node_modules` 目录联接），不随提交入库。

---

## 二、审查发现与缺陷清单

### P2-1 R7 P2-1 的「虚拟价漂移」形态未闭环：时效未知的内容升级通道仍可让集合竞价虚拟价写死当日 `open`/`low` 并锁死全天（R7 验收② 未达成）

- **严重级别**：P2
- **文件与行号**：
  - 判据：`src/js/kline.js:657-670`（`if (last.preview)` → `:659` `isQuotePreOpen`、`:660` `isPostOpenTime`、`:668` `hasNewPriceInfo`、`:669-670` `hasOfficialTradeEvidence` 的**第二析取项**）
  - 落点：`src/js/kline.js:672-684`（清 `preview`、`open = quoteOpen || price`、`low = min(open, price, quoteLow)`）
  - 对照（本轮已闭环）：追加分支 `src/js/kline.js:578-605`（`hasOfficialTrade = isPostOpenTime && hasValidOpen`，时效未知一律 `preview`）
  - 注释与实现不符：`src/js/kline.js:664-667` 自述这两条合取项可 "prevent locking pre-open virtual low"，与实测行为相反
  - 应覆盖而未覆盖的用例：`tests/callAuction.test.js:1231-1270`（`5.1(1s)`，仅覆盖「价格未变」）、`:1369-1430`（`5.1(1v)`/`5.1(1w)`，仅覆盖「价格推进后清除」）
- **触发条件**（全部为代码可见路径）：
  1. A 股 + `period === '1d'`（`isAStock` 门 `:502-503`）；
  2. 当日柱已存在 `preview: true`（`last.preview`，即先前任意一拍已建预览柱）；
  3. 该拍报价**时效未知**（`getQuoteBeijingTimeMinutes` → `null`）；该来源已由代码确证：`parseEastmoney`（`src/js/parser.js:299-320`）返回对象**不含** `updateTime`/`time`（实测 keys 无此两项），而 `src/js/api.js:209-216` 在腾讯漏码/整批失败时对 A 股逐码回落东财；
  4. 客户端时钟 `≥ 09:30`（`:655`）+ `open` 有效（`:661`）+ `volume/amount > 0`（`:662`）；
  5. **该拍 `price` 与上一拍预览价不等**（`:668`）——即**虚拟价在竞价过程中发生漂移**（如 09:20 虚拟价 19.60，09:24 虚拟价被申报推到 19.65），此后该陈旧 payload 于 `≥09:30` 抵达。
- **实际行为与期望行为**：
  - 实测（真实模块 + 真实 `parseEastmoney` 输出，`_audit_r8/probe2.mjs` §G）：

    ```
    items = [ {time:'2026-09-17', o/h/l/c=20, volume:1000} ]   （末柱为昨日）
    09:31:00  东财兜底价 19.60（无 updateTime） -> preview=true  o=l=19.6  volume=0     （正确保持预览）
    09:31:30  同上通道漂移到 19.65            -> preview=undefined o=l=19.65 volume=12000 ← 误落「官方柱」
    14:55:00  真实成交 20.50                  -> o=20.5 l=19.65          low==20.50? false
    ```
  - 期望：任务书验收标准 1「日K集合竞价下影线**彻底**消除」；R7 §二 P2-1「修复后验收标准 ②」原文「**虚拟价漂移形态（19.60 → 19.65）同样不得清 `preview`**」。当前 `low` 因 `Math.min` 单向不可逆被虚拟价 19.65 写死并持续整个交易日（全仓无周期官方 K 重载，官方柱仅在展开/切周期/强刷时重载）。
- **根因**：`quoteTimeMinutes === null`（时效无法证明）时 `isQuotePreOpen` 恒为 `false`，第二析取项退化为「客户端时钟 `≥09:30` + `open>0` + 有量 + 价格与上一拍预览价不等」的**内容代理判据**——把「价格与预览价不相等」当作「已获得真实成交信息」的充分证据。追加分支已按 R7 首选方向改为 fail-closed（`:589`），原地分支未同步收紧，同一合取式在两个落点仍只改了一处。
- **影响范围**：A 股 `1d` 当日柱的 `open`/`low` 可被集合竞价虚拟价写死并持续全天，与验收标准 1 直接冲突。非 A 股、分钟/周/月周期不受影响。**补充事实（供实现方取舍）**：收紧该通道会使 5 条现有用例转红——实测把 `:669-670` 改为 `isPostOpenTime && hasValidOpen` 后 `npm test` 得 **908 pass / 5 fail**，失败项为 `5.1(1i)`、`5.1(1p)`、`5.1(1s)`、`5.1(1v)`、`5.1(1w)`（均以其 `14:55` 无时间戳清 `preview` 的期望为前提）。即该内容通道是**已被用例固化的既有设计选择**，收紧它须同步改写这 5 条用例并落实 R7 建议的「官方日K兜底重载」。
- **复现方法 / 运行证据**：
  1. `_audit_r8/probe2.mjs` §F/§G 输出即上表（先经真实 `parseEastmoney` 解析出对象、再补入 `chartRowController.js:395-398` 同款 `date` 兜底后逐拍调用真实 `applyLiveQuoteToKline`）；
  2. 等价纯输入版本：`_audit_r8/probe.mjs` §B（`{price:19.65, open:19.65, volume:12500, amount:245000}` 无时间戳 + 09:31）实测 `previewCleared=true`、`14:55 low=19.65`；
  3. 与 R7 相位对照：R7 在 `a505481` 上对同一序列（09:24:50 预览 19.60 → 09:31 漂移 19.65）得到同一结果，故属**该链 7 轮修复均未覆盖的残留缺口**，非本轮新引入的回归；
  4. 反证：`_audit_r8/experiment.mjs` 变体 B（把 `hasNewPriceInfo` 置 `false`）同样 908/5 ⇒ 该形态无任何负例防护。
- **修复建议**：
  1. **首选（与追加分支一致）**：删除第二析取项，原地分支升级亦须 `isPostOpenTime && hasValidOpen`；随后按 R7 P2-1 修复建议 1 补「`≥09:30` 后按固定周期重取官方日K，用带时间戳的权威柱替换 `preview` 柱」的兜底，并把 `:664-667` 注释改写为准确描述（须**登记保守侧副作用**：东财兜底路径上当日柱将以 `preview` 语义存在至兜底重载完成）；同步改写 `5.1(1i)/(1p)/(1s)/(1v)/(1w)` 五条用例的期望，并为「未知时效 ⇒ 不落官方柱」补正/反例。
  2. **次选（若坚持保留内容通道）**：为原地分支另立能自证「已获新信息」的锚点，例如要求**连续两拍**报价在 `open` 与 `price` 上互相印证且累积量能单调增长（盘前虚拟价漂移不满足该组合），并为该判据补正/反例；同时把 `:664-667` 注释改为如实描述非保守侧风险。
- **修复后验收标准**：① 上文 `09:31 preview 19.60 → 09:31:30 漂移 19.65 → 14:55 真实 20.50` 序列（无时间戳）当日柱**不得**出现官方非 `preview` 柱的 `open === low === 19.65`，14:55 当日 `low === 20.50`；② 追加分支两形态（09:24:59/09:30:00 冷启动、09:35 平开）与 `5.1(1a~1w)` 全量不回归；③ 为修复新增的判据补正/反例，移除该项后对应断言**确定性转红**；④ `npm test` 全绿、`npx eslint` 0 问题。

---

### P3-1 分时竞价窗口的 A 股判别谓词与仓库既有契约不一致：8/17 类 A 股代码形态丢失 09:15-09:25 竞价分钟点（相对 `a505481` 的行为回归）

- **严重级别**：P3
- **文件与行号**：
  - 谓词：`src/js/kline.js:797-805`（`code.startsWith('sh6') || 'sz0' || 'sz3' || 'bj' || 'sh000' || 'sz399'`）
  - 落点：`src/js/kline.js:806-809`（非 A 股走 `_correctLastIntradayPoint`，不追加竞价点）；窗口定义 `:734-747`
  - 契约对照：同文件 `:502-503` 的日K守卫用 `inferAssetType(effectiveCode) === ASSET_TYPES.STOCK_CN`；`src/js/parser.js:35` 的 A 股定义为 `^(?:sh|sz|bj)\d{6}$`；设计文档 `docs/handoff/2026-09-18-call-auction-kline-intraday-voice-handoff.md:97`「仅当…`inferAssetType(effectiveCode) === ASSET_TYPES.STOCK_CN` 时」
  - 测试：`tests/callAuction.test.js:415-446`（`5.1(4g)`，仅覆盖 `hk00700`/`usAAPL`，无 A 股非股票类样本）
- **触发条件**：A 股 + 分时实时 tick + 北京时间 09:15-09:25 + `quote.code` 属于 `sh5xxxxx`（ETF/基金）、`sz1xxxxx`（ETF/债）、`sh1xxxxx`（可转债/国债）、`sz2xxxxx`（B 股）、`sh9xxxxx`（B 股）或裸 6 位码。可达性已确认：`src/js/api.js:131-132` 把任何 `^(sh|sz|bj)\d{6}$` 归入 `stocks` 并走 `fetchTencent`/`fetchEastmoney`，报价进入 `state.quotes` 后被 `app.js:1360`/`chartRowController.js:564` 送入 `applyLiveQuoteToIntraday`。
- **实际行为与期望行为**：实测（`_audit_r8/probe2.mjs` §H，17 个代码形态逐项对比 `inferAssetType` 与「是否追加 09:18 竞价点」）：

  | 形态 | 样本 | `inferAssetType` | 09:18 追加竞价点 | 一致 |
  |---|---|---|---|---|
  | 沪深主板/创业板/科创板/北交所 | `sh600519`/`sz000001`/`sz300750`/`sh688981`/`bj430047` | STOCK_CN | 是 | ✓ |
  | 指数 | `sh000001`/`sz399001` | STOCK_CN | 是 | ✓ |
  | ETF/基金 | `sh510300`/`sz159915`/`sh588000` | STOCK_CN | **否** | ✗ |
  | 可转债 | `sh113050`/`sz128036` | STOCK_CN | **否** | ✗ |
  | B 股 | `sh900901`/`sz200011` | STOCK_CN | **否** | ✗ |
  | 裸 6 位码 | `600519` | STOCK_CN | **否** | ✗ |

  即 **8/17 不一致**。期望：与同文件日K守卫同口径（`inferAssetType(...) === STOCK_CN`）；`a505481` 上 `_isTradingMinute(parts)` 未分资产类型，上述形态**均**会追加竞价点，故本轮属行为收窄。
- **根因**：本轮新增的资产隔离用手写前缀白名单实现，未复用仓库既有的 A 股判别（`inferAssetType`），白名单未覆盖后者认可的全部 A 股形态；`5.1(4g)` 只用港美样本，无法判别该收窄。
- **影响范围**：A 股 ETF/可转债/B 股/裸码标的的分时图在北京 09:15-09:25 不再出现竞价撮合点（需求 2「分时图纳入 09:15-09:25」对这部分标的**部分未实现**），而同一批标的的日K仍走 A 股盘前守卫（`inferAssetType` 口径）⇒ 同一标的两条链路口径不一致。港美/外盘「零回归」不受影响（K6/K7 变异均转红）。
- **复现方法 / 运行证据**：`_audit_r8/probe3.mjs` §K 与 `probe2.mjs` §H 逐项实跑（脚本见证据基建说明）；对应变异 `K7-intraday-prefix-drop-sh6`（删 `sh6` 分支）在 913 项门禁下 **fail=1 转红**，证明该谓词已被 `5.1(4g)` 部分覆盖，但仅覆盖股票前缀。
- **修复建议**：把 `src/js/kline.js:797-805` 的谓词替换为与日K守卫同口径的判别：`const isAStock = quote.code ? inferAssetType(String(quote.code)) === ASSET_TYPES.STOCK_CN : (!quote.type || quote.type === 'stock');`（`inferAssetType`/`ASSET_TYPES` 已在 `:1` 导入），并据此补用例：ETF（`sh510300`）、可转债（`sh113050`）在 09:18 追加竞价点，港美仍不追加；同时在设计文档 §3.3 把「A 股」口径一句话对齐（或显式声明非股票 A 股标的不在需求 2 范围内）。
- **修复后验收标准**：`sh603533`/`sh510300`/`sh113050` 在 09:18 均追加分钟点，`hk00700`/`usAAPL` 均不追加；把判别改写回手写白名单（或删 `inferAssetType` 判别）后新增用例**确定性转红**；`5.1(4a~4g)` 与其余门禁不回归。

---

### P3-2 原地分支「虚拟价漂移」形态无任何负例用例（R7 验收② 与验收标准 4 的覆盖缺口）

- **严重级别**：P3
- **文件与行号**：应覆盖未覆盖：`tests/callAuction.test.js:1231-1270`（`5.1(1s)`）、`:1369-1430`（`5.1(1v)`、`5.1(1w)`）；被测实现 `src/js/kline.js:668-670`
- **触发条件**：运行全量门禁即成立（非偶发，可重复）。
- **实际行为与期望行为**：现有用例对原地分支只构造两类输入——「`price === last.close`（价格未变）⇒ 保持 `preview`」与「`price` 推进且 <b>被当作真实成交</b> ⇒ 清 `preview`」，**没有**任何用例断言「价格发生偏移但仍是盘前虚拟价 ⇒ 不得清 `preview`」。实测（`_audit_r8/experiment.mjs`）：
  - 变体 A（删去第二析取项、只保留 `isPostOpenTime && hasValidOpen`）：**908 pass / 5 fail** —— 转红的 5 条全都是**正例侧**（要求 `14:55` 无时间戳清 `preview`），说明该通道的正例侧已被固化，而负例侧（漂移不得清）完全缺失；
  - 变体 B（`hasNewPriceInfo = false`）：**908 pass / 5 fail**，同样全部为正例侧失败。
  即：无论把该通道放宽还是收紧到「漂移不再清 `preview`」的反方向，都只能命中正例侧断言 ⇒ **漂移形态零判别力**。期望：R7 验收② 与任务书验收标准 4「测试与变异矩阵全覆盖」要求该形态有可复现的正/反例。
- **根因**：`5.1(1s)/(1v)/(1w)` 的第三步统一构造为「价格推进到 20.50 并清 `preview`」，未构造「价格在虚拟价区间内漂移（19.60 → 19.65）」这一与 P2-1 同源的形态；用例与实现共享同一错误假设（「价格变了即真实成交」）。
- **影响范围**：P2-1 的修复载体无回归防护——后续若把 `:669-670` 放宽或删除第二析取项，门禁仍全绿（与 R7 P3-1 所指同类的假闭环）。
- **复现方法 / 运行证据**：`_audit_r8/probe.mjs` §B 证明现状（漂移清 `preview`、`14:55 low=19.65`）；`_audit_r8/experiment.mjs` 两个方向变体均只命中正例侧（见上）。
- **修复建议**：按 P2-1 采纳的方向补用例——若采纳首选（收紧），在 `5.1(1s)` 或新增用例中构造「09:24:50 预览 19.60 → 09:31 无时间戳 19.65（带量、`open` 有效）⇒ 保持 `preview: true` 且 `volume === 0`」，并保留其正例对照（同参数改带 `updateTime: '20260918093100'` ⇒ 落官方柱）；同时改写被收紧影响的 `5.1(1i)/(1p)/(1v)/(1w)` 期望。
- **修复后验收标准**：新增漂移负例在收紧后的实现下通过、在放宽回现状的实现下**确定性转红**；`5.1(1a~1w)` 全量不回归；`npm test` 全绿、`npx eslint` 0 问题。

---

## 三、待确认风险与未验证项

1. **待确认风险（`getQuoteBeijingTimeMinutes` 不校验日期，理论上陈旧时间戳可通过 `isPostOpenTime` 门）**：`src/js/kline.js:456-482` 仅按 `updateTime` 的时分秒计算分钟数，`'20260917150000'`（上一交易日 15:00）同样得 `900 ≥ 565` ⇒ 被当作「官方时效」落官方柱（`_audit_r8/probe3.mjs` §M 实测）。**当前不可达依据**：两个真实 A 股报价源的时间戳与日期同源——`parseTencent`（`parser.js:207-213/230-231`）的 `quoteDate` 由 `updateTime.slice(0,8)` 派生，二者恒同日；`parseEastmoney`（`parser.js:299-320`）根本不返回时间戳。**需要条件**：若后续接入新源或改动 `quoteDate`/`updateTime` 的派生关系，须改为「时间戳日期 == 当日交易日」的显式校验。**残余风险**：一旦可组合出「`tradingDay` 为今日 + `updateTime` 为往日 ≥09:25」，虚拟价/昨收将被写成今日官方 `open`/`low`。
2. **未验证项（P2-1 与 P3-2 共同的上游前提，继承 R5/R6/R7）**：`≥09:30` 后上游是否仍返回**盘前 payload**、持续多久、其 `price` 是否在虚拟价区间内漂移。本机无活体行情（离线）、当前为非交易时段，无法取证。**需要条件**：交易时段内对东财 A 股兜底端点（`api.js` 回落链）与腾讯字段 30 的实测采样。**残余风险**：P2-1 的触发概率无法量化；但其修复方向不依赖该前提（判据可无条件收紧）。
3. **待确认风险（未知时效通道收紧后的保守侧副作用，继承 R6 风险 2/4、R7 风险 3）**：若采纳 P2-1 首选，东财兜底路径上的 A 股当日柱将以 `preview` 语义（四价坍缩到现价、`volume=0`）存在，直到带时间戳的报价或官方日K兜底重载抵达；对「一字板/长期价平」标的该状态可能持续较长时间。实测形态：`{price:20, open:20, volume:12000}` 无时间戳 + `≥09:30` ⇒ `preview:true, volume:0`。需实现方在修复时一并确认取舍并登记。
4. **未验证项（需求 2 显示侧）**：`chart.js` 的 253 网格（`[9,15]-[9,25]` + `[9,30]-[11,30]` + `[13,00]-[15,00]`）本轮未改动，仅以现有 QUnit（`5.1(4a)~5.1(4g)`，均绿）+ 独立探针确认；未做浏览器端渲染核对，`09:25→09:30` 之间 4 分钟无槽位（既有设计）的视觉连续性未评估。
5. **本次未覆盖项**：未执行 `npm run e2e`（Playwright）与 `npm run build`；未连接活体行情；`server/` 侧 `intradayService`、`spotService` 未审查（本任务范围外）。
6. **冷启动偶发失败（继承）**：本轮仓库外共运行 26 次门禁，其中 0 次出现与集合竞价无关的失败签名（`910/913` 期间的先行探针为 `913/0`），未复现 R5/R6 记录的「缓存污染伪影」。

---

## 四、推荐修复顺序与复审验收标准

**推荐修复顺序**：

1. **P3-1**（把 `src/js/kline.js:797-805` 的谓词换成 `inferAssetType(...) === STOCK_CN` + 补 ETF/可转债正反例）——改动最小、与日K守卫口径对齐、独立于日K逻辑。
2. **P2-1 + P3-2**（原地分支的授权条件与用例**同批**落地）——二者互为判别力；落地时须同步改写 `:664-667` 注释、改写 `5.1(1i)/(1p)/(1s)/(1v)/(1w)` 五条受影响的期望，并登记保守侧副作用与「官方日K兜底重载」方案。
3. 待确认风险 1（时间戳日期校验）建议随 P2-1 一并加守卫；待确认风险 2（上游盘前 payload 持续性）建议在实现后于交易时段取样补证。

**复审验收标准**（下一轮需逐条提供可复现证据）：

- P2-1：`09:31 preview 19.60（无时间戳）→ 09:31:30 漂移 19.65 → 14:55 真实 20.50` 序列下当日柱**不得**出现官方非 `preview` 柱的 `open === low === 19.65`，14:55 `low === 20.50`；且「追加分支冷启动」「追加分支平开」「原地分支价格未变」「原地分支漂移」四形态均有正/反例。
- P3-1：`sh603533`/`sh510300`/`sh113050` 在 09:18 追加竞价点、`hk00700`/`usAAPL` 不追加；把谓词改写回手写白名单后新增用例**确定性转红**。
- P3-2：漂移负例在放宽实现下**确定性转红**，并给出变异前后 `preview`/`volume`/`open`/`low` 实值对照。
- 变异矩阵：R7 点名的 **R7-A 等价形态（追加分支时效门）**、**M19b/M19c/M20b/M20c** 与对照 R6 的 **M8/M8b、N1/N2/N4/N7/N8/N9/N11**、**N3/N5/N6/N10、M10/M10b/M10c、M11b、M16、M17、M19、M20** 杀红力不回归；`npm test` 全绿、`npx eslint` 0 问题。
- 探针 A/A2/B/B2（09:25:02/09:25:05 保持 `preview`、14:55 `low === 20.50`）、形态 C（真实低点 20.30 保留）、形态 D（`open:0,volume:0`）不回归。
- 需求 2：A 股含 09:15-09:25 数据走 253 固定网格、港/美/国际期货走数据驱动轴且数据点全可见。
- 需求 3：`09:20:00`/`09:24:59` 全量播报、`09:25:00` 起恢复去重、`09:29:00` 智能时段放行的端点断言；记忆基线在窗口内被写入且 `09:25` 起无重复播报。
- `git diff` 范围仅含修复与用例，不夹带无关改动；`:664-667` 残余风险注释与实现行为一致。

---

**报告文件命名说明**：本轮按任务书模板使用 `{DATE}-workbuddy-code-review-round8-handoff.md`（DATE 取审查执行日 2026-09-19）；本任务链前序轮次使用的 `call-auction-implementation-` 前缀文档 `2026-09-18-call-auction-implementation-workbuddy-code-review-round{1..7}-handoff.md` 保持不变。
