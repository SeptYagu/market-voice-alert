# 国际品种接入方案 WorkBuddy 独立审查 round 8 复查报告

> **结论**：**未通过（3×P2 + 2×P3）**
> 被审 HEAD：`b344926f241425e95456a432b733ff5fa97eab6c`
> 基准提交：`45593a5522343c1ed56725f841b16760f80217b7`
> 本轮实际审查增量：`8a05917..b344926` = 4 文件 / +279 −63（纯文档，主方案 316 行）
> 审查日期：2026-09-16

---

## 一、审查基本信息与通过项简述（≤10 行）

- 版本核验：分支 `main` 已配置上游 `origin/main`、工作区干净、`git pull --ff-only` 返回 `Already up to date`；实际 HEAD `b344926f241425e95456a432b733ff5fa97eab6c` 与任务给定待审 SHA **完全一致**，`45593a5` 为其祖先；diff 仅含 `STATUS.md`、`docs/handoff/INDEX.md` 与 8 份 `.md`（无产品代码/测试改动）。
- Round 7 四项缺陷**主项基本落地**：§3.1.1 已按「东财 qt 原始单价 / 真实除数」逐品种登记，本轮真源复测 10/10 成立（`qt.f43/qtDivisor` 对同源 `trends2` 末根偏差 ≤0.02%、`qt.f60/qtDivisor == trends2.preClose` **误差恒 0**、`f44/f45/f46` 与 `f43` 同量纲；实测除数 `CL/NQ/ES=100、GC/A50=10、SI/NG=1000、HG=10000、YM/HSI=1` 与 §3.1.2 `:175` 登记逐项相同，`HG` 的 `sinaScale=0.01` 经 `648.447×0.01 = 6.4845` 对东财 `6.4805` 复核成立）。
- 另核验：`(d.f107, d.f57)` 反查键对 10 个 secid **逐一等于 secid 本身**（`101/102/103/104/134` 实测与注册表一致），身份推导路径可落地；P1-2 落点已正确指向 `voiceSchedule.js:28` 与 `marketSession.js:46-52`；P3-1/P3-2 新增行号 `parser.js:34-40`、`marketData.js:196`、`klineService.js:41`、`tts.js:217-218`、`alert.js:87-88`、`monitorTableView.js:95/339`、`kline.js:522`、`batchExportService.js:102` **逐条与源码相符**，`formatQuoteSpeech`（`tts.js:229`）、`decideVoiceSchedule`（`voiceSchedule.js:7`）、`_isTradingSessionTime`/`_filterIntradaySessions`（`api.js:252/260`）均为仓库真实导出。
- 独立设计并执行 5 组探针（10 品种同刻 `qt`/`trends2`/新浪三源比对 ×3 轮、`(f107,f57)` 键回放、`trends2` 契约核验、真实模块 `decideVoiceSchedule` 时钟注入、仓库外副本变异回放），其中 **3 组命中缺陷**（见第二节）。

---

## 二、审查发现与缺陷清单

### P2-1 §3.1.2 行 12 与 §3.2.2 项 2 对 `voiceSchedule.js:28` 的关闭判据给出**互斥表达式**（`!allowed` vs `session === 'after-close'`）→ 按清单字面实现会在午休永久关闭语音（复现 2026-09-09 M1 回归），且全仓既有门禁不可见

- **严重级别**：P2
- **文件与行号**：
  - 清单（错）：`docs/handoff/2026-09-16-global-market-feasibility-and-architecture-handoff.md:182` —— 改造目标写「将关闭判据重构为“自选池中无非 A 股时段资产且无正在交易标的”（`!hasExtendedHoursAssets && !allowed`）」，**丢掉了原有的 `session === 'after-close'` 维度**；同口径被复制到 `STATUS.md:16`。
  - 正文（对）：同文件 `:238` —— 「将第 28 行判据重构为：`if (cfg.enabled && enabled && !hasExtendedHoursAssets && session === 'after-close' && cfg.autoStopAfterClose) enabled = false;`」
  - 代码落点：`src/js/services/voiceSchedule.js:28`（当前实现 `!futures.length && session === 'after-close' && cfg.autoStopAfterClose`，为全仓唯一的持久化关闭写入点，`voiceController.js:93-94` 消费）。
- **触发条件**：按 `:182`（或 `STATUS.md:16`）字面实现新判据，自选池为**纯 A 股**、`smartSchedule.pauseLunchBreak=true`（默认，`marketSession.js:12`）、北京时间 11:30–13:00。
- **实际行为与期望行为**：
  - 期望：午休只暂停（`enabled` 保持 `true`、`timerShouldRun=false`、播「中午休市」），13:00 自动恢复；`enabled=false` 只发生在 A 股收盘后且自选池无跨时段资产时。
  - 实际（按 `:182` 字面）：午休时 `getVoiceEligibleCodes` 因 `pauseLunchBreak` 返回空集 → `allowed=false` → 与 `!hasExtendedHoursAssets`（纯 A 股池）、`autoStopAfterClose=true` 三者同时成立 → `enabled = false` **持久化关闭**；`voiceController.js:93-94` 立即 `saveSettings({enabled:false})`，`:126` 每 30s 复评，13:00 不会自动恢复，用户手动重开后下一拍仍被关。
- **根因**：清单把新判据写成摘要时用 `!allowed` **替换**了 `session === 'after-close'`。`allowed` 在午休、盘前、非交易日等**更多状态**下同为 `false`，等于把永久关闭窗口从「A 股收盘后」放大到「全天非交易时段」。`§3.2.2:238` 的正文表达式保留了 `session === 'after-close'`，故两处对同一行给出互斥契约。
- **复现方法/运行证据**：建仓库外试验副本（`src/js` 整树复制，未触碰受审仓库 `.git`），仅按 `:182` 文字把 `:28` 改为 `!hasExtendedHoursAssets && !allowed` 并补 `hasExtendedHoursAssets`（按 §3.1.2 `:173` 契约实现），以真实 `decideVoiceSchedule` + 真实 `getVoiceEligibleCodes` 注入固定时钟回放：

  | 时钟（北京） | 自选池 | 变异后 `enabled` | 变异后 `notice` | 现网判据 `enabled` | 现网判据 `notice` |
  |---|---|---|---|---|---|
  | 10:00 | `['sh600519']` | true | null | true | null |
  | **12:00（午休）** | `['sh600519']` | **false（永久关闭）** | 中午休市 | **true（仅暂停）** | 中午休市 |
  | 14:00 | `['sh600519']` | true | null | true | null |
  | 16:00 | `['sh600519']` | false | 已收盘 | false | 已收盘 |

- **影响范围**：每个交易日 11:30 起语音被持久化关闭且不再自动恢复，与 `marketSession.js:60-70` 明文记载的 2026-09-09 M1 回归（「午休第一拍就把语音永久关闭、13:00 无法自动恢复」）完全同形。
- **判别力缺口（为何现有门禁拦不住）**：`grep -rn "resolveVoiceScheduleAction" src/` 显示该函数**仅由 `marketSession.js:86` 定义、无任何生产消费方**，而 `tests/codeReviewRegressions.test.js:279-345` 的 4 条 M1 用例**全部只断言 `resolveVoiceScheduleAction`** → 按清单字面实现后，真实落点 `enabled=false` 无任何既有用例覆盖（843 项门禁仍全绿）。
- **修复建议**：① 把 `:182` 的括注改写为与 `:238` 逐字一致的 `!hasExtendedHoursAssets && session === 'after-close' && cfg.autoStopAfterClose`，`STATUS.md:16` 同步；若确需引入 `!allowed`，必须显式写明它与 `session === 'after-close'` 是**与**关系而非替换关系；② §5 补「午休只暂停」断言：北京 12:00、纯 A 股自选、`pauseLunchBreak=true` → `enabled === true && timerShouldRun === false && transitionNotice === '中午休市'`。
- **修复后验收标准**：两处文本逐字一致；北京 12:00 纯 A 股自选得 `enabled === true`；把 `:28` 判据换成 `!hasExtendedHoursAssets && !allowed`（变异）时该用例**必须转红**；13:00 自动恢复仍成立。

---

### P2-2 §3.1.1 的 `sinaScale` 列把**东财数值回填为「新浪」值**（`GL_CL0`/`GL_NG0` 实测证伪）→ §5 新增的 10/10 跨源 `≤0.5%` 断言对两个首批品种**不可满足**

- **严重级别**：P2
- **文件与行号**：
  - 登记：同文件 `:63`（`GL_CL0` 行）、`:67`（`GL_NG0` 行）、`:109`/`:113`（§3.1.1 表 `sinaScale` 列：CL「1.0 (新浪 104.450，1:1)」、NG「1.0 (新浪 2.903，1:1)」）
  - 验收断言：同文件 `:335` —— `assert.ok(Math.abs(qt.f43 / qtDivisor - sina.field0 * sinaScale) / (sina.field0 * sinaScale) <= 0.005)`
  - 备源机制：同文件 `:264-270`（§3.4.2 只登记了 `GL_HG0` 的 100×（`:269`）与 `GL_HSI` 的日夜盘基准滚动（`:268`），**无 CL/NG**）
- **触发条件**：按 §5 `:335` 对 Phase 1 全部 10 个品种执行跨源断言；或运行中 `GL_CL0`/`GL_NG0` 切换到新浪 `hf_` 备源。
- **实际行为与期望行为**：
  - 期望（文档登记）：CL/NG 的 `sinaScale=1.0`、主备源 1:1，跨源偏 ≤0.5%。
  - 实际（2026-09-16 17:25 北京，`push2delay.eastmoney.com` + `hq.sinajs.cn`（带 `Referer`），三轮独立采样）：

    | 品种 | 东财 `qt.f43/qtDivisor` | 同刻新浪 `field0` | 跨源偏差 | 文档登记的新浪值 | 备注 |
    |---|---|---|---|---|---|
    | `GL_CL0` | 104.83 / 104.83 / 104.84 | 100.04 / 100.076 / 100.058 | **4.79% / 4.75% / 4.78%** | 104.450 | 该值等于同刻东财 `trends2` 末根（104.45） |
    | `GL_NG0` | 2.916 / 2.916 / 2.916 | 3.046 / 3.046 / 3.053 | **4.27% / 4.27% / 4.49%** | 2.903 | 该值等于同刻东财 `trends2` 末根（2.903） |
    | 对照 `GL_NQ0` | 29069.6 / 29069.6 / 29070.8 | 29070.8 / 29070.8 / 29070.8 | 0.004% / 0.004% / 0.000% | 29085.50 | 无法区分采样时点差异 |
    | 对照 `GL_ES0` | 7667.3 / 7667.3 / 7665.2 | 7665.6 / 7665.2 / 7666.0 | 0.022% / 0.027% / 0.010% | 7673.05 | 无法区分采样时点差异 |
    | 对照 `GL_YM0` | 52556 / 52555 / 52555 | 52534.7 / 52536.6 / 52540.9 | 0.041% / 0.035% / 0.027% | 52572.06 | — |

    同探针同刻的 NQ/ES/GC/SI/HG/YM 均在 0.05% 内 → 排除网络与时刻因素；`GL_CL0`/`GL_NG0` 的偏差是**系统性的 4.3%~4.8%**（与 Round 1 P2-6 对 `hf_CL` 实测的 4.97%~5.1% 同源），即两源并非同一合约月/基准。
- **根因**：Round 7 报告的「交叉源佐证」只比对过 `hf_GC`/`hf_SI`/`hf_YM`/`hf_HSI`/`hf_CHA50CFD` 五个品种，其余 5 行（CL/HG/NG/NQ/ES）从未取过新浪真值；本轮把它们写入 `sinaScale` 列时，`GL_CL0`/`GL_NG0` 填入的数值**恰好等于同刻东财 `trends2` 末根**（`104.45` / `2.903`），属以主源数值回填备源列；§5 `:335` 又被扩为 10/10 品种，使这条自设门禁对 2/10 品种结构性不可满足。
- **影响范围**：① 方案自身的 §5 门禁对 `GL_CL0`/`GL_NG0` **必然失败**，实现者只能改断言或造数；② 真实运行中 CL/NG 切到新浪备源时价格水平跳 4.3%~4.8%、涨跌幅基准切换，而 §3.4.2 未登记任何处置（只登记了 HG 的 100× 与 HSI 的基准滚动）。
- **复现方法/运行证据**：`node` 探针直连 `push2delay.eastmoney.com/api/qt/stock/get?secid=102.CL00Y|102.NG00Y&fields=f43,f60` 与 `hq.sinajs.cn/list=hf_CL|hf_NG`（`Referer: https://finance.sina.com.cn`，按 GBK→latin1 解析），三轮采样见上表；东财内部一致性另行复核：`qt.f60/qtDivisor == trends2.preClose` 对 10/10 品种误差恒 0（`CL 105.83 == 105.83`、`NG 2.919 == 2.919`、`HG 6.4435 == 6.4435`），证明问题只在**跨源登记值**。
- **修复建议**：① 对 CL/NG（以及未复核的 NQ/ES）**重新逐品种取新浪真值**并回填 `sinaScale` 列；`GL_CL0`/`GL_NG0` 需显式登记「两源合约月/基准不同，实测偏差 4.3%~4.8%，禁止按 1:1 换算」；② §5 `:335` 的跨源断言改为按注册表逐品种给容差（豁免品种改断言「偏差落在登记区间内」），或为 CL/NG 单独给出换算策略；③ §3.4.2 增列 CL/NG 的备源切换处置（与 HG 的量纲换算同层级）。
- **修复后验收标准**：10/10 品种在登记口径下跨源断言可通过；把任一品种的 `sinaScale` 或 `qtDivisor` 改错一档时对应用例**必须转红**；`GL_CL0`/`GL_NG0` 的跨源偏差有显式登记与处置。

---

### P2-3 本轮把全文 **22 处反斜杠转义全部删除** → 6 处正则契约语义被改坏（`\d` 退化为字面 `d`），§3.1.2 改造清单 **5 行**被拆成 5~8 列、表格结构与该列内容在渲染后丢失

- **严重级别**：P2
- **文件与行号**：同文件 `:170`、`:173`、`:176`、`:177`、`:178`、`:186`（§3.1.2 改造清单 6 行）
- **触发条件**：阅读/渲染该表，或把表中契约文本**逐字**实现为代码。
- **实际行为与期望行为**：
  - 期望（上一轮 `8a05917` 状态）：`` `^(sh\|sz\|bj)\d{6}$` ``、`` `hk\d{5}` ``、`` `v_((?:sh\|sz\|bj)\d{6}\|r_hk\d{5}\|us[A-Za-z]+)` ``、`` `^\d{14}$` ``、`` `hq_str_(?:nf_?[a-z0-9]+\|hf_[A-Za-z0-9_]+)` `` —— 正则语义正确、表内管道符已转义使表格闭合。
  - 实际（当前 HEAD）：`` `^(sh|sz|bj)d{6}$` ``、`` `hkd{5}` ``、`` `v_((?:sh|sz|bj)d{6}|r_hkd{5}|us[A-Za-z]+)` ``、`` `^d{14}$` ``、`` `hq_str_(?:nf_?[a-z0-9]+|hf_[A-Za-z0-9_]+)` `` —— `\d`（数字类）变成字面字母 `d`（如 `^(sh|sz|bj)d{6}$` 不再匹配 `sh600519`）；表内未转义的 `|` 被 GFM 当作列分隔符。
- **根因**：本轮改写主方案时把 markdown 与正则的转义符一并剥除（本轮 diff 中这些行均为 `-`/`+` 成对改写，非历史遗留）。
- **复现方法/运行证据**（仓库外脚本：逐行计数 + 按 GFM 规则还原列数）：
  - `git show 8a05917:<doc>` 中 `\d` 出现 **8** 次、`\|` 出现 **14** 次、反斜杠合计 **22** 次；当前 HEAD 三者分别为 **0 / 0 / 0**。
  - 表格列数分布：`8a05917` 全部表行只有 **4 列与 8 列两种**（无畸形行）；当前 HEAD 出现 5 个畸形行 —— `:170`=6 列、`:173`=7 列、`:176`=8 列、`:178`=5 列、`:186`=6 列（表头为 4 列），超出列数的单元格在渲染时被丢弃，即 `:170` 的「改造目标」列只剩截断片段、`:178` 的「扩展支持…」整段丢失。
- **影响范围**：核心实施清单 5/17 行在渲染后内容缺失/错位；正则契约若被逐字实现则直接得错正则。属**本轮新引入的回归**（非历史文本回溯）。
- **修复建议**：恢复 `\|` 与 `\d` 转义（`git show 8a05917:<doc>` 即为权威文本）；建议交付前加三道结构自检：① 标题唯一性；② 全表列数一致（按转义还原后统计）；③ 契约文本逐字落码后可执行。
- **修复后验收标准**：全文件反斜杠计数 ≥22；所有表行列数为 4/8/9 且无畸形行；§3.1.1 断言块与 §3.1.2 契约文本逐字落码后可执行。

---

### P3-1 Round 7 待确认风险 1 的闭环不完整：§2.1 `:59` 的无条件「完全同源一致」与同轮新增的 §3.4.2 限定互斥；新增机制描述**未附** Round 7 要求的日/夜盘各 ≥3 次复采证据

- **严重级别**：P3
- **文件与行号**：`:59`（`m:134` 括注「新浪 `hf_HSI` 字段 7 昨结 24676.000 与东财 `f60` **完全同源一致**」，本轮未修改）对 `:267`（「**常规时段**新浪 `hf_HSI` 昨结（24676.000）与东财 `f60`（24676）1:1 对齐」）+ `:268`（新增「HKFE 16:30–17:15 日夜盘转场结算…滚动至新交易日结算基准（如 24688）…新浪备源发布可能存在短暂延迟」）
- **触发条件**：夜间（HKFE 夜盘 17:15 后）比对两侧昨结；或按 `:59` 的无条件口径实现备源基准对齐。
- **实际行为与期望行为**：期望同一文档对同一关系只有一个口径，且风险已按 Round 7 要求复采定论。实际：2026-09-16 17:23 与 17:28（夜盘已开）独立复采 —— 东财 `134.HSI_M` `f60 = 24688`（与同源 `trends2.preClose = 24688` 一致），新浪 `hf_HSI` 字段 7 = `24681`（`f0 = 24678.38`），**偏差 7 点 / 0.028% 持续存在**（Round 7 于 17:07 测得 12 点 / 0.05%），而同一交易日 16:35 两侧同为 24676 → 与 `:59` 的「完全同源一致」直接矛盾，且 §3.4.2 的「短暂延迟」缺少可核验的采样序列。
- **根因**：本轮把处置写在 §3.4.2（该处口径正确），但未同步限定 §2.1 `:59` 的绝对化断言，也未落档 Round 7 明确要求的「日盘（16:00 前）与夜盘（17:15 后）各复采 ≥3 次」证据。
- **影响范围**：同一文档对同一关系给出互斥口径；0.028%~0.05% 的基准漂移量级只有单侧机制描述、无采样支撑。用户可见影响为主备源切换时涨跌幅 ≤0.05% 的跳变。
- **修复建议**：① 把 `:59` 的括注限定为「日盘常规时段」或直接删除该分句（与 `:267` 统一）；② 在 §3.4.2 附日/夜盘各 ≥3 次的复采表（含采样时刻与两侧数值），把「短暂延迟」量化为实测区间。
- **修复后验收标准**：全文对 `hf_HSI` 昨结关系的表述一致；日/夜盘复采证据落档，且 `trends2.preClose` 作为权威基准的处置可被复现。

---

### P3-2 §5 反向断言按字面**不可满足** —— `decideVoiceSchedule` 的 `transitionNotice` 依赖 `previous` 入参，文档未给出

- **严重级别**：P3
- **文件与行号**：同文件 `:346`（「在北京时间 16:00 注入纯 A 股自选 `['sh600519']`，断言 `enabled === false`、`timerShouldRun === false`、`transitionNotice === '已收盘'`」）；相关正向断言 `:343`
- **触发条件**：按 §5 文字（只给 `codes`/`now`/`settings`）调用真实 `decideVoiceSchedule`。
- **实际行为与期望行为**：期望按文档字面即可写出通过用例。实际：`voiceSchedule.js:24-26` 的判据链要求 `previous?.timerShouldRun && priorSubscriptionStillPresent`，未传 `previous` 时 `transitionNotice` 恒为 `null`。真实模块探针（`codes=['sh600519']`、北京 16:00、`autoStopAfterClose=true`）：
  - 不带 `previous` → `{enabled:false, timerShouldRun:false, transitionNotice:null}`
  - 带 `previous={timerShouldRun:true, eligibleCodes:['sh600519']}` → `transitionNotice:'已收盘'`
- **根因**：§5 的断言清单只描述「注入自选 + 断言输出」，未给出 `previous`（上一拍决策）与固定时钟这两个必需入参。
- **影响范围**：按文档字面实现得到的是**假失败**（`notice=null`），实现者须自行猜测入参；与 Round 11 的教训（测试方案必须能在现有范式下落地）同族，属测试规格不完整。
- **修复建议**：在 `:343`/`:346` 明确写出 `previous`（含上一拍会话与 `timerShouldRun`）与固定时钟；或把该条断言的判据收窄为 `enabled`/`timerShouldRun`（`transitionNotice` 另行断言）。
- **修复后验收标准**：按文档字面实现的三条隔离断言在缺陷已修的实现上全部通过；回退 `voiceSchedule.js:28` 为 `!futures.length`（变异）时跨时区用例**必须转红**。

---

## 三、待确认风险与未验证项

1. **待确认风险（`GL_NQ0`/`GL_ES0` 的 `sinaScale` 列同族存疑，未定级）**：文档登记的新浪值 `29085.50` / `7673.05` 与本轮 17:25 实测（`29070.8` / `7665.95`）之差无法区分「采样时点差异」还是与 `GL_CL0`/`GL_NG0` 同族的**主源值回填** —— 这两个登记值**恰好等于 Round 7 第二轮采样的东财 `trends2` 末根**（`29085.50` / `7673.05`）。建议与 P2-2 一并复采核定（本轮同刻两源偏差仅 0.004%/0.010%，单次采样无法判别）。
2. **未验证项（沿用 Round 7 结论）**：受限网络下「先失败→后轮转至 `push2delay`」的转移时序无法在同一出口复现 —— 本机 `push2his` 族不可达、`push2delay` 三端点全 `rc=0`；残余风险为轮转实现后的失败切换时延（`server/proxyService.js:35` 既有 `timeoutMs: 15_000`）与前端请求预算的叠加。
3. **未验证项**：Phase 2/3（港股/美股）的腾讯 `78`/`71` 字段数与东财 `116`/`105`/`106` secid、§2.2/§2.3 的根数口径（港股 331/330、美股 391/390）本轮未重采，沿用 Round 2/3 结论。
4. **附带说明（非缺陷、非阻塞）**：§3.1.2 标题（`:166`）与 §4 甘特图的「全仓 **15 处**」与本轮表格实际 **17 行**不一致（Round 5 的「11 处」当时对 11 行成立），建议改为「17 处」或改用「4 类 17 条」措辞。

---

## 四、推荐修复顺序与复审验收标准

**推荐修复顺序**：P2-3（文档交付物损坏，一次改净）→ P2-2（跨源登记值，直接决定 §5 门禁可否通过）→ P2-1（判据互斥，直接决定 15:00/12:00 语音行为）→ P3-2（断言入参）→ P3-1（HSI 复采落档）。

**复审验收标准**：
1. 全文反斜杠转义恢复（`\d` ≥8、`\|` ≥14，合计 ≥22），所有表行列数为 4/8/9 且无畸形行；§3.1.1 断言块与 §3.1.2 契约逐字落码后可执行。
2. §3.1.1 的 `sinaScale` 列按**逐一真源复采**回填；`GL_CL0`/`GL_NG0`（及 `GL_NQ0`/`GL_ES0`）的跨源偏差显式登记；§5 `:335` 的 10/10 断言在登记口径下可通过，且任一 `sinaScale`/`qtDivisor` 错一档时用例必须转红。
3. `:182`（及 `STATUS.md:16`）与 `:238` 的判据表达式逐字一致；新增「午休只暂停」断言（北京 12:00 纯 A 股池 `enabled === true`）且对 `:28` 判据变异**确定性转红**；13:00 自动恢复成立。
4. §5 `:343`/`:346` 补齐 `previous` 与固定时钟后，按文档字面实现的隔离断言全部通过；回退 `:28` 为 `!futures.length` 时跨时区用例必须转红。
5. §2.1 `:59` 与 §3.4.2 `:267`/`:268` 口径统一，HKFE 日/夜盘各 ≥3 次复采证据落档。

**审查通过条件**：上述 3×P2 + 2×P3 **100% 修复闭环** + 待确认风险 1 复采定论 + 未验证项明确列明，方可判定通过。
