# WorkBuddy 独立代码审查 Round 5 Handoff — 集合竞价日K下影线消除 / 分时图纳入 / 语音时段去重（修复轮复查 4）

## 一、审查基本信息与通过项简述

- 被审 HEAD：`18d24114248ca35e0a5f65a4cb423a7c4a7b4a37`（`main` 与 `origin/main` 同步，`git pull --ff-only` → already up to date，工作区干净）；基准 `7c955b0cbcdba12adff9b42b5b159fe7c9663823`；实际审查范围 `7c955b0..18d2411` = 14 文件 / +1908 −27，本轮修复提交自身 `1571180..18d2411` = 3 文件 / +339 −21（`src/js/kline.js` +89 −11、`tests/app.test.js` ±4、`tests/callAuction.test.js` +267）。仓库内无 `docs/review-checklist.md`。
- 门禁实跑：`npm test` **904/904 全绿**（仓库外 pristine 导出连续 6 次均 904/0；另 1 次 `903/1` 未捕获用例名，见 §三-1）、`npx eslint` 0 问题（均实跑复核，非采信自述）。
- **通过项（极简）**：R4 所要求的三项主形态确已生效——形态 D（`items` 末柱为昨日 + 09:25:05 无时间戳 `open: 0, volume: 0`）落成 `preview` 柱、真实快照到达后全天 `low = 20.50`；形态 C（同价掩盖 + 成交量）09:25:05 即清 `preview` 且 09:27 保留真实低点 20.30；A 股连续时段恢复吸收官方 `quote.high/low`（14:00 → `high 12.5 / low 8.8`）而盘前脏 `high/low` 仍收敛于 `price`（`req.1` 隔离不回归）；非 A 股（`hk00700`/`usAAPL`）零回归；需求 2 实时分时路径（09:15/09:18/09:25 追加、09:26/09:29 不追加、09:30 恢复）与需求 3 语音三态用例经独立复核**有效**。
- 独立验证：仓库外 pristine 导出（`git archive 18d2411` + 目录联接 `node_modules`）完成 **34 点定点变异矩阵** + **3 组探针序列**（形态 A/B/D、滞留分支可达性、时钟/端点边界）：**8 个变异存活**、**3 组反例序列命中** → 本轮发现 **1×P2 + 2×P3**，判定**未通过**。

---

## 二、审查发现与缺陷清单

### P2-1 「时效未知」仍被判为「非盘前」：官方柱 / `preview` 判定退化为**内容代理**（`volume`/`amount` 字段的有无与取值），盘前虚拟价仍在 09:25 后被写进当日 `low` 并锁死全天 —— R4 P2-1 / P2-2 **未闭环**

- **严重级别**：P2
- **文件与行号**：追加分支判定 `src/js/kline.js:578-590`（`:580` `isQuotePreOpen`、`:581` `isPostOpenTime`、`:582` `hasValidOpen`、`:583` `hasTradeVolume`、`:584` `isZeroVolume`、`:586-590` `hasOfficialTrade`），柱构造 `:592-632`；原地 `preview` 分支判定 `:659-674`（`:666-668` `hasPriceShift`/`hasOpenShift`/`hasSubstantialShift`、`:670-674` `hasOfficialTradeEvidence`），清除/重基线 `:676-688`；时效解析 `:456-482`（`updateTime`/`time` 皆缺失时 `return null`）。
- **触发条件**（全部代码可见路径）：
  1. A 股 + `period === '1d'`；
  2. 到达拍为**无时间戳**快照（`getQuoteBeijingTimeMinutes` → `null`；来源：`parseEastmoney` 恒无 `updateTime`/`time`，`parser.js:207-213` 字段 30 不匹配时 `updateTime=''`，`api.js` 的 A 股 Eastmoney 逐码回落 —— 与 R4 已确认一致）；
  3. **该拍携带非空量能证据或缺失 `volume` 字段**（使 `isZeroVolume === false`）：即 `volume > 0`、`amount > 0`、或报价对象无 `volume` 键之一；
  4. `items` 末柱为上一交易日（追加分支）**或**末柱为今日 `preview` 柱（原地分支），北京时间处于 09:25:00 之后。
- **实际行为（真实模块 `applyLiveQuoteToKline` 实跑，仓库外探针 `probe.mjs` / `probe3.mjs`；真实开盘与全天最低均为 20.50，盘前最后一拍虚拟价 19.60）**：

  ```
  A. 原地分支（形态 A，报价无 volume 字段）
     09:24:50 q={price:19.6,open:19.6}       -> 19.6/19.6/19.6/19.6  preview=true
     09:25:02 q={price:19.9,open:19.9}       -> 19.9/19.9/19.9/19.9  preview=false  ← 误清 preview，重基线到虚拟价
     09:25:32 q={price:20.5,open:20.5,vol:8000} -> 20.5/20.5/19.9/20.5 preview=false
     14:55:00 q={price:21.3,open:20.5,vol:9e5}  -> 20.5/21.3/19.9/21.3                ← 全天 low 19.90
  A2. 同序列但 09:25:02 带 volume:300 -> 同样 preview=false，全天 low 19.90
  B. 追加分支（形态 D 变体，末柱为昨日 + open 有效 + 无 volume 字段，09:25:05）
     -> 20.5/20.5/19.6/19.6  preview=false；14:55 -> 20.5/21.3/19.6/21.3  ← 全天 low 19.60
  B2. 同 B 但 09:25:05 为 {price:19.6,open:20.5,volume:100,amount:2000} -> 全天 low 19.60
  C. 对照（09:25:05 为 {price:19.6,open:0,volume:0}）-> 19.6/19.6/19.6/19.6 preview=true；
     真实快照到达后 20.5/20.5/20.5/20.5，14:55 low=20.50  ✔（R4 验收形态 D 已闭环）
  D. 对照（09:25:05 为 {price:19.6,open:20.5,volume:0}）-> preview=true  ✔
  ```

  即：**同一序列的输出差异完全由「报价是否带 `volume` 键 / `volume` 是否 > 0」决定，与快照时效无关**。
- **期望行为**（验收标准 1「日K集合竞价下影线彻底消除」+ Round 4 复审验收标准 ①「09:25:02 滞后快照（`price:19.9`、虚拟 `open:19.9`、无 `updateTime`）保持 `preview: true`」）：`quoteTimeMinutes === null`（时效无法证明）时不得清除 `preview`、不得让当日柱 `high/low` 由盘前虚拟价驱动；内容证据只能作为**受时效约束的升级通道**，不能作为时效判据本身。
- **根因**：`:580/:660` 的 `isQuotePreOpen = quoteTimeMinutes !== null && quoteTimeMinutes < 9*60+25` 使 `null`（未知）继续落到「非盘前」一侧（`!isQuotePreOpen === true`），随后判定改由内容代理完成：追加分支第三析取项 `hasValidOpen && !isZeroVolume`（`:589`）与原地分支 `hasSubstantialShift`（`:668`，仅额外加了一个 `!isZeroVolume` 闸）。R4 修复建议 ①（`isQuotePreOpen || isQuoteTimeUnknown → preview`）**未被实现**，仅把 fail-open 收窄到 `open` 无效或「`volume===0` 且 `amount` 假值」这一子集；本轮新增用例恰好被构造成该子集（见 P3-2），故门禁全绿。
- **影响范围**：A 股 `1d` 当日 `low` 被盘前虚拟价写死（本组反例 0.60~0.90 元），官方K 仅在展开/切周期/强制刷新时重载（`app.js:1284/1307/1314`，全仓无周期重载），故持续整个交易日；仅 A 股 `1d`。
- **是否本轮新引入**：否。以本轮基准 `1571180`（R4 被审版）对照，该分支自 `d3f415b` 起即为内容/时效混判，本轮只是新增了 `isZeroVolume` 闸收窄 fail-open 面 ⇒ 属**仍存在的残留缺口 + R4 验收标准字面未达成**（R4 表列该两项为 P2，故同级判定）。
- **复现方法 / 运行证据**：
  1. 探针 `_audit_r5/probe.mjs`（A / A2 / B / C 段）、`_audit_r5/probe3.mjs`（B/B2/D 段），逐拍调用真实 `applyLiveQuoteToKline(items, quote, '1d', 'sh603533', new Date(...+08:00))`；
  2. **测试侧反证**：把 `tests/callAuction.test.js:920` 的 09:25:02 拍输入去掉其 `volume: 0`（即还原为 R4 验收标准所写形态）→ `5.1(1l)` 立即转红（变异 C2，实跑 1 fail）；说明该用例的通过**唯一依赖**被测输入中额外补入的 `volume: 0`（`:920`）；
  3. `5.1(1l)` 标题自述「**但无 volume 时**保持 preview」（`:901-902`），而输入实为 `volume: 0`（有该字段），标题与输入不一致。
- **修复建议**：
  1. 显式引入时效三态：`const isQuoteTimeUnknown = quoteTimeMinutes === null;`，并将其与 `isQuotePreOpen` 同列处理（`hasOfficialTrade`/`hasOfficialTradeEvidence` 一律取假 ⇒ 保持 `preview` 柱语义）；`!isQuotePreOpen` 的前缀应删除，改为 `isPostOpenTime`（`quoteTimeMinutes !== null && ≥ 09:25`）为唯一时效授权；
  2. 若必须为「Eastmoney 无时间戳」保留升级通道，通道须**可本地判定且与实时钟绑定**，例如「`volume > 0` **且** `price !== preview 价` **且** 客户端时钟 ≥ 09:30」三者合取，并在注释中明确其残余风险；不得再由 `volume` 字段的**有无**决定；
  3. 追加分支第三析取项 `(hasValidOpen && !isZeroVolume)`（`:589`）删除或按上述通道改写（该析取项使 `:581` 的时效门整体失去作用，见 P3-2 变异 M13/M11）。
- **修复后验收标准**：① 探针 A/A2/B/B2 四条序列在 09:25:02 / 09:25:05 拍均保持 `preview: true`（或该拍被丢弃），14:55 日K `low === 20.50`；② 新增「无 `volume` 字段」与「`volume > 0`」两条无时间戳用例，且对「恢复 `!isQuotePreOpen` 前缀」「恢复 `hasValidOpen && !isZeroVolume` 析取」各至少 1 条断言确定性转红；③ `5.1(1a~1o)` 既有断言与形态 C（真实低点 20.30 保留）不回归。

---

### P3-1 本轮新增的「滞留期真实极值包络」被自身重基线丢弃（自相矛盾的死代码），且在用例集中零覆盖

- **严重级别**：P3
- **文件与行号**：`src/js/kline.js:689-713` 滞留分支，其中**本轮新增**的包络累计 `:693-700`（`updated.high = Math.max(lastHigh, price)` / `updated.low = Math.min(lastLow, price)` / `updated.volume = quoteVolume || last.volume || 0`），塌缩分支 `:701-708`；二者累计的极值在 `:676-688` 清 `preview` 时被重基线覆盖（`:677` `open = quoteOpen || price`、`:682-686` `lowCandidates = [open, price, quoteLow]`、`:679-681` `high`）。
- **触发条件**：A 股 `1d`，`items` 末柱为今日 `preview` 柱，且该拍 `quoteTimeMinutes === null || ≥ 09:25`、`hasTradeVolume === true`、**`quoteOpen` 无效（0/缺失）**（这正是 `:693` 包络分支的成立条件），随后某拍携带有效 `open` 的官方快照到达并清除 `preview`。
- **实际行为（真实模块实跑，`_audit_r5/probe.mjs` == D ==）**：

  ```
  09:24:50 q={price:20.5,open:0}                -> 20.5/20.5/20.5/20.5 preview=true
  09:25:05 q={price:20.3,open:0,volume:6000}    -> 20.5/20.5/20.3/20.3 vol=6000 preview=true  ← 包络累计 20.30
  09:25:35 q={price:19.8,open:0,volume:7000}    -> 20.5/20.5/19.8/19.8 vol=7000 preview=true  ← 包络累计 19.80
  09:27:00 q={price:20.5,open:20.5,volume:9000} -> 20.5/20.5/20.5/20.5 preview=false          ← 包络被丢弃
  14:55:00 q={price:21.3,open:20.5,volume:9e5}  -> 20.5/21.3/20.5/21.3                        ← 全天 low 20.50
  ```
- **期望行为**：R4 P2-2 修复建议 ③/④要求「滞留期作为已确认真实观测的**运行包络**累计」且「清除 `preview` 时的重基线改为 `low = Math.min(open, price, 已观测 low 包络)`」。当前实现采用了建议 ③（累计）却未采用建议 ④（重基线纳入包络）⇒ 若该窗口内价格确为真实成交（`hasTradeVolume` 为真即实现自身的判据），则期间真实极值被永久丢弃，当日 `high/low` 偏窄；若实现方主张该窗口必为盘前虚拟价，则 `:693-700` 的包络分支应整体删除、只保留 `:701-708` 的塌缩语义。**两者只能取一，现状是「累计后再丢弃」的自相矛盾状态。**
- **根因**：本轮为闭环 R4「滞留期量能与极值语义」而新增包络分支，但未同步改写清 `preview` 时的重基线（建议 ④ 漏做），且未补任何断言固化包络语义（见 P3-2 的 M7/M8/M19 全部存活）。
- **影响范围**：仅在「首拍起持续无有效 `open`（行情字段滞后）」这一窄场景下使当日 `low/high` 失真；期间 `preview` 柱的 `volume/amount` 由 0 变为报价累计值（与 `:701-708` 的归零契约相反，两分支语义未在文档/测试中声明）。仅 A 股 `1d`。
- **复现方法 / 运行证据**：探针 `_audit_r5/probe.mjs` == D ==（逐拍实跑）；变异 M7（：693 条件恒假 ⇒ 强制塌缩）与 M8（：706-707 不归零）在 904 项门禁下**均 904 pass / 0 fail 存活**，M19（重基线 `low` 去掉 `open`/包络）同样存活 ⇒ 该分支整体无用例。
- **修复建议**：二选一并写明语义：① 采用 R4 建议 ④——清 `preview` 时 `low = Math.min(open, price, quoteLow, 已观测 low)`、`high = Math.max(open, price, quoteHigh, 已观测 high)`（并对 `5.1(1a~1o)` 做回归确认）；② 或删除 `:693-700`，恢复塌缩语义并同步更新 `:689-692` 注释。无论取哪条，都需补一条**两步序列**用例（先落同日 `preview` 柱 → 再用 `volume>0`/盘前 `updateTime` 的拍更新）固化契约。
- **修复后验收标准**：①②之一落实且注释、用例、实现三者一致；对「删除 `:693-700`」或「重基线忽略包络」的定点变异**确定性转红**；`5.1(1a~1o)` 不回归。

---

### P3-2 变异矩阵仍未达成 R4 验收：**M7 由 RED 退化为 GREEN**、**M8 存活**，追加分支时效门 / 第三析取项、原地重基线组合、两处新增 `isContinuousTrading` 时钟门均零判别力；`5.1(1o)` 未触及目标分支

- **严重级别**：P3
- **文件与行号**：`src/js/kline.js:586-590`（追加分支 `hasOfficialTrade` 第三析取项）、`:581`（`isPostOpenTime` 时效门）、`:679-686`（重基线组合）、`:608-611` / `:653-656`（本轮新增的两处 `isContinuousTrading` 客户端时钟门）、`:693-700` / `:701-708`（滞留分支两路）；对应用例 `tests/callAuction.test.js:1054-1068`（`5.1(1o)`）、`:902-971`（`5.1(1l)`）。
- **触发条件**：运行全量门禁即成立（非偶发，实跑可重复）。
- **实际行为（仓库外 pristine 导出 + 34 点定点变异矩阵实跑，`node scripts/run-tests.mjs`，基线 904/0）**：

  | 变异 | 目标 | 结果 |
  |---|---|---|
  | **M7** 滞留分支恒塌缩（`if (false && hasTradeVolume && !isQuotePreOpen)`） | `:693` | **904 pass / 0 fail → 存活**（R4 表中同义变异为 **RED**，判别力**退化**） |
  | **M8** 滞留塌缩分支不再归零 `volume/amount` | `:706-707` | **存活**（R4 P3-2 明确要求杀红） |
  | **M11(b)** 追加分支 `isPostOpenTime` 09:25 → 09:30 | `:581` | **存活** |
  | **M13** 追加分支删除第三析取项 `(hasValidOpen && !isZeroVolume)` | `:589` | **存活** |
  | **M14** 追加分支 `isZeroVolume` 恒 `false` | `:584` | **存活** |
  | **M16** 原地清 `preview` 分支去掉 `isContinuousTrading` 时钟门 | `:679-686` | **存活**（本轮新增门无判别力） |
  | **M17** 追加分支 `isContinuousTrading` 恒 `true` | `:611` | **存活**（同上） |
  | **M19** 清 `preview` 重基线改为 `low = Math.min(price, quoteLow)` | `:682-686` | **存活** |
  | M12 `_positiveNumber` 去掉 `n > 0` | `:453` | 存活（等价变异体，负值无输入源，可接受） |
  | M1/M2/M3/M4/M5/M6/M9/M10/M11(a)/M15/M20/M21/M22 | 见下 | **RED ✅** |

  其中 RED 组：M1（`evidence = !isQuotePreOpen`，4 条）、M2（删 `!isQuotePreOpen`，`5.1(1j)`）、**M3/M4（`hasSubstantialShift` 去合取项，`5.1(1l)`）**、M5（时效门 09:25→09:30，`5.1(1n)`）、M6（时效通道恒 `null`，`5.1(1j)`）、M9（09:25 防抖窗口，`5.1(5)`）、**M10（`_isTradingMinute` 删 09:15-09:25 窗口，`5.1(4f)`）**、M11(a)（追加分支 `isQuotePreOpen` 门 R4 字面目标，`5.1(1n)`）、M15（原地恒吸收脏极值，`5.1(2)`）、M20（原地盘中不吸收官方极值）、M21（原地删 `hasTradeVolume` 析取项，`5.1(1m)`）、M22（追加分支 `hasOfficialTrade` 恒真，`5.1(1k)`）。
- **测试侧定点变异（证明用例与实现共享同一假设 / 打在错误分支）**：
  - **C2**：仅把 `5.1(1l)`（`:920`）09:25:02 拍的 `volume: 0` 删除（还原 R4 验收标准字面形态）→ `5.1(1l)` **转红** ⇒ 该用例通过唯一依赖输入中补入的 `volume: 0`（P2-1 证据链 ②）；
  - **C3**：把 `5.1(1k)`（`:868`）09:25:05 拍的 `open: 0` 改为 `open: 20.5` → **仍 904/0 全绿** ⇒ 形态 D 的保护实际由 `volume: 0`（非 `open`）提供，用例标题所述「无时间戳即不得落成官方柱」的判据未被真正固化；
  - **C4**：把 `5.1(1o)`（`:1063`）的时钟由 09:24:50 改为 09:26:00（意图命中 `:693-708`）→ **仍全绿**；**C5**：M8 + C4 组合 → **仍全绿** ⇒ `5.1(1o)` 走的是**追加**分支（`items` 末柱为昨日），其 `preview` 柱的 `volume/amount` 由 `:599-600` 硬编码为 0，故**无论怎么移动时钟都无法杀 M8**；要触及 `:706-707` 必须构造「先落同日 `preview` 柱 → 再用盘前 `updateTime` 的拍更新」的两步序列（探针 `probe2.mjs` == 1 == 已证实该分支可达：09:26 拍落成 `20.1/20.1/20.1/20.1 vol=0 preview=true`）。
- **根因**：新增用例仍以「单点形态 + 追加分支」为构造单位；本轮改动新增的判据（第三析取项、两处客户端时钟门、重基线组合、包络/塌缩两路）未随改动同步扩充用例；`5.1(1o)` 目标分支与实际落点不符。
- **影响范围**：验收标准 4「测试与变异矩阵全覆盖」未达成；`.589` 第三析取项与 `:581` 时效门的零判别力意味着 P2-1 所描述的 fail-open 即使被修复/回退都无法由门禁察觉（「实现缺失仍全绿」的盲区在本轮新增守卫上依旧存在）。
- **复现方法 / 运行证据**：`_audit_r5/mutate.mjs`、`_audit_r5/mutate2.mjs`、`_audit_r5/mutate3.mjs`（均在仓库外：`git archive 18d2411` 导出 + `New-Item -ItemType Junction` 联接 `node_modules`，逐变异跑 `node scripts/run-tests.mjs` 后还原）；上表为实跑输出，每轮基线重取（904/0）。
- **修复建议**：① 补「无 `volume` 字段 / `volume > 0`」两张无时间戳用例（配合 P2-1 修复），杀 M13/M14/M11(b)；② 补两步序列用例（同日 `preview` 柱 → 09:26 盘前 `updateTime` 拍）断言 `volume === 0 && amount === 0`，并补一条包络语义断言（依 P3-1 的取舍），杀 M8/M7；③ 补一条断言重基线 `low/high` 的**组合**（`open`+`price`+`quoteLow`+包络），杀 M19；④ 补一条「客户端时钟 09:26 vs 09:31 时官方 `quote.high/low` 吸收差异」断言，杀 M16/M17；⑤ 修正 `5.1(1l)` 标题与输入不一致（或按 R4 验收标准去掉 `volume: 0` 并同步修实现）；⑥ 修正 `5.1(1o)` 的时序或改名为其实际覆盖的语义。
- **修复后验收标准**：上表存活项 **M7/M8/M11(b)/M13/M14/M16/M17/M19 全部确定性转红**并给出变异前后对照（四价 / `volume` / `preview` 标记）；M1~M6/M9/M10/M11(a)/M15/M20/M21/M22 杀红力不回归；`npm test` 全绿；用例标题与输入语义一致。

---

## 三、待确认风险与未验证项

1. **冷启动偶发 1 例失败（继承 R4 风险 5，仍未定位）**：仓库外 pristine 导出共运行 19 次，其中 **1 次为 `903/1`**（发生在本轮变异矩阵首次基线运行时，未捕获 `not ok` 用例名），其后连续 18 次均 904/0。无法复现、无法判定是否与本轮改动相关。建议在 CI 上以「失败即保留完整日志」方式观察 3-5 次以定位用例名。
2. **上游 09:25:00 之后是否仍返回盘前 payload、持续多久、以及盘前 payload 的 `volume`/`amount` 是否可能为 0 以外值（P2-1 的直接前提，继承 R4 风险 1/2）**：离线无活体样本。已确证的是代码路径（`parseEastmoney` 无 `updateTime`、`updateTime=''` 落 `null`）与「判定完全由 `volume`/`amount`/`open` 的取值决定」；未确证的是 `volume > 0` 的滞后盘前快照在生产中出现。**修复方向（时效未知 ⇒ 保持 `preview`）不依赖该前提即可闭合，故 P2-1 不因此降级。**
3. **上游 09:26-09:29 分时点与 253 固定网格缺槽（继承 R4 风险 3）**：`chinaStockStrategy.getIntradaySessionRanges()` 仍放行该窗口而网格无槽位，无网络样本，无法判定上游是否实际返回。
4. **港美外盘分时零回归依赖「客户端 `_isTradingSessionTime` + 服务端 `intradayService.js` 双侧过滤完整」这一前提（继承 R4 风险 4）**：本轮未改动相关代码，`5.1(4c/4d/4e)` 与独立探针一致。
5. **A 股 09:25-09:29 不吸收官方 `quote.high/low`（本轮新引入的实现选择）**：R4 修复建议为「时钟 ≥ 09:25 恢复吸收」，实现取 `>= 9*60+30`（`:611`/`:656`）。探针实测该窗口内 `quote.high/low` 被忽略、09:30 起吸收。评估为**无实际影响**（该窗口无连续竞价，官方 `high/low` 即开盘撮合价），登记供修复方确认，不列为缺陷。

---

## 四、推荐修复顺序与复审验收标准

**推荐修复顺序**：

1. **P2-1**（`kline.js:580/:660` 的 `null` 分派 + `:589`/`:668` 的内容代理）——与验收标准 1「彻底消除」直接冲突，且 R4 验收标准 ① 字面未达成，先修。
2. **P3-2**（用例与变异矩阵补齐，随 P2-1 同批落地，含 C2/C3 暴露的标题-输入不一致）。
3. **P3-1**（滞留包络语义二选一并固化，同时决定 `5.1(1o)` 目标分支与重基线组合断言）。

**复审验收标准**（下一轮需逐条提供可复现证据）：

- 探针 A/A2/B/B2 四条序列在 09:25:02 / 09:25:05 拍保持 `preview: true`（或该拍被丢弃），14:55 日K `low === 20.50`；形态 D（`open: 0, volume: 0`）与形态 C（同价掩盖 + 成交量，真实低点 20.30 保留）**不回归**。
- A 股 14:00 报价 `high:12.5/low:8.8` 时末柱 `high === 12.5 && low === 8.8`；盘前（09:18/09:24）脏 `high/low` 仍收敛于 `price`（`5.1(2)`/`5.1(1d)` 不回归）。
- 变异矩阵：**M7/M8/M11(b)/M13/M14/M16/M17/M19 确定性转红**并给出变异前后对照；M1~M6/M9/M10/M11(a)/M15/M20/M21/M22 杀红力不回归；`npm test` 全绿（当前 904 + 新增）、`npx eslint` 0 问题。
- 需求 2（A 股 253 固定网格 6/6 可见 + 09:15-09:25 实时追加窗口 + 港/美/国际期货数据驱动轴零回归）与需求 3（09:20-09:25 强制全量播报 + 基线写回、09:25 恢复去重、智能时段关闭态 09:17/09:22 放行）全体用例与独立探针一致。
- Round 4 三项要求中确已生效的形态（形态 D、形态 C 带有效 `open` 变体、连续时段官方极值吸收）不得回归。
- `git diff` 范围仅含修复与用例，不夹带无关改动。
