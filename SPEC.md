# 股票/期货监控网页 - 技术规格文档

## 1. 项目概述

- **项目名称**: 股票期货实时监控助手
- **项目类型**: 单页 Web 应用 (SPA)
- **核心功能**: 实时监控中国A股、期货市场价格，支持语音播报和价格提醒
- **目标用户**: 股票/期货投资者
- **构建工具**: Vite
- **代码规范**: ESLint

## 2. 技术栈

| 技术 | 版本/来源 |
|------|-----------|
| 前端框架 | 原生 HTML5 + CSS3 + ES6+ |
| 构建工具 | Vite 5.x |
| 代码检查 | ESLint 8.x |
| 图表库 | ECharts 5.x (CDN) |
| 语音合成 | Web Speech API (SpeechSynthesis) |
| 后台任务 | Web Worker |
| 数据存储 | localStorage |
| 测试框架 | QUnit 3.x |
| 数据源 | 新浪财经免费API (主) + 备用免费API |

## 2.1 备用数据源

| 优先级 | 数据源 | 说明 |
|--------|--------|------|
| 1 | 新浪财经 API | `hq.sinajs.cn` |
| 2 | stock-api (npm) | npm包，作为备用 |
| 3 | 阿里云财经API | 备用数据源 |
| 4 | 东方财富API | 备用数据源 |

## 3. 功能模块

### 3.1 数据获取模块 (API)

| 功能 | 实现 |
|------|------|
| 股票数据 | `https://hq.sinajs.cn/list={code}` |
| 期货数据 | `https://hq.sinajs.cn/list=nf{code}` |
| 数据格式 | JSONP 解决跨域 |
| 刷新频率 | 3s / 10s / 30s / 60s (可调) |

### 3.2 监控列表模块

| 功能 | 说明 |
|------|------|
| 添加标的 | 输入代码自动识别股票/期货 |
| 代码前缀 | sh=上海, sz=深圳, nf=期货 |
| 实时价格 | 价格/涨跌幅/涨跌额 |
| 涨跌颜色 | 涨=红, 跌=绿, 平=灰 |
| 删除标的 | 点击删除按钮 |
| 勾选标的 | 复选框选择 |
| 导出选中 | 导出选中股票到txt文件 |
| 持久化 | localStorage 保存 |

### 3.3 走势图模块 (ECharts)

| 图表类型 | 说明 |
|----------|------|
| K线图 | 日K/周K/月K |
| 分时图 | 当日走势 |
| 期货图 | 期货专用 |

### 3.4 提醒模块

| 功能 | 说明 |
|------|------|
| 价格阈值 | 设置涨跌幅阈值 |
| 触发条件 | 达到阈值时触发 |
| 提醒方式 | 语音播报 + 浏览器通知 |

### 3.5 语音播报模块

| 功能 | 说明 |
|------|------|
| 播报方式 | Web Speech API (TTS) |
| 播报内容 | "【代码】现价【价格】元，涨跌【百分比】%" |
| 播报模式 | 定时循环 + 价格触发 |
| 播报间隔 | 5s / 10s / 30s / 60s / 手动 |
| 后台运行 | Web Worker 保持定时任务 |
| 音量控制 | 0-100% 可调 |

### 3.6 涨停看板模块

| 功能 | 说明 |
|------|------|
| 数据获取 | 东财 clist/get 一次拉全市场涨停股（fs=m:0+t:6+f:!2,m:0+t:13+f:!2,m:0+t:80+f:!2） |
| 分类方式 | 4 个分组：3 连板及以上 / 2 连板 / 1 连板或首板 / 炸板 |
| 排序规则 | 4 个选项：连板数 / 涨跌幅 / 封板时间 / 成交金额 |
| 实时价合并 | 监控 timer 后台拉取 → 看板行内价格跟随（不影响连板数等 metadata） |
| 空响应处理 | 锁定显示最近一次非空快照 + 状态栏提示"缓存自 HH:MM · 已空 N 次" |
| 批量加入 | 复选框多选 → "➕ 添加选中" 按钮批量加入监控 + 自动跳监控页 |
| 行内 K 线 | 点击行 → K 线在所点行下方内嵌展开（不跳转） |
| 页面类型 | Hash 路由（`#/limit-up`）独立页面 + SPA 内嵌 |
| 刷新频率 | 10s / 30s / 60s (可调，默认 30s) |

## 4. UI/UX 设计

### 4.1 布局结构

```
┌──────────────────────────────────────────────────┐
│  顶部标题栏 [监控] [涨停看板]                      │
├──────────────────────────────────────────────────┤
│  监控页面                                          │
│  ├─ [搜索栏: 输入代码] [+添加] [导出选中]           │
│  ├─ 监控列表                                       │
│  │  ┌─────────────────────────────────────────┐  │
│  │  │ ☐ sh600519 茅台   ¥1850.00  +2.35%     │  │
│  │  │ ☐ sz000858 五粮液  ¥165.50   -1.20%    │  │
│  │  │ ☐ nf2105  螺纹钢   ¥5200     +0.85%    │  │
│  │  └─────────────────────────────────────────┘  │
│  ├─ 走势图区域 (ECharts)                          │
│  │  [日K] [周K] [月K] [分时]                     │
│  └─ 设置面板                                       │
│       - 刷新频率 [10s ▼]                         │
│       - 语音播报 [☑ 启用] [间隔: 10s ▼]          │
│       - 价格提醒 [☑ 启用] [阈值: ±5%]            │
├──────────────────────────────────────────────────┤
│  涨停看板页面 (独立页面)                           │
│  ├─ [刷新频率: 30s ▼]                            │
│  ├─ 分类展示                                       │
│  │  ┌─────────────────────────────────────────┐  │
│  │  │ ▼ 3连板及以上                            │  │
│  │  │   sh600519 贵州茅台    3连板  +10.00%   │  │
│  │  │   sz000001 平安银行    3连板  +10.00%   │  │
│  │  ├─ ▼ 2连板                                  │  │
│  │  │   sh600036 招商银行    2连板  +10.00%   │  │
│  │  └─ ▼ 首板                                    │  │
│  │      ...                                     │  │
│  └─ 总计: XX只涨停                               │
└──────────────────────────────────────────────────┘
```

### 4.2 配色方案

| 用途 | 颜色 |
|------|------|
| 背景 | #1a1a2e (深蓝黑) |
| 卡片 | #16213e (深蓝) |
| 文字 | #e8e8e8 (浅灰白) |
| 涨 | #ff4757 (红色) |
| 涨停 | #ffd700 (黄色) |
| 跌 | #2ed573 (绿色) |
| 平 | #a4b0be (灰色) |
| 强调 | #3742fa (蓝色) |

### 4.3 字体

- 主字体: "", "PingFMicrosoft YaHeiang SC", sans-serif
- 数字字体: "Roboto Mono", monospace

### 4.4 主题切换

| 主题 | 背景 | 卡片 | 文字 | 涨 | 跌 | 平 |
|------|------|------|------|-----|-----|-----|
| 深色 | #1a1a2e | #16213e | #e8e8e8 | #ff4757 | #2ed573 | #a4b0be |
| 浅色 | #f5f5f5 | #ffffff | #333333 | #e74c3c | #27ae60 | #95a5a6 |

- 主题切换支持: 是
- 默认主题: 深色
- 主题持久化: localStorage

### 4.5 响应式设计

| 设备类型 | 断点 | 布局 |
|----------|------|------|
| 移动端 | < 768px | 单列布局，垂直堆叠 |
| 桌面端 | >= 768px | 多列布局，并排显示 |

- 移动端特性:
  - 监控列表单列显示
  - 图表区域全宽
  - 设置面板抽屉式展开
  - 触摸优化按钮大小 (>= 44px)

## 5. 数据结构

### 5.1 股票/期货对象

```javascript
{
  code: "sh600519",      // 代码 (含前缀)
  name: "贵州茅台",       // 名称
  price: 1850.00,       // 当前价格
  change: 42.50,        // 涨跌额
  changePercent: 2.35,  // 涨跌幅 (%)
  open: 1808.00,        // 开盘价
  high: 1860.00,        // 最高价
  low: 1800.00,         // 最低价
  volume: 1250000,      // 成交量
  type: "stock" | "future"  // 类型
}
```

### 5.2 提醒设置对象

```javascript
{
  alertEnabled: true,
  alertThreshold: 5,      // 涨跌幅阈值 %
  voiceEnabled: true,    // 语音播报启用
  voiceInterval: 10000,  // 播报间隔 (ms)
  voiceVolume: 80,       // 音量 0-100
  selectedForVoice: ["sh600519", "nf2105"]  // 勾选播报的标的
}
```

### 5.3 涨停股票对象

```javascript
{
  code: "sh600519",          // 代码 (含前缀)
  name: "贵州茅台",           // 名称（GBK 解码，UTF-8 fallback）
  market: "sh",              // sh | sz | bj
  price: 1850.00,            // 当前价格（实时价合并可覆盖）
  change: 50.00,             // 涨跌额
  changePercent: 10.00,      // 涨跌幅 %
  amount: 160304839.10,      // 成交额（用于排序）
  open: 1800.00,             // 开盘价
  high: 1850.00,             // 最高价
  low: 1800.00,              // 最低价
  limitUpCount: 3,           // 连板数（0=首板，1+=连板；best-effort per-stock API）
  firstLimitTime: "09:35",   // 首次封板时间 'HH:mm'（未封板=null）
  breakCount: 0,             // 炸板次数（best-effort）
  isST: false,               // 名字含 ST / *ST（用 ST_RE 识别）
  type: "stock"               // 类型
}
```

**注**：
- `code` 字段经 `normalizeCode()` 归一化（自动加 sh/sz/bj 前缀）
- `name` 字段从东财 f14 解码，GBK 编码 + UTF-8 fallback
- 连板数 / 封板时间 / 炸板次数依赖 per-stock API（best-effort，默认 0/null/0）
- 实时价合并只覆盖 `price` / `change` / `changePercent`（来自监控 timer），不更新 metadata

### 5.4 导出功能

| 功能 | 说明 |
|------|------|
| 导出格式 | txt文本文件 |
| 内容格式 | 每行一个股票代码(6位数字，无前缀) |
| 文件命名 | `stocks_yyyyMMdd_HHmmss.txt` |
| 导出方式 | 浏览器下载对话框 |
| 示例内容 | |
| | 600519 |
| | 000858 |
| | 000001 |

## 6. 文件结构 (Vite 标准)

```
project1/
├── index.html              # 主页面入口
├── package.json           # 项目配置
├── vite.config.js         # Vite 配置
├── .eslintrc.cjs          # ESLint 配置
├── SPEC.md                # 本规格文档
├── TODO.md                # 任务清单
├── public/
│   └── alert.mp3          # 备用提示音
├── src/
│   ├── main.js            # 入口文件
│   ├── style.css         # 全局样式
│   ├── css/
│   │   └── components.css # 组件样式
│   └── js/
│       ├── api.js         # API 请求封装
│       ├── parser.js      # 数据解析
│       ├── storage.js     # localStorage 封装
│       ├── worker.js      # Web Worker (后台定时)
│       ├── tts.js         # 语音合成模块
│       ├── chart.js       # ECharts 图表
│       ├── alert.js       # 提醒逻辑
│       ├── router.js      # 页面路由 (SPA)
│       ├── limitUp.js     # 涨停看板逻辑
│       ├── export.js      # 导出功能模块
│       └── app.js         # 主逻辑入口
├── src/pages/
│   ├── index.html         # 监控页面
│   └── limit-up.html      # 涨停看板页面
└── tests/                 # 测试文件
    ├── api.test.js
    ├── parser.test.js
    ├── storage.test.js
    ├── worker.test.js
    ├── tts.test.js
    ├── alert.test.js
    ├── chart.test.js
    ├── limitUp.test.js
    └── app.test.js
```

## 7. 浏览器兼容性

| 浏览器 | 最低版本 |
|--------|----------|
| Chrome | 80+ |
| Firefox | 75+ |
| Safari | 14+ |
| Edge | 80+ |

## 8. 测试规范

### 8.1 测试框架

- 框架: QUnit 3.x
- 运行方式: 浏览器端测试 + CI集成

### 8.2 测试覆盖范围

| 模块 | 测试内容 |
|------|----------|
| api.js | API请求、错误处理、备用源切换 |
| parser.js | 数据解析、格式验证 |
| storage.js | 读写操作、数据迁移 |
| worker.js | 定时任务、消息传递 |
| tts.js | 语音合成、音量控制 |
| alert.js | 阈值检测、通知触发 |
| chart.js | 图表渲染、数据更新 |
| limitUp.js | 涨停数据获取、连板分类、排序 |
| export.js | 导出文件生成、格式验证 |
| app.js | 集成测试、UI交互 |

## 9. 构建与部署

### 9.1 Vite 配置

- 开发服务器: localhost:5173
- 生产构建: `npm run build`
- 代码检查: `npm run lint`
- 运行测试: `npm run test`

## 10. 已知限制

1. **跨域**: 新浪API可能有跨域限制，需使用 JSONP
2. **交易时段**: 非交易时段数据可能不更新
3. **后台运行**: 部分浏览器最小化后可能暂停 Web Worker
4. **语音**: Web Speech API 依赖浏览器支持，部分移动端可能不支持
